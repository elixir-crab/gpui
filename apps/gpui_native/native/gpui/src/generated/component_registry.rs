// This file is generated by RustQ. Do not edit by hand.

#[derive(Clone, Copy, Debug, Eq, Hash, PartialEq)]
enum ComponentKind {
    DropTarget,
    Split,
    Popover,
    Dialog,
    DropdownMenu,
    Input,
    Select,
    Combobox,
    VirtualList,
    VirtualCollection,
    RichText,
    DataTable,
    Tree,
    CodeViewer,
    Slider,
    TextSurface,
}
#[allow(clippy::large_enum_variant)]
enum StatefulComponent {
    DropTarget(ComponentDropTarget),
    Split(ComponentSplit),
    Popover(ComponentPopover),
    Dialog(ComponentDialog),
    DropdownMenu(ComponentDropdownMenu),
    Input(ComponentInput),
    Select(ComponentSelect),
    Combobox(ComponentCombobox),
    VirtualList(ComponentVirtualList),
    VirtualCollection(ComponentVirtualCollection),
    RichText(ComponentRichText),
    DataTable(ComponentDataTable),
    Tree(ComponentTree),
    CodeViewer(ComponentCodeViewer),
    Slider(ComponentSlider),
    TextSurface(ComponentTextSurface),
}
impl ComponentRegistry {
    pub(crate) fn drop_target_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentDropTarget> {
        let key = ComponentKey::new(ComponentKind::DropTarget, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::DropTarget(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_drop_target(
        &mut self,
        id: &str,
        component: ComponentDropTarget,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::DropTarget, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::DropTarget(component)).is_none()
    }
    pub(crate) fn split_mut(&mut self, id: &str) -> Option<&mut ComponentSplit> {
        let key = ComponentKey::new(ComponentKind::Split, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Split(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_split(&mut self, id: &str, component: ComponentSplit) -> bool {
        let key = ComponentKey::new(ComponentKind::Split, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Split(component)).is_none()
    }
    pub(crate) fn popover_mut(&mut self, id: &str) -> Option<&mut ComponentPopover> {
        let key = ComponentKey::new(ComponentKind::Popover, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Popover(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_popover(
        &mut self,
        id: &str,
        component: ComponentPopover,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::Popover, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Popover(component)).is_none()
    }
    pub(crate) fn dialog_mut(&mut self, id: &str) -> Option<&mut ComponentDialog> {
        let key = ComponentKey::new(ComponentKind::Dialog, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Dialog(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_dialog(
        &mut self,
        id: &str,
        component: ComponentDialog,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::Dialog, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Dialog(component)).is_none()
    }
    pub(crate) fn dropdown_menu_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentDropdownMenu> {
        let key = ComponentKey::new(ComponentKind::DropdownMenu, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::DropdownMenu(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_dropdown_menu(
        &mut self,
        id: &str,
        component: ComponentDropdownMenu,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::DropdownMenu, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::DropdownMenu(component)).is_none()
    }
    pub(crate) fn input_mut(&mut self, id: &str) -> Option<&mut ComponentInput> {
        let key = ComponentKey::new(ComponentKind::Input, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Input(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_input(&mut self, id: &str, component: ComponentInput) -> bool {
        let key = ComponentKey::new(ComponentKind::Input, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Input(component)).is_none()
    }
    pub(crate) fn select_mut(&mut self, id: &str) -> Option<&mut ComponentSelect> {
        let key = ComponentKey::new(ComponentKind::Select, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Select(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_select(
        &mut self,
        id: &str,
        component: ComponentSelect,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::Select, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Select(component)).is_none()
    }
    pub(crate) fn combobox_mut(&mut self, id: &str) -> Option<&mut ComponentCombobox> {
        let key = ComponentKey::new(ComponentKind::Combobox, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Combobox(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_combobox(
        &mut self,
        id: &str,
        component: ComponentCombobox,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::Combobox, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Combobox(component)).is_none()
    }
    pub(crate) fn virtual_list_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentVirtualList> {
        let key = ComponentKey::new(ComponentKind::VirtualList, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::VirtualList(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_virtual_list(
        &mut self,
        id: &str,
        component: ComponentVirtualList,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::VirtualList, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::VirtualList(component)).is_none()
    }
    pub(crate) fn virtual_collection_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentVirtualCollection> {
        let key = ComponentKey::new(ComponentKind::VirtualCollection, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::VirtualCollection(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_virtual_collection(
        &mut self,
        id: &str,
        component: ComponentVirtualCollection,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::VirtualCollection, id);
        self.active.insert(key.clone());
        self.entries
            .insert(key, StatefulComponent::VirtualCollection(component))
            .is_none()
    }
    pub(crate) fn rich_text_mut(&mut self, id: &str) -> Option<&mut ComponentRichText> {
        let key = ComponentKey::new(ComponentKind::RichText, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::RichText(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_rich_text(
        &mut self,
        id: &str,
        component: ComponentRichText,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::RichText, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::RichText(component)).is_none()
    }
    pub(crate) fn data_table_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentDataTable> {
        let key = ComponentKey::new(ComponentKind::DataTable, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::DataTable(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_data_table(
        &mut self,
        id: &str,
        component: ComponentDataTable,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::DataTable, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::DataTable(component)).is_none()
    }
    pub(crate) fn tree_mut(&mut self, id: &str) -> Option<&mut ComponentTree> {
        let key = ComponentKey::new(ComponentKind::Tree, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Tree(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_tree(&mut self, id: &str, component: ComponentTree) -> bool {
        let key = ComponentKey::new(ComponentKind::Tree, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Tree(component)).is_none()
    }
    pub(crate) fn code_viewer_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentCodeViewer> {
        let key = ComponentKey::new(ComponentKind::CodeViewer, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::CodeViewer(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_code_viewer(
        &mut self,
        id: &str,
        component: ComponentCodeViewer,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::CodeViewer, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::CodeViewer(component)).is_none()
    }
    pub(crate) fn slider_mut(&mut self, id: &str) -> Option<&mut ComponentSlider> {
        let key = ComponentKey::new(ComponentKind::Slider, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::Slider(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_slider(
        &mut self,
        id: &str,
        component: ComponentSlider,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::Slider, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::Slider(component)).is_none()
    }
    pub(crate) fn text_surface_mut(
        &mut self,
        id: &str,
    ) -> Option<&mut ComponentTextSurface> {
        let key = ComponentKey::new(ComponentKind::TextSurface, id);
        self.active.insert(key.clone());
        match self.entries.get_mut(&key) {
            Some(StatefulComponent::TextSurface(component)) => Some(component),
            _ => None,
        }
    }
    pub(crate) fn insert_text_surface(
        &mut self,
        id: &str,
        component: ComponentTextSurface,
    ) -> bool {
        let key = ComponentKey::new(ComponentKind::TextSurface, id);
        self.active.insert(key.clone());
        self.entries.insert(key, StatefulComponent::TextSurface(component)).is_none()
    }
    pub(crate) fn remove_text_surface(&mut self, id: &str) -> bool {
        let key = ComponentKey::new(ComponentKind::TextSurface, id);
        self.active.remove(&key);
        self.entries.remove(&key).is_some()
    }
}
