# GPUI Components

Declarative conventional controls for GPUI, rendered by the
`:gpui_component` native host with `gpui-component`.

See the [project website](https://github.com/elixir-crab/gpui) and the
[component guide](https://hexdocs.pm/gpui/components.html).
