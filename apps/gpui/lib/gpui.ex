defmodule GPUI do
  @moduledoc """
  Elixir-facing entry point for GPUI applications and views.

  Views normally use `GPUI.Template` through `use GPUI.View`. Programmatic
  element construction is available explicitly from `GPUI.Builder`.
  """
end
