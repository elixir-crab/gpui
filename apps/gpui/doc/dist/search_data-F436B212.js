searchData={"items":[{"type":"module","title":"GPUI.Accessibility","doc":"Bounded renderer-independent accessibility contracts for generic elements.\n\nSemantic native components own their roles and state policy. This module\nvalidates explicit metadata on generic primitives before it crosses a display\nor transport boundary.","ref":"GPUI.Accessibility.html"},{"type":"type","title":"GPUI.Accessibility.checked/0","doc":"","ref":"GPUI.Accessibility.html#t:checked/0"},{"type":"type","title":"GPUI.Accessibility.interaction/0","doc":"","ref":"GPUI.Accessibility.html#t:interaction/0"},{"type":"type","title":"GPUI.Accessibility.role/0","doc":"","ref":"GPUI.Accessibility.html#t:role/0"},{"type":"type","title":"GPUI.Accessibility.role_spec/0","doc":"","ref":"GPUI.Accessibility.html#t:role_spec/0"},{"type":"type","title":"GPUI.Accessibility.state_rule/0","doc":"","ref":"GPUI.Accessibility.html#t:state_rule/0"},{"type":"function","title":"GPUI.Accessibility.attrs/0","doc":"Returns generic accessibility attributes accepted by schema containers.","ref":"GPUI.Accessibility.html#attrs/0"},{"type":"function","title":"GPUI.Accessibility.metadata?/1","doc":"Returns whether an attribute map contains generic accessibility metadata.","ref":"GPUI.Accessibility.html#metadata?/1"},{"type":"function","title":"GPUI.Accessibility.role_specs/0","doc":"Returns role specifications used by validation and native code generation.","ref":"GPUI.Accessibility.html#role_specs/0"},{"type":"function","title":"GPUI.Accessibility.roles/0","doc":"Returns the closed generic accessibility role vocabulary.","ref":"GPUI.Accessibility.html#roles/0"},{"type":"function","title":"GPUI.Accessibility.state_roles/0","doc":"Returns accessibility state-to-role validation rules.","ref":"GPUI.Accessibility.html#state_roles/0"},{"type":"function","title":"GPUI.Accessibility.validate_generic!/2","doc":"Validates generic accessibility metadata for one supported element tag.","ref":"GPUI.Accessibility.html#validate_generic!/2"},{"type":"module","title":"GPUI.Color","doc":"Compile-time hexadecimal RGB and RGBA color literals.\n\n`~RGB` accepts three or six hexadecimal digits. `~RGBA` accepts four or\neight hexadecimal digits. Short literals expand each digit, so `~RGB\"abc\"`\nis equivalent to `{:rgb, 0xAABBCC}`.\n\nThese sigils deliberately omit a leading `#` because their names already\nidentify the literal as a hexadecimal color.","ref":"GPUI.Color.html"},{"type":"type","title":"GPUI.Color.rgb/0","doc":"","ref":"GPUI.Color.html#t:rgb/0"},{"type":"type","title":"GPUI.Color.rgba/0","doc":"","ref":"GPUI.Color.html#t:rgba/0"},{"type":"type","title":"GPUI.Color.t/0","doc":"","ref":"GPUI.Color.html#t:t/0"},{"type":"macro","title":"GPUI.Color.sigil_RGB/2","doc":"Builds an opaque RGB literal from three or six hexadecimal digits.","ref":"GPUI.Color.html#sigil_RGB/2"},{"type":"macro","title":"GPUI.Color.sigil_RGBA/2","doc":"Builds an RGBA literal from four or eight hexadecimal digits.","ref":"GPUI.Color.html#sigil_RGBA/2"},{"type":"module","title":"GPUI.Native","doc":"Availability information for the optional native GPUI backend.\n\nApplication code normally uses `GPUI.Runtime`, `GPUI.Display.Native`,\n`GPUI.Image`, and `GPUI.Text.Buffer` rather than calling the native backend\ndirectly. The latter three capabilities are provided by the `gpui_native`\npackage; without it, native-backed APIs return\n`{:error, :native_backend_unavailable}`.","ref":"GPUI.Native.html"},{"type":"function","title":"GPUI.Native.available?/0","doc":"Reports whether the configured native backend loaded successfully.","ref":"GPUI.Native.html#available?/0"},{"type":"function","title":"GPUI.Native.compiled?/0","doc":"Deprecated alias for `available?/0`.","ref":"GPUI.Native.html#compiled?/0"},{"type":"module","title":"GPUI.Schema","doc":"Canonical element, component, event, resource, and style protocol schema.","ref":"GPUI.Schema.html"},{"type":"function","title":"GPUI.Schema.apply_defaults/2","doc":"","ref":"GPUI.Schema.html#apply_defaults/2"},{"type":"function","title":"GPUI.Schema.component!/1","doc":"","ref":"GPUI.Schema.html#component!/1"},{"type":"function","title":"GPUI.Schema.component_options_doc/1","doc":"Returns generated public option documentation for a component tag.","ref":"GPUI.Schema.html#component_options_doc/1"},{"type":"function","title":"GPUI.Schema.components/0","doc":"","ref":"GPUI.Schema.html#components/0"},{"type":"function","title":"GPUI.Schema.defaults/1","doc":"","ref":"GPUI.Schema.html#defaults/1"},{"type":"macro","title":"GPUI.Schema.define_component_option_types/1","doc":"Defines public component option types from schema definitions.","ref":"GPUI.Schema.html#define_component_option_types/1"},{"type":"function","title":"GPUI.Schema.events/0","doc":"","ref":"GPUI.Schema.html#events/0"},{"type":"function","title":"GPUI.Schema.extension/1","doc":"Returns one schema-owned presentation contract by ID.","ref":"GPUI.Schema.html#extension/1"},{"type":"function","title":"GPUI.Schema.extensions/0","doc":"Returns all schema-owned versioned presentation contracts.","ref":"GPUI.Schema.html#extensions/0"},{"type":"function","title":"GPUI.Schema.identified_tags/0","doc":"","ref":"GPUI.Schema.html#identified_tags/0"},{"type":"function","title":"GPUI.Schema.native_tags/0","doc":"Returns every renderer-native schema tag in declaration order.","ref":"GPUI.Schema.html#native_tags/0"},{"type":"function","title":"GPUI.Schema.registry/0","doc":"Returns an immutable registry containing neutral core declarations.","ref":"GPUI.Schema.html#registry/0"},{"type":"function","title":"GPUI.Schema.registry/1","doc":"Returns a composed registry for explicitly selected schema modules.","ref":"GPUI.Schema.html#registry/1"},{"type":"function","title":"GPUI.Schema.resource_specs/0","doc":"","ref":"GPUI.Schema.html#resource_specs/0"},{"type":"function","title":"GPUI.Schema.resources/0","doc":"","ref":"GPUI.Schema.html#resources/0"},{"type":"function","title":"GPUI.Schema.stateful_components/0","doc":"","ref":"GPUI.Schema.html#stateful_components/0"},{"type":"function","title":"GPUI.Schema.style_specs/0","doc":"","ref":"GPUI.Schema.html#style_specs/0"},{"type":"function","title":"GPUI.Schema.styles/0","doc":"","ref":"GPUI.Schema.html#styles/0"},{"type":"function","title":"GPUI.Schema.tags/0","doc":"","ref":"GPUI.Schema.html#tags/0"},{"type":"function","title":"GPUI.Schema.validate_component_assigns!/3","doc":"","ref":"GPUI.Schema.html#validate_component_assigns!/3"},{"type":"module","title":"GPUI.Schema.Component","doc":"Declarative schema for one renderer-native element or UI component.","ref":"GPUI.Schema.Component.html"},{"type":"type","title":"GPUI.Schema.Component.attr_type/0","doc":"","ref":"GPUI.Schema.Component.html#t:attr_type/0"},{"type":"type","title":"GPUI.Schema.Component.scalar_type/0","doc":"","ref":"GPUI.Schema.Component.html#t:scalar_type/0"},{"type":"type","title":"GPUI.Schema.Component.t/0","doc":"","ref":"GPUI.Schema.Component.html#t:t/0"},{"type":"function","title":"GPUI.Schema.Component.renderer_internal?/1","doc":"Returns whether a schema component exists only for renderer orchestration.","ref":"GPUI.Schema.Component.html#renderer_internal?/1"},{"type":"module","title":"GPUI.Schema.Registry","doc":"Immutable composition of explicitly selected declarative schema modules.\n\nA registry is assembled by the framework or maintainer tooling from known\nmodules. It is not a runtime plugin registry and does not accept renderer\ncallbacks or opaque native payloads.","ref":"GPUI.Schema.Registry.html"},{"type":"type","title":"GPUI.Schema.Registry.provider_entry/0","doc":"","ref":"GPUI.Schema.Registry.html#t:provider_entry/0"},{"type":"type","title":"GPUI.Schema.Registry.schema_module/0","doc":"","ref":"GPUI.Schema.Registry.html#t:schema_module/0"},{"type":"type","title":"GPUI.Schema.Registry.t/0","doc":"","ref":"GPUI.Schema.Registry.html#t:t/0"},{"type":"function","title":"GPUI.Schema.Registry.component!/2","doc":"Returns one component from the composed registry.","ref":"GPUI.Schema.Registry.html#component!/2"},{"type":"function","title":"GPUI.Schema.Registry.components/1","doc":"Returns the composed components in declaration order.","ref":"GPUI.Schema.Registry.html#components/1"},{"type":"function","title":"GPUI.Schema.Registry.entries/1","doc":"Returns provider/component entries in composed declaration order.","ref":"GPUI.Schema.Registry.html#entries/1"},{"type":"function","title":"GPUI.Schema.Registry.from_components/1","doc":"Builds an immutable registry directly from already-validated component declarations.","ref":"GPUI.Schema.Registry.html#from_components/1"},{"type":"function","title":"GPUI.Schema.Registry.include/2","doc":"Includes one explicit schema module and rejects duplicate tags.","ref":"GPUI.Schema.Registry.html#include/2"},{"type":"function","title":"GPUI.Schema.Registry.native_tags/1","doc":"Returns all composed native tags in declaration order.","ref":"GPUI.Schema.Registry.html#native_tags/1"},{"type":"function","title":"GPUI.Schema.Registry.new/0","doc":"Creates an empty immutable schema registry.","ref":"GPUI.Schema.Registry.html#new/0"},{"type":"function","title":"GPUI.Schema.Registry.order/2","doc":"Orders a complete registry by an explicit canonical tag manifest.","ref":"GPUI.Schema.Registry.html#order/2"},{"type":"function","title":"GPUI.Schema.Registry.provider!/2","doc":"Returns the explicit provider module for one composed component.","ref":"GPUI.Schema.Registry.html#provider!/2"},{"type":"function","title":"GPUI.Schema.Registry.stateful_components/1","doc":"Returns the stateful components in declaration order.","ref":"GPUI.Schema.Registry.html#stateful_components/1"},{"type":"module","title":"GPUI.Text.RichRun","doc":"A neutral shaping run for immutable rich text.\n\nThe half-open `range` uses zero-based `{line, utf16_offset}` positions. Runs\ncarry renderer facts only; Markdown, syntax, diagnostics, and product policy\nremain consumer-owned. `link` is an opaque bounded application value emitted\nthrough the rich text component's ordinary link event.","ref":"GPUI.Text.RichRun.html"},{"type":"type","title":"GPUI.Text.RichRun.rgb/0","doc":"","ref":"GPUI.Text.RichRun.html#t:rgb/0"},{"type":"type","title":"GPUI.Text.RichRun.t/0","doc":"","ref":"GPUI.Text.RichRun.html#t:t/0"},{"type":"type","title":"GPUI.Text.RichRun.underline_style/0","doc":"","ref":"GPUI.Text.RichRun.html#t:underline_style/0"},{"type":"function","title":"GPUI.Text.RichRun.new/2","doc":"","ref":"GPUI.Text.RichRun.html#new/2"},{"type":"function","title":"GPUI.Text.RichRun.validate!/1","doc":"Validates the bounded styles, colors, range, and optional link of a rich run.","ref":"GPUI.Text.RichRun.html#validate!/1"},{"type":"module","title":"GPUI.Text.StyleRun","doc":"A neutral shaping style applied to a logical text range.\n\nStyle runs carry presentation facts only. They do not identify syntax,\ndiagnostics, languages, or semantic-token kinds. Positions remain zero-based\nUTF-16 document coordinates and the native text surface converts them to\nshaping ranges without changing buffer contents or history.","ref":"GPUI.Text.StyleRun.html"},{"type":"type","title":"GPUI.Text.StyleRun.font_style/0","doc":"","ref":"GPUI.Text.StyleRun.html#t:font_style/0"},{"type":"type","title":"GPUI.Text.StyleRun.font_weight/0","doc":"","ref":"GPUI.Text.StyleRun.html#t:font_weight/0"},{"type":"type","title":"GPUI.Text.StyleRun.rgb/0","doc":"","ref":"GPUI.Text.StyleRun.html#t:rgb/0"},{"type":"type","title":"GPUI.Text.StyleRun.t/0","doc":"","ref":"GPUI.Text.StyleRun.html#t:t/0"},{"type":"function","title":"GPUI.Text.StyleRun.new/2","doc":"","ref":"GPUI.Text.StyleRun.html#new/2"},{"type":"function","title":"GPUI.Text.StyleRun.validate!/1","doc":"Validates a style run's color, font values, and non-empty presentation.","ref":"GPUI.Text.StyleRun.html#validate!/1"},{"type":"module","title":"GPUI.Transfer.Event","doc":"A bounded renderer-independent drag/drop event fact.\n\n`position` is expressed in the closed `:window_native_pixels` coordinate\nspace. External paths in `payload` always name resources on the machine\nrunning the display; this value does not read or interpret them.","ref":"GPUI.Transfer.Event.html"},{"type":"type","title":"GPUI.Transfer.Event.coordinate_space/0","doc":"","ref":"GPUI.Transfer.Event.html#t:coordinate_space/0"},{"type":"type","title":"GPUI.Transfer.Event.position/0","doc":"","ref":"GPUI.Transfer.Event.html#t:position/0"},{"type":"type","title":"GPUI.Transfer.Event.t/0","doc":"","ref":"GPUI.Transfer.Event.html#t:t/0"},{"type":"type","title":"GPUI.Transfer.Event.type/0","doc":"","ref":"GPUI.Transfer.Event.html#t:type/0"},{"type":"function","title":"GPUI.Transfer.Event.normalize/2","doc":"Normalizes a native wire map or validates an existing transfer event value.","ref":"GPUI.Transfer.Event.html#normalize/2"},{"type":"function","title":"GPUI.Transfer.Event.normalize!/2","doc":"Normalizes a transfer event value or raises `ArgumentError`.","ref":"GPUI.Transfer.Event.html#normalize!/2"},{"type":"function","title":"GPUI.Transfer.Event.to_payload/1","doc":"Converts a canonical transfer event into its serializable native/remote wire map.","ref":"GPUI.Transfer.Event.html#to_payload/1"},{"type":"function","title":"GPUI.Transfer.Event.type?/1","doc":"Returns whether a term is a supported drag/drop event type.","ref":"GPUI.Transfer.Event.html#type?/1"},{"type":"module","title":"GPUI.Transfer.Payload","doc":"Bounded renderer-independent clipboard or drag/drop facts.\n\nExternal paths always refer to the machine running the display. A payload\ndoes not read files, infer MIME types, upload content, or assign product\nmeaning to paths.","ref":"GPUI.Transfer.Payload.html"},{"type":"type","title":"GPUI.Transfer.Payload.t/0","doc":"","ref":"GPUI.Transfer.Payload.html#t:t/0"},{"type":"function","title":"GPUI.Transfer.Payload.new/1","doc":"","ref":"GPUI.Transfer.Payload.html#new/1"},{"type":"function","title":"GPUI.Transfer.Payload.to_payload/1","doc":"Converts a canonical transfer payload into a serializable map.","ref":"GPUI.Transfer.Payload.html#to_payload/1"},{"type":"function","title":"GPUI.Transfer.Payload.validate!/1","doc":"Validates that a transfer payload is already canonical and bounded.","ref":"GPUI.Transfer.Payload.html#validate!/1"},{"type":"module","title":"GPUI","doc":"Elixir-facing entry point for GPUI applications and views.\n\nViews normally use `GPUI.Template` through `use GPUI.View`. Programmatic\nelement construction is available explicitly from `GPUI.Builder`.","ref":"GPUI.html"},{"type":"behaviour","title":"GPUI.Application","doc":"Behaviour and DSL for OTP-supervised GPUI applications.\n\n`mount/1` declares a session's initial windows. Interactive state belongs to\neach root view's assigns rather than an unused application-level state value.\nWindow blocks may bind stable command IDs with `shortcut/2`; native and remote\ndisplays dispatch them to the same view handlers used by buttons and menus.","ref":"GPUI.Application.html"},{"type":"callback","title":"GPUI.Application.identity/0","doc":"","ref":"GPUI.Application.html#c:identity/0"},{"type":"callback","title":"GPUI.Application.mount/1","doc":"Builds the initial renderer-independent window set for a session.","ref":"GPUI.Application.html#c:mount/1"},{"type":"macro","title":"GPUI.Application.chrome/1","doc":"Declares whether the window uses system or application-rendered content chrome.","ref":"GPUI.Application.html#chrome/1"},{"type":"function","title":"GPUI.Application.identity/1","doc":"Returns and validates an application's declared identity, when present.","ref":"GPUI.Application.html#identity/1"},{"type":"macro","title":"GPUI.Application.min_size/2","doc":"Declares a minimum window size inside `window`.","ref":"GPUI.Application.html#min_size/2"},{"type":"macro","title":"GPUI.Application.resizable/1","doc":"Declares whether the native window can be resized by the user.","ref":"GPUI.Application.html#resizable/1"},{"type":"macro","title":"GPUI.Application.root/2","doc":"Declares a root view inside `window`.","ref":"GPUI.Application.html#root/2"},{"type":"macro","title":"GPUI.Application.shortcut/2","doc":"Binds an application command to a modified shortcut inside `window`.","ref":"GPUI.Application.html#shortcut/2"},{"type":"macro","title":"GPUI.Application.size/2","doc":"Declares a window size inside `window`.","ref":"GPUI.Application.html#size/2"},{"type":"macro","title":"GPUI.Application.window/2","doc":"Builds a window specification from a DSL block.","ref":"GPUI.Application.html#window/2"},{"type":"macro","title":"GPUI.Application.window/3","doc":"Builds a keyed window specification from a DSL block.","ref":"GPUI.Application.html#window/3"},{"type":"module","title":"GPUI.Application.Icon","doc":"Application-owned icon source metadata.\n\n`source` is a relative path to an icon asset or asset directory. Platform\npackaging remains responsible for producing bundle, executable, and desktop\nintegration resources.","ref":"GPUI.Application.Icon.html"},{"type":"type","title":"GPUI.Application.Icon.t/0","doc":"","ref":"GPUI.Application.Icon.html#t:t/0"},{"type":"function","title":"GPUI.Application.Icon.new!/1","doc":"","ref":"GPUI.Application.Icon.html#new!/1"},{"type":"function","title":"GPUI.Application.Icon.validate!/1","doc":"","ref":"GPUI.Application.Icon.html#validate!/1"},{"type":"module","title":"GPUI.Application.Identity","doc":"Stable process-wide identity for a GPUI application.\n\nThe identifier should use reverse-DNS form. `icon` names an application-owned\nsource asset or asset set; platform release tooling remains responsible for\nproducing macOS bundles, Windows resources, and Linux desktop entries.","ref":"GPUI.Application.Identity.html"},{"type":"type","title":"GPUI.Application.Identity.t/0","doc":"","ref":"GPUI.Application.Identity.html#t:t/0"},{"type":"function","title":"GPUI.Application.Identity.new!/1","doc":"","ref":"GPUI.Application.Identity.html#new!/1"},{"type":"function","title":"GPUI.Application.Identity.validate!/1","doc":"","ref":"GPUI.Application.Identity.html#validate!/1"},{"type":"module","title":"GPUI.Builder","doc":"Programmatic builders for immutable `GPUI.Element` trees.\n\nMost views use the `~GPUI` template sigil. Import this module explicitly when\npipe-style construction is a better fit for generated or data-driven trees.","ref":"GPUI.Builder.html"},{"type":"function","title":"GPUI.Builder.bg/2","doc":"Sets background color.","ref":"GPUI.Builder.html#bg/2"},{"type":"function","title":"GPUI.Builder.child/2","doc":"Adds a child to an element for pipe-style view construction.","ref":"GPUI.Builder.html#child/2"},{"type":"macro","title":"GPUI.Builder.div/0","doc":"Creates a `div` element.","ref":"GPUI.Builder.html#div/0"},{"type":"macro","title":"GPUI.Builder.div/1","doc":"","ref":"GPUI.Builder.html#div/1"},{"type":"macro","title":"GPUI.Builder.div/2","doc":"","ref":"GPUI.Builder.html#div/2"},{"type":"function","title":"GPUI.Builder.flex/1","doc":"Adds flex display style to an element.","ref":"GPUI.Builder.html#flex/1"},{"type":"function","title":"GPUI.Builder.flex_col/1","doc":"Adds column flex direction to an element.","ref":"GPUI.Builder.html#flex_col/1"},{"type":"function","title":"GPUI.Builder.items_center/1","doc":"Centers children on the cross axis.","ref":"GPUI.Builder.html#items_center/1"},{"type":"function","title":"GPUI.Builder.justify_center/1","doc":"Centers children on the main axis.","ref":"GPUI.Builder.html#justify_center/1"},{"type":"function","title":"GPUI.Builder.px/1","doc":"Represents a GPUI pixel length.","ref":"GPUI.Builder.html#px/1"},{"type":"function","title":"GPUI.Builder.rgb/1","doc":"Represents a GPUI RGB color.","ref":"GPUI.Builder.html#rgb/1"},{"type":"function","title":"GPUI.Builder.size/2","doc":"Sets width and height to the same value.","ref":"GPUI.Builder.html#size/2"},{"type":"function","title":"GPUI.Builder.style/3","doc":"Sets a schema-backed style on an element.","ref":"GPUI.Builder.html#style/3"},{"type":"macro","title":"GPUI.Builder.text/1","doc":"Creates a text node.","ref":"GPUI.Builder.html#text/1"},{"type":"module","title":"GPUI.Command","doc":"Declarative application command bound to a modified keyboard shortcut.\n\nCommand IDs are the same non-empty event names used by buttons and menus.\nShortcuts use `primary` for Command on macOS and Control elsewhere, with\noptional `ctrl`, `alt`, and `shift` modifiers, for example `primary-r` or\n`primary-shift-p`.","ref":"GPUI.Command.html"},{"type":"type","title":"GPUI.Command.t/0","doc":"","ref":"GPUI.Command.html#t:t/0"},{"type":"function","title":"GPUI.Command.new/2","doc":"Builds and validates one application command.","ref":"GPUI.Command.html#new/2"},{"type":"function","title":"GPUI.Command.to_payload/1","doc":"Converts a command into its renderer-independent protocol tuple.","ref":"GPUI.Command.html#to_payload/1"},{"type":"function","title":"GPUI.Command.validate_all!/1","doc":"Validates command count, IDs, shortcuts, and uniqueness.","ref":"GPUI.Command.html#validate_all!/1"},{"type":"module","title":"GPUI.Debug","doc":"Renderer-independent inspection of authoritative GPUI snapshots and trees.","ref":"GPUI.Debug.html"},{"type":"function","title":"GPUI.Debug.format_tree/2","doc":"Formats a bounded, human-readable tree.","ref":"GPUI.Debug.html#format_tree/2"},{"type":"function","title":"GPUI.Debug.print_tree/2","doc":"Prints a bounded tree and returns the original source.","ref":"GPUI.Debug.html#print_tree/2"},{"type":"function","title":"GPUI.Debug.tree/2","doc":"Returns a window's authoritative element tree. Runtime render failures raise `GPUI.Runtime.Error`.","ref":"GPUI.Debug.html#tree/2"},{"type":"module","title":"GPUI.Dev.Reload","doc":"Development-time source reloading for a running GPUI runtime.\n\n`watch/2` watches explicitly listed Elixir source files, recompiles a changed\nfile, and asks the runtime to render its existing windows again. Window\nassigns remain authoritative, so ordinary `render/1`, `handle_event/3`, and\n`handle_info/2` edits take effect without reopening the native window.\n\nA watcher may receive a `:notify` PID. After every attempted file reload it\nreceives `{:gpui_reload, watcher, path, {:ok, modules}}` or\n`{:gpui_reload, watcher, path, {:error, reason}}`. Files are parsed before\ncompilation so syntax failures cannot unload or partially redefine the live\nroot module.\n\nThis facility is intended for trusted local development source. It requires\nthe optional `:file_system` dependency; applications that do not use source\nreload do not need to start or include it. The watcher does not reload native\ncode, remount applications, or migrate changed state shapes.","ref":"GPUI.Dev.Reload.html"},{"type":"function","title":"GPUI.Dev.Reload.child_spec/1","doc":"Returns a specification to start this module under a supervisor.\n\nSee `Supervisor`.","ref":"GPUI.Dev.Reload.html#child_spec/1"},{"type":"function","title":"GPUI.Dev.Reload.wait/2","doc":"Keeps an example alive and enables source reloading under `mix gpui.dev`.","ref":"GPUI.Dev.Reload.html#wait/2"},{"type":"function","title":"GPUI.Dev.Reload.watch/2","doc":"Starts a source watcher linked to the caller.","ref":"GPUI.Dev.Reload.html#watch/2"},{"type":"module","title":"GPUI.Runtime","doc":"Primary application-facing process API for one GPUI application and display.\n\nA runtime starts the renderer-independent `GPUI.Session`, owns the configured\n`GPUI.Display`, synchronizes authoritative snapshots, drains strict typed\ndisplay events, and publishes completed updates to subscribers. Applications\nnormally supervise their `GPUI.Application`, whose child specification starts\nthis process; direct `GPUI.Session` use is reserved for remote hosting and\ncustom display infrastructure.\n\nUse this module for application operations such as snapshots, resources,\ndynamic windows, view messages, subscriptions, and frame barriers.","ref":"GPUI.Runtime.html"},{"type":"module","title":"Options - GPUI.Runtime","doc":"* `:app` - required `GPUI.Application` module;\n  * `:args` - application mount argument, defaulting to `[]`;\n  * `:display` - display module, defaulting to `GPUI.Display.Native`;\n  * `:display_opts` - options passed to the display;\n  * `:poll_interval` - positive milliseconds or `nil` to disable polling,\n    defaulting to `16`;\n  * `:name` - optional runtime process name.","ref":"GPUI.Runtime.html#module-options"},{"type":"type","title":"GPUI.Runtime.display_error/0","doc":"","ref":"GPUI.Runtime.html#t:display_error/0"},{"type":"type","title":"GPUI.Runtime.error/0","doc":"","ref":"GPUI.Runtime.html#t:error/0"},{"type":"type","title":"GPUI.Runtime.state/0","doc":"","ref":"GPUI.Runtime.html#t:state/0"},{"type":"function","title":"GPUI.Runtime.await_frame/3","doc":"Waits until a complete display frame follows the current window state.","ref":"GPUI.Runtime.html#await_frame/3"},{"type":"function","title":"GPUI.Runtime.await_frame_after/4","doc":"Waits for a display frame completed after the supplied generation.","ref":"GPUI.Runtime.html#await_frame_after/4"},{"type":"function","title":"GPUI.Runtime.child_spec/1","doc":"Returns a specification to start this module under a supervisor.\n\nSee `Supervisor`.","ref":"GPUI.Runtime.html#child_spec/1"},{"type":"function","title":"GPUI.Runtime.close_window/2","doc":"Closes a declarative window by key or session ID and synchronizes the display.","ref":"GPUI.Runtime.html#close_window/2"},{"type":"function","title":"GPUI.Runtime.dispatch_event/2","doc":"Validates and dispatches one explicit typed event and synchronizes the resulting snapshot.","ref":"GPUI.Runtime.html#dispatch_event/2"},{"type":"function","title":"GPUI.Runtime.drain_events/1","doc":"Drains display events, applies them to the session, and synchronizes the result.","ref":"GPUI.Runtime.html#drain_events/1"},{"type":"function","title":"GPUI.Runtime.drop_resource/2","doc":"Drops a session resource and synchronizes the resulting snapshot.","ref":"GPUI.Runtime.html#drop_resource/2"},{"type":"function","title":"GPUI.Runtime.events/1","doc":"Returns the bounded history of handled display events.","ref":"GPUI.Runtime.html#events/1"},{"type":"function","title":"GPUI.Runtime.frame_token/2","doc":"Returns the latest completed display frame generation for a window.","ref":"GPUI.Runtime.html#frame_token/2"},{"type":"function","title":"GPUI.Runtime.info/1","doc":"Returns stable runtime topology and synchronization information.","ref":"GPUI.Runtime.html#info/1"},{"type":"function","title":"GPUI.Runtime.inject_event/2","doc":"Injects an event into the active display queue without dispatching it immediately.","ref":"GPUI.Runtime.html#inject_event/2"},{"type":"function","title":"GPUI.Runtime.open_window/2","doc":"Adds a keyed declarative window and synchronizes it to the display.","ref":"GPUI.Runtime.html#open_window/2"},{"type":"function","title":"GPUI.Runtime.put_resource/3","doc":"Stores a session resource and synchronizes the resulting snapshot.","ref":"GPUI.Runtime.html#put_resource/3"},{"type":"function","title":"GPUI.Runtime.refresh/1","doc":"Rerenders every window from its current module and assigns, then synchronizes the display.","ref":"GPUI.Runtime.html#refresh/1"},{"type":"function","title":"GPUI.Runtime.request_frame/1","doc":"Requests a display frame for the current snapshot without changing application state.","ref":"GPUI.Runtime.html#request_frame/1"},{"type":"function","title":"GPUI.Runtime.send_view/3","doc":"Delivers an OTP message to a window's root view and synchronizes the display.","ref":"GPUI.Runtime.html#send_view/3"},{"type":"function","title":"GPUI.Runtime.snapshot/1","doc":"Returns the current authoritative session snapshot.","ref":"GPUI.Runtime.html#snapshot/1"},{"type":"function","title":"GPUI.Runtime.snapshot!/1","doc":"Returns the current authoritative session snapshot or raises when rendering fails.","ref":"GPUI.Runtime.html#snapshot!/1"},{"type":"function","title":"GPUI.Runtime.start_link/1","doc":"Starts a runtime linked to the caller.","ref":"GPUI.Runtime.html#start_link/1"},{"type":"function","title":"GPUI.Runtime.subscribe/1","doc":"Subscribes the calling process to synchronized runtime updates.","ref":"GPUI.Runtime.html#subscribe/1"},{"type":"function","title":"GPUI.Runtime.unsubscribe/1","doc":"Unsubscribes the calling process from runtime updates.","ref":"GPUI.Runtime.html#unsubscribe/1"},{"type":"function","title":"GPUI.Runtime.windows/1","doc":"Returns the runtime session's declarative windows.","ref":"GPUI.Runtime.html#windows/1"},{"type":"exception","title":"GPUI.Runtime.Error","doc":"Exception raised by a bang runtime operation when its non-bang form returns an error.","ref":"GPUI.Runtime.Error.html"},{"type":"module","title":"GPUI.Runtime.Update","doc":"A synchronized runtime update delivered to `GPUI.Runtime` subscribers.\n\n`revision` increases monotonically within one runtime. `events` contains the\nnormalized events handled to produce `snapshot`.","ref":"GPUI.Runtime.Update.html"},{"type":"type","title":"GPUI.Runtime.Update.t/0","doc":"","ref":"GPUI.Runtime.Update.html#t:t/0"},{"type":"module","title":"GPUI.Snapshot","doc":"Renderer-independent snapshot of a running `GPUI.Session`.\n\nSnapshots are the only state transferred from sessions to local or remote\ndisplays.","ref":"GPUI.Snapshot.html"},{"type":"type","title":"GPUI.Snapshot.t/0","doc":"","ref":"GPUI.Snapshot.html#t:t/0"},{"type":"module","title":"GPUI.Snapshot.Window","doc":"Typed shape of one serialized window in a `GPUI.Snapshot`.\n\nWindow snapshots remain plain maps so they can cross local and remote display\nboundaries without conversion. This module supplies named map types for that\nstable in-process contract.","ref":"GPUI.Snapshot.Window.html"},{"type":"type","title":"GPUI.Snapshot.Window.root/0","doc":"","ref":"GPUI.Snapshot.Window.html#t:root/0"},{"type":"type","title":"GPUI.Snapshot.Window.t/0","doc":"","ref":"GPUI.Snapshot.Window.html#t:t/0"},{"type":"module","title":"GPUI.Tree","doc":"Renderer-independent queries over `GPUI.Element` and serialized element trees.","ref":"GPUI.Tree.html"},{"type":"type","title":"GPUI.Tree.element_node/0","doc":"","ref":"GPUI.Tree.html#t:element_node/0"},{"type":"type","title":"GPUI.Tree.selector/0","doc":"","ref":"GPUI.Tree.html#t:selector/0"},{"type":"function","title":"GPUI.Tree.all/2","doc":"Returns every node matching a selector.","ref":"GPUI.Tree.html#all/2"},{"type":"function","title":"GPUI.Tree.find/2","doc":"Returns the first matching node, or nil.","ref":"GPUI.Tree.html#find/2"},{"type":"function","title":"GPUI.Tree.find!/2","doc":"Returns the first matching node or raises.","ref":"GPUI.Tree.html#find!/2"},{"type":"function","title":"GPUI.Tree.matches?/2","doc":"Returns whether an element node matches every selector attribute.","ref":"GPUI.Tree.html#matches?/2"},{"type":"function","title":"GPUI.Tree.path/2","doc":"Returns the root-to-node path for the first match.","ref":"GPUI.Tree.html#path/2"},{"type":"function","title":"GPUI.Tree.walk/1","doc":"Returns all nodes in depth-first order.","ref":"GPUI.Tree.html#walk/1"},{"type":"behaviour","title":"GPUI.View","doc":"Behaviour for Elixir-rendered GPUI views.\n\nViews render from assigns, handle native input through `handle_event/3`, and\nreceive supervised application updates through `handle_info/2` when a caller\nuses `GPUI.Runtime.send_view/3`.","ref":"GPUI.View.html"},{"type":"type","title":"GPUI.View.callback_result/0","doc":"","ref":"GPUI.View.html#t:callback_result/0"},{"type":"type","title":"GPUI.View.window_event/0","doc":"","ref":"GPUI.View.html#t:window_event/0"},{"type":"callback","title":"GPUI.View.handle_event/3","doc":"","ref":"GPUI.View.html#c:handle_event/3"},{"type":"callback","title":"GPUI.View.handle_info/2","doc":"","ref":"GPUI.View.html#c:handle_info/2"},{"type":"callback","title":"GPUI.View.handle_window_event/3","doc":"","ref":"GPUI.View.html#c:handle_window_event/3"},{"type":"callback","title":"GPUI.View.render/1","doc":"","ref":"GPUI.View.html#c:render/1"},{"type":"module","title":"GPUI.Session","doc":"Renderer-independent state engine for one running `GPUI.Application`.\n\nA session owns declarative windows, root-view assigns, rendered snapshots,\nresources, strict event dispatch, and atomic callback transitions. It has no\nknowledge of native GPUI, displays, or network transports.\n\nOrdinary applications should use `GPUI.Runtime`, which composes this state\nengine with a display and synchronization lifecycle. Direct session use is an\nadvanced infrastructure boundary for remote hosting, custom runtimes, and\nrenderer-independent protocol integration.","ref":"GPUI.Session.html"},{"type":"type","title":"GPUI.Session.operation_error/0","doc":"","ref":"GPUI.Session.html#t:operation_error/0"},{"type":"type","title":"GPUI.Session.snapshot/0","doc":"","ref":"GPUI.Session.html#t:snapshot/0"},{"type":"type","title":"GPUI.Session.state/0","doc":"","ref":"GPUI.Session.html#t:state/0"},{"type":"type","title":"GPUI.Session.topology_error/0","doc":"","ref":"GPUI.Session.html#t:topology_error/0"},{"type":"function","title":"GPUI.Session.child_spec/1","doc":"Returns a specification to start this module under a supervisor.\n\nSee `Supervisor`.","ref":"GPUI.Session.html#child_spec/1"},{"type":"function","title":"GPUI.Session.close_window/2","doc":"Removes one declarative window by stable key or native session ID.","ref":"GPUI.Session.html#close_window/2"},{"type":"function","title":"GPUI.Session.dispatch_event/2","doc":"Validates and dispatches one display event, returning the handled fact and current snapshot.","ref":"GPUI.Session.html#dispatch_event/2"},{"type":"function","title":"GPUI.Session.dispatch_events/2","doc":"Validates and dispatches display events in order, returning handled facts and current snapshot.","ref":"GPUI.Session.html#dispatch_events/2"},{"type":"function","title":"GPUI.Session.drop_resource/2","doc":"Removes one renderer-independent resource from the next snapshot.","ref":"GPUI.Session.html#drop_resource/2"},{"type":"function","title":"GPUI.Session.open_window/2","doc":"Adds a keyed declarative window without remounting the application.","ref":"GPUI.Session.html#open_window/2"},{"type":"function","title":"GPUI.Session.put_resource/3","doc":"Stores one renderer-independent resource in the next snapshot.","ref":"GPUI.Session.html#put_resource/3"},{"type":"function","title":"GPUI.Session.refresh/1","doc":"Rerenders every window with its current module and assigns.","ref":"GPUI.Session.html#refresh/1"},{"type":"function","title":"GPUI.Session.send_view/3","doc":"Delivers an OTP message to a window's root view.","ref":"GPUI.Session.html#send_view/3"},{"type":"function","title":"GPUI.Session.snapshot/1","doc":"Returns the current authoritative renderer-independent snapshot.","ref":"GPUI.Session.html#snapshot/1"},{"type":"function","title":"GPUI.Session.start_link/1","doc":"Starts the renderer-independent state engine linked to the caller.","ref":"GPUI.Session.html#start_link/1"},{"type":"function","title":"GPUI.Session.windows/1","doc":"Returns the session's declarative windows.","ref":"GPUI.Session.html#windows/1"},{"type":"module","title":"GPUI.Text.BlockProjection","doc":"A non-editable block rendered adjacent to an explicit logical text line.\n\nBlocks are visual annotations only. They do not enter the Rope, create visual\nrows inside text layout, affect selections, or participate in history.","ref":"GPUI.Text.BlockProjection.html"},{"type":"type","title":"GPUI.Text.BlockProjection.placement/0","doc":"","ref":"GPUI.Text.BlockProjection.html#t:placement/0"},{"type":"type","title":"GPUI.Text.BlockProjection.rgb/0","doc":"","ref":"GPUI.Text.BlockProjection.html#t:rgb/0"},{"type":"type","title":"GPUI.Text.BlockProjection.t/0","doc":"","ref":"GPUI.Text.BlockProjection.html#t:t/0"},{"type":"function","title":"GPUI.Text.BlockProjection.new/3","doc":"Creates a bounded non-editable block adjacent to a logical text line.","ref":"GPUI.Text.BlockProjection.html#new/3"},{"type":"function","title":"GPUI.Text.BlockProjection.validate!/1","doc":"Validates a block projection's bounded text and presentation values.","ref":"GPUI.Text.BlockProjection.html#validate!/1"},{"type":"module","title":"GPUI.Text.Buffer","doc":"Persistent native Rope text storage with revisioned atomic edits.\n\n`GPUI.Text.Buffer` requires the `gpui_native` package because its Rope storage\nis a native resource. If that package is absent or disabled, operations return\n`{:error, :native_backend_unavailable}` rather than calling an unavailable\nmodule.\n\nThis is a model primitive. It does not represent a file, editor, language,\ngutter, or workspace. Consumers own those policies and may later attach one\nor more renderer primitives to the same buffer.","ref":"GPUI.Text.Buffer.html"},{"type":"type","title":"GPUI.Text.Buffer.result/1","doc":"","ref":"GPUI.Text.Buffer.html#t:result/1"},{"type":"opaque","title":"GPUI.Text.Buffer.t/0","doc":"","ref":"GPUI.Text.Buffer.html#t:t/0"},{"type":"function","title":"GPUI.Text.Buffer.new/2","doc":"Creates a native text buffer with one primary selection.","ref":"GPUI.Text.Buffer.html#new/2"},{"type":"function","title":"GPUI.Text.Buffer.redo/2","doc":"Redoes one native transaction and creates a new monotonic revision.","ref":"GPUI.Text.Buffer.html#redo/2"},{"type":"function","title":"GPUI.Text.Buffer.snapshot/1","doc":"Returns the current text, revision, selections, and history availability.","ref":"GPUI.Text.Buffer.html#snapshot/1"},{"type":"function","title":"GPUI.Text.Buffer.transact/2","doc":"Atomically applies a transaction based on the current revision.","ref":"GPUI.Text.Buffer.html#transact/2"},{"type":"function","title":"GPUI.Text.Buffer.undo/2","doc":"Undoes one native transaction and creates a new monotonic revision.","ref":"GPUI.Text.Buffer.html#undo/2"},{"type":"module","title":"GPUI.Text.CaretGeometry","doc":"Window-relative native pixel bounds for a text surface's primary caret.\n\nThe logical position uses zero-based UTF-16 line coordinates. Bounds are\nreported only while the caret is part of the currently laid-out viewport.","ref":"GPUI.Text.CaretGeometry.html"},{"type":"type","title":"GPUI.Text.CaretGeometry.t/0","doc":"","ref":"GPUI.Text.CaretGeometry.html#t:t/0"},{"type":"function","title":"GPUI.Text.CaretGeometry.from_event/1","doc":"Decodes a protocol caret-geometry map into a typed value.","ref":"GPUI.Text.CaretGeometry.html#from_event/1"},{"type":"module","title":"GPUI.Text.Decoration","doc":"A neutral visual annotation attached to a logical text range.\n\nDecorations carry rendering facts only. `background` and `underline` are\nsix-digit RGB integers; consumers retain diagnostic, language, severity, and\ncommand policy outside the renderer primitive.","ref":"GPUI.Text.Decoration.html"},{"type":"type","title":"GPUI.Text.Decoration.rgb/0","doc":"","ref":"GPUI.Text.Decoration.html#t:rgb/0"},{"type":"type","title":"GPUI.Text.Decoration.t/0","doc":"","ref":"GPUI.Text.Decoration.html#t:t/0"},{"type":"type","title":"GPUI.Text.Decoration.underline_style/0","doc":"","ref":"GPUI.Text.Decoration.html#t:underline_style/0"},{"type":"function","title":"GPUI.Text.Decoration.new/2","doc":"Creates a validated visual decoration for a logical text range.","ref":"GPUI.Text.Decoration.html#new/2"},{"type":"function","title":"GPUI.Text.Decoration.validate!/1","doc":"Validates a decoration's colors and underline style.","ref":"GPUI.Text.Decoration.html#validate!/1"},{"type":"module","title":"GPUI.Text.Edit","doc":"An atomic replacement of one half-open text range.","ref":"GPUI.Text.Edit.html"},{"type":"type","title":"GPUI.Text.Edit.t/0","doc":"","ref":"GPUI.Text.Edit.html#t:t/0"},{"type":"function","title":"GPUI.Text.Edit.insert/2","doc":"Creates an insertion edit at a logical position.","ref":"GPUI.Text.Edit.html#insert/2"},{"type":"function","title":"GPUI.Text.Edit.new/2","doc":"Creates an atomic replacement for a half-open range.","ref":"GPUI.Text.Edit.html#new/2"},{"type":"module","title":"GPUI.Text.InlineProjection","doc":"Non-editable text rendered at an explicit logical text position.\n\nInline projections are visual only. They do not enter the Rope, affect\nselection coordinates, participate in history, or imply completion policy.","ref":"GPUI.Text.InlineProjection.html"},{"type":"type","title":"GPUI.Text.InlineProjection.rgb/0","doc":"","ref":"GPUI.Text.InlineProjection.html#t:rgb/0"},{"type":"type","title":"GPUI.Text.InlineProjection.t/0","doc":"","ref":"GPUI.Text.InlineProjection.html#t:t/0"},{"type":"function","title":"GPUI.Text.InlineProjection.new/3","doc":"Creates a bounded inline projection at a logical position.","ref":"GPUI.Text.InlineProjection.html#new/3"},{"type":"function","title":"GPUI.Text.InlineProjection.validate!/1","doc":"Validates an inline projection's bounded text and color.","ref":"GPUI.Text.InlineProjection.html#validate!/1"},{"type":"module","title":"GPUI.Text.Position","doc":"A zero-based logical text position.\n\n`utf16_offset` counts UTF-16 code units from the beginning of `line`. It is\ndeliberately explicit so positions can be exchanged with language servers\nwithout treating byte, code-point, and UTF-16 offsets as interchangeable.","ref":"GPUI.Text.Position.html"},{"type":"type","title":"GPUI.Text.Position.t/0","doc":"","ref":"GPUI.Text.Position.html#t:t/0"},{"type":"function","title":"GPUI.Text.Position.new/2","doc":"","ref":"GPUI.Text.Position.html#new/2"},{"type":"module","title":"GPUI.Text.Range","doc":"A half-open range between two `GPUI.Text.Position` values.","ref":"GPUI.Text.Range.html"},{"type":"type","title":"GPUI.Text.Range.t/0","doc":"","ref":"GPUI.Text.Range.html#t:t/0"},{"type":"function","title":"GPUI.Text.Range.caret/1","doc":"Creates a collapsed range at a logical position.","ref":"GPUI.Text.Range.html#caret/1"},{"type":"function","title":"GPUI.Text.Range.new/2","doc":"Creates a half-open range between two logical positions.","ref":"GPUI.Text.Range.html#new/2"},{"type":"module","title":"GPUI.Text.RangeGeometry","doc":"Window-relative native pixel bounds for one requested logical text range.\n\nRange geometry is bounded to at most 64 requests per surface and includes\nonly ranges present in the current native layout. Each result contains a\nbounded rectangle for every visual row crossed by wrapped text.","ref":"GPUI.Text.RangeGeometry.html"},{"type":"type","title":"GPUI.Text.RangeGeometry.t/0","doc":"","ref":"GPUI.Text.RangeGeometry.html#t:t/0"},{"type":"function","title":"GPUI.Text.RangeGeometry.from_event/1","doc":"Decodes a protocol range-geometry map into a typed value.","ref":"GPUI.Text.RangeGeometry.html#from_event/1"},{"type":"module","title":"GPUI.Text.Rectangle","doc":"Window-relative native pixel rectangle for laid-out text.","ref":"GPUI.Text.Rectangle.html"},{"type":"type","title":"GPUI.Text.Rectangle.t/0","doc":"","ref":"GPUI.Text.Rectangle.html#t:t/0"},{"type":"function","title":"GPUI.Text.Rectangle.from_event/1","doc":"Decodes a protocol rectangle map into a typed rectangle value.","ref":"GPUI.Text.Rectangle.html#from_event/1"},{"type":"module","title":"GPUI.Text.Selection","doc":"A directed selection represented by anchor and head positions.","ref":"GPUI.Text.Selection.html"},{"type":"type","title":"GPUI.Text.Selection.t/0","doc":"","ref":"GPUI.Text.Selection.html#t:t/0"},{"type":"function","title":"GPUI.Text.Selection.caret/3","doc":"Creates a collapsed selection at a logical position.","ref":"GPUI.Text.Selection.html#caret/3"},{"type":"function","title":"GPUI.Text.Selection.new/4","doc":"Creates a directed selection from anchor to head.","ref":"GPUI.Text.Selection.html#new/4"},{"type":"module","title":"GPUI.Text.Snapshot","doc":"An immutable snapshot of a persistent native text buffer.","ref":"GPUI.Text.Snapshot.html"},{"type":"type","title":"GPUI.Text.Snapshot.t/0","doc":"","ref":"GPUI.Text.Snapshot.html#t:t/0"},{"type":"module","title":"GPUI.Text.Transaction","doc":"A revisioned atomic set of text edits and resulting selections.\n\nUse `new/2` with a `GPUI.Text.Snapshot` when preparing an external edit. The\nconstructor copies the snapshot revision and current selections, so callers\nspecify only what changes. Pass `:selections` when the transaction should\nproduce a different complete selection set.","ref":"GPUI.Text.Transaction.html"},{"type":"type","title":"GPUI.Text.Transaction.origin/0","doc":"","ref":"GPUI.Text.Transaction.html#t:origin/0"},{"type":"type","title":"GPUI.Text.Transaction.t/0","doc":"","ref":"GPUI.Text.Transaction.html#t:t/0"},{"type":"function","title":"GPUI.Text.Transaction.new/2","doc":"Builds a validated transaction against the supplied text snapshot.","ref":"GPUI.Text.Transaction.html#new/2"},{"type":"function","title":"GPUI.Text.Transaction.validate!/1","doc":"Validates a transaction's public shape and returns it.","ref":"GPUI.Text.Transaction.html#validate!/1"},{"type":"module","title":"GPUI.Text.Transaction.Result","doc":"Result of applying a `GPUI.Text.Transaction` to a persistent text buffer.\n\n`duplicate` is true when an identical transaction ID and payload was already\napplied. The resulting selections are decoded public `GPUI.Text.Selection`\nvalues.","ref":"GPUI.Text.Transaction.Result.html"},{"type":"type","title":"GPUI.Text.Transaction.Result.t/0","doc":"","ref":"GPUI.Text.Transaction.Result.html#t:t/0"},{"type":"module","title":"GPUI.Text.Viewport","doc":"A revision-tagged snapshot of one text surface's visible visual rows.\n\nScroll coordinates are native GPUI pixels relative to the surface's laid-out\ntext viewport. Offsets are typically zero or negative as content moves above\nor to the left of the viewport.","ref":"GPUI.Text.Viewport.html"},{"type":"type","title":"GPUI.Text.Viewport.t/0","doc":"","ref":"GPUI.Text.Viewport.html#t:t/0"},{"type":"function","title":"GPUI.Text.Viewport.from_event/1","doc":"Decodes a protocol text-viewport map into a typed viewport value.","ref":"GPUI.Text.Viewport.html#from_event/1"},{"type":"behaviour","title":"GPUI.Display","doc":"Behaviour for displays that present GPUI snapshots and return input events.\n\nDisplays own renderer-specific lifecycle and resources while application\nsessions remain renderer-independent. Implementations provide process startup,\nsnapshot synchronization, event draining, and deterministic event injection.\nFrame barriers and presentation capabilities are optional.\n\nFramework runtimes invoke this contract through an internal support layer that\nvalidates callback results and turns callback failures into structured errors.","ref":"GPUI.Display.html"},{"type":"type","title":"GPUI.Display.event/0","doc":"","ref":"GPUI.Display.html#t:event/0"},{"type":"type","title":"GPUI.Display.snapshot/0","doc":"","ref":"GPUI.Display.html#t:snapshot/0"},{"type":"callback","title":"GPUI.Display.await_frame/3","doc":"","ref":"GPUI.Display.html#c:await_frame/3"},{"type":"callback","title":"GPUI.Display.await_frame_after/4","doc":"","ref":"GPUI.Display.html#c:await_frame_after/4"},{"type":"callback","title":"GPUI.Display.drain_events/1","doc":"","ref":"GPUI.Display.html#c:drain_events/1"},{"type":"callback","title":"GPUI.Display.frame_token/2","doc":"","ref":"GPUI.Display.html#c:frame_token/2"},{"type":"callback","title":"GPUI.Display.inject_event/2","doc":"","ref":"GPUI.Display.html#c:inject_event/2"},{"type":"callback","title":"GPUI.Display.presentation_capabilities/1","doc":"","ref":"GPUI.Display.html#c:presentation_capabilities/1"},{"type":"callback","title":"GPUI.Display.start_link/1","doc":"","ref":"GPUI.Display.html#c:start_link/1"},{"type":"callback","title":"GPUI.Display.sync/2","doc":"","ref":"GPUI.Display.html#c:sync/2"},{"type":"module","title":"GPUI.Element","doc":"Serializable element tree produced by `GPUI.View` modules.","ref":"GPUI.Element.html"},{"type":"type","title":"GPUI.Element.child/0","doc":"","ref":"GPUI.Element.html#t:child/0"},{"type":"type","title":"GPUI.Element.primitive/0","doc":"","ref":"GPUI.Element.html#t:primitive/0"},{"type":"type","title":"GPUI.Element.t/0","doc":"","ref":"GPUI.Element.html#t:t/0"},{"type":"function","title":"GPUI.Element.append_child/2","doc":"Appends one child to an immutable element value.","ref":"GPUI.Element.html#append_child/2"},{"type":"function","title":"GPUI.Element.put_style/3","doc":"Adds or replaces one explicit style value on an immutable element.","ref":"GPUI.Element.html#put_style/3"},{"type":"function","title":"GPUI.Element.to_payload/1","doc":"Converts an element tree into plain serializable maps/lists.","ref":"GPUI.Element.html#to_payload/1"},{"type":"module","title":"GPUI.Event","doc":"Normalized UI event delivered from a display into `GPUI.Session`.","ref":"GPUI.Event.html"},{"type":"type","title":"GPUI.Event.payload/0","doc":"","ref":"GPUI.Event.html#t:payload/0"},{"type":"type","title":"GPUI.Event.t/0","doc":"","ref":"GPUI.Event.html#t:t/0"},{"type":"type","title":"GPUI.Event.type/0","doc":"","ref":"GPUI.Event.html#t:type/0"},{"type":"function","title":"GPUI.Event.injectable_types/0","doc":"Returns event types accepted by the low-level native injection boundary.","ref":"GPUI.Event.html#injectable_types/0"},{"type":"macro","title":"GPUI.Event.is_routed_type/1","doc":"","ref":"GPUI.Event.html#is_routed_type/1"},{"type":"function","title":"GPUI.Event.normalize/1","doc":"","ref":"GPUI.Event.html#normalize/1"},{"type":"function","title":"GPUI.Event.routed_types/0","doc":"Returns the renderer-independent event types routed to root view callbacks.","ref":"GPUI.Event.html#routed_types/0"},{"type":"function","title":"GPUI.Event.to_map/1","doc":"Converts an event struct and its extra attributes into a plain event map.","ref":"GPUI.Event.html#to_map/1"},{"type":"module","title":"GPUI.Raster","doc":"Generic packed 32-bit CPU raster image payload for GPUI image elements.\n\nThis is renderer-independent: image decoders, screenshots, video frames, or\ntensors can produce `%GPUI.Raster{}` values. Rows may be tightly packed or\nuse an explicit byte stride.","ref":"GPUI.Raster.html"},{"type":"type","title":"GPUI.Raster.format/0","doc":"","ref":"GPUI.Raster.html#t:format/0"},{"type":"type","title":"GPUI.Raster.t/0","doc":"","ref":"GPUI.Raster.html#t:t/0"},{"type":"function","title":"GPUI.Raster.new/4","doc":"","ref":"GPUI.Raster.html#new/4"},{"type":"function","title":"GPUI.Raster.to_payload/1","doc":"Converts a validated raster into its renderer-independent payload.","ref":"GPUI.Raster.html#to_payload/1"},{"type":"function","title":"GPUI.Raster.validate!/1","doc":"","ref":"GPUI.Raster.html#validate!/1"},{"type":"module","title":"GPUI.ResourceRef","doc":"Reference to a remote/display resource such as a raster image.","ref":"GPUI.ResourceRef.html"},{"type":"type","title":"GPUI.ResourceRef.t/0","doc":"","ref":"GPUI.ResourceRef.html#t:t/0"},{"type":"function","title":"GPUI.ResourceRef.new/2","doc":"","ref":"GPUI.ResourceRef.html#new/2"},{"type":"function","title":"GPUI.ResourceRef.to_payload/1","doc":"Converts a resource reference into its renderer-independent payload.","ref":"GPUI.ResourceRef.html#to_payload/1"},{"type":"module","title":"GPUI.Schema.Extension","doc":"Compile-time metadata for one versioned renderer presentation contract.","ref":"GPUI.Schema.Extension.html"},{"type":"type","title":"GPUI.Schema.Extension.t/0","doc":"","ref":"GPUI.Schema.Extension.html#t:t/0"},{"type":"function","title":"GPUI.Schema.Extension.max_capabilities/0","doc":"Maximum capabilities declared or advertised for one extension version.","ref":"GPUI.Schema.Extension.html#max_capabilities/0"},{"type":"module","title":"GPUI.Schema.Extension.Support","doc":"Bounded display support for one exact presentation contract version.","ref":"GPUI.Schema.Extension.Support.html"},{"type":"type","title":"GPUI.Schema.Extension.Support.t/0","doc":"","ref":"GPUI.Schema.Extension.Support.html#t:t/0"},{"type":"function","title":"GPUI.Schema.Extension.Support.max_contracts/0","doc":"Maximum extension contracts advertised by one display or remote peer.","ref":"GPUI.Schema.Extension.Support.html#max_contracts/0"},{"type":"function","title":"GPUI.Schema.Extension.Support.new/3","doc":"Builds and validates support against the canonical schema contract.","ref":"GPUI.Schema.Extension.Support.html#new/3"},{"type":"function","title":"GPUI.Schema.Extension.Support.provides?/4","doc":"Tests whether support provides one exact contract version and optional capability.","ref":"GPUI.Schema.Extension.Support.html#provides?/4"},{"type":"module","title":"GPUI.Tailwind","doc":"Small Tailwind-compatible class normalizer for GPUI element styles.\n\nThis intentionally starts with a constrained subset that maps cleanly to GPUI.\nUnknown classes are preserved under `:class` by callers and handled according\nto the `:unknown_classes` application setting: `:warn` (the development\ndefault), `:error`, or `:keep`.","ref":"GPUI.Tailwind.html"},{"type":"type","title":"GPUI.Tailwind.result/0","doc":"","ref":"GPUI.Tailwind.html#t:result/0"},{"type":"function","title":"GPUI.Tailwind.handle_unknown!/1","doc":"Applies the configured warning or error policy to unsupported classes.","ref":"GPUI.Tailwind.html#handle_unknown!/1"},{"type":"function","title":"GPUI.Tailwind.normalize/1","doc":"","ref":"GPUI.Tailwind.html#normalize/1"},{"type":"module","title":"GPUI.Template","doc":"HEEx-style template support for GPUI.\n\nThis module intentionally reuses Phoenix LiveView's tag parser for the\nHEEx-compatible tokenizer/tree builder, then compiles the parsed tree into\n`%GPUI.Element{}` values instead of `%Phoenix.LiveView.Rendered{}`.\n\nLowercase tags are renderer primitives. Native components use `GPUI.UI` or\n`GPUI.UI.Overlay`; their internal `ui_*` element tags are intentionally not a\npublic template surface.","ref":"GPUI.Template.html"},{"type":"function","title":"GPUI.Template.compile/3","doc":"","ref":"GPUI.Template.html#compile/3"},{"type":"macro","title":"GPUI.Template.sigil_GPUI/2","doc":"Compiles a HEEx-style GPUI template into an element tree.","ref":"GPUI.Template.html#sigil_GPUI/2"},{"type":"module","title":"GPUI.WindowSpec","doc":"Declarative window specification returned by the application DSL.\n\nEach window owns a bounded set of platform-aware `GPUI.Command` bindings in\naddition to its title, size, and root view.","ref":"GPUI.WindowSpec.html"},{"type":"type","title":"GPUI.WindowSpec.chrome/0","doc":"","ref":"GPUI.WindowSpec.html#t:chrome/0"},{"type":"type","title":"GPUI.WindowSpec.key/0","doc":"","ref":"GPUI.WindowSpec.html#t:key/0"},{"type":"type","title":"GPUI.WindowSpec.root/0","doc":"","ref":"GPUI.WindowSpec.html#t:root/0"},{"type":"type","title":"GPUI.WindowSpec.t/0","doc":"","ref":"GPUI.WindowSpec.html#t:t/0"},{"type":"function","title":"GPUI.WindowSpec.new/2","doc":"Builds and validates a declarative window specification.","ref":"GPUI.WindowSpec.html#new/2"},{"type":"function","title":"GPUI.WindowSpec.validate!/1","doc":"Validates a declarative window specification and returns it.","ref":"GPUI.WindowSpec.html#validate!/1"},{"type":"module","title":"GPUI.Remote.Client","doc":"Display-side client for a remote GPUI application session.\n\nThe client owns a local display, synchronizes remote snapshots into it, and\nforwards local display events to `GPUI.Remote.Server`.","ref":"GPUI.Remote.Client.html"},{"type":"module","title":"Options - GPUI.Remote.Client","doc":"* `:host` and `:port` - required remote endpoint;\n  * `:ssl` - `false` or SafeRPC TLS options;\n  * `:display` - local display module, defaulting to `GPUI.Display.Native`;\n  * `:display_opts` - options passed to the display;\n  * `:session_id` - stable session identity, generated when omitted;\n  * `:poll_interval` - positive milliseconds or `nil` to disable display\n    polling, defaulting to `16`;\n  * `:name` - optional client process name.","ref":"GPUI.Remote.Client.html#module-options"},{"type":"function","title":"GPUI.Remote.Client.await_frame/3","doc":"Waits until a complete display frame follows the current window state.","ref":"GPUI.Remote.Client.html#await_frame/3"},{"type":"function","title":"GPUI.Remote.Client.await_frame_after/4","doc":"Waits for a display frame completed after the supplied generation.","ref":"GPUI.Remote.Client.html#await_frame_after/4"},{"type":"function","title":"GPUI.Remote.Client.child_spec/1","doc":"Builds the remote client's OTP child specification.","ref":"GPUI.Remote.Client.html#child_spec/1"},{"type":"function","title":"GPUI.Remote.Client.event/2","doc":"Dispatches one normalized event remotely and synchronizes its snapshot.","ref":"GPUI.Remote.Client.html#event/2"},{"type":"function","title":"GPUI.Remote.Client.frame_token/2","doc":"Returns the latest completed display frame generation for a window.","ref":"GPUI.Remote.Client.html#frame_token/2"},{"type":"function","title":"GPUI.Remote.Client.mount/2","doc":"Mounts or remounts the configured application session and synchronizes its snapshot.","ref":"GPUI.Remote.Client.html#mount/2"},{"type":"function","title":"GPUI.Remote.Client.snapshot/1","doc":"Fetches and synchronizes the current remote session snapshot.","ref":"GPUI.Remote.Client.html#snapshot/1"},{"type":"function","title":"GPUI.Remote.Client.start_link/1","doc":"Starts a remote display client linked to the caller.","ref":"GPUI.Remote.Client.html#start_link/1"},{"type":"function","title":"GPUI.Remote.Client.subscribe/1","doc":"Subscribes the calling process to synchronized remote display updates.","ref":"GPUI.Remote.Client.html#subscribe/1"},{"type":"function","title":"GPUI.Remote.Client.unsubscribe/1","doc":"Unsubscribes the calling process from remote display updates.","ref":"GPUI.Remote.Client.html#unsubscribe/1"},{"type":"module","title":"GPUI.Remote.Server","doc":"SafeRPC endpoint for renderer-independent GPUI application sessions.\n\nEvery remote session owns a distinct `GPUI.Session`. The server never starts a\nnative display; rendering happens only on the connected display client.","ref":"GPUI.Remote.Server.html"},{"type":"module","title":"Options - GPUI.Remote.Server","doc":"* `:app` - required `GPUI.Application` module;\n  * `:args` - default mount argument used when a client omits one;\n  * `:port` - listening port, defaulting to `0` for an OS-assigned port;\n  * `:ssl` - `false` or TLS listener options;\n  * `:session_ttl` - inactivity timeout in milliseconds or `:infinity`,\n    defaulting to 30 minutes;\n  * `:max_in_flight_requests_per_connection` - positive limit up to 4,096,\n    defaulting to 64;\n  * `:max_in_flight_requests_per_session` - positive limit up to 4,096,\n    defaulting to 16;\n  * `:name` - optional supervisor name.","ref":"GPUI.Remote.Server.html#module-options"},{"type":"function","title":"GPUI.Remote.Server.child_spec/1","doc":"Builds the remote server supervision-tree child specification.","ref":"GPUI.Remote.Server.html#child_spec/1"},{"type":"function","title":"GPUI.Remote.Server.coordinator/1","doc":"Finds the coordinator process belonging to a remote server tree.","ref":"GPUI.Remote.Server.html#coordinator/1"},{"type":"function","title":"GPUI.Remote.Server.port/1","doc":"Returns the server's effective listening port.","ref":"GPUI.Remote.Server.html#port/1"},{"type":"function","title":"GPUI.Remote.Server.start_coordinator/1","doc":"Starts the internal remote-server coordinator process.","ref":"GPUI.Remote.Server.html#start_coordinator/1"},{"type":"function","title":"GPUI.Remote.Server.start_link/1","doc":"Starts the remote server supervision tree linked to the caller.","ref":"GPUI.Remote.Server.html#start_link/1"},{"type":"module","title":"GPUI.Test","doc":"ExUnit helpers for renderer-independent application tests and deterministic\nGPUI interaction tests.\n\nRenderer-independent tests use a lightweight `GPUI.Test.Display`:\n\n    use GPUI.Test, async: true\n\nDeterministic tests that need GPUI layout, focus, hit testing, or keyboard\ndispatch opt in to a supervised native UI:\n\n    use GPUI.Test, native: [size: {640, 480}]\n\nNative UI cases are skipped by ordinary ExUnit runs. Use the dedicated\ndeterministic native-test Mix task so the package is compiled for the\n`native_test` target.\n\nBoth modes import one concise helper vocabulary; the first argument identifies\nwhether an interaction targets a runtime or an interactive UI.\n\n    defmodule FocusTimerTest do\n      use GPUI.Test, async: true\n\n      test \"advances from an OTP message\" do\n        runtime = start_runtime!(FocusTimerApp, args: %{seconds: 2})\n\n        click(runtime, \"start\")\n        send_view(runtime, :tick)\n        assert %{remaining: 1, status: :running} = assigns(runtime)\n      end\n    end\n\nEvents are dispatched through `GPUI.Test.Display`, so the same\n`GPUI.Runtime` polling boundary used by a real display is exercised without\nloading GPUI or requiring desktop libraries.","ref":"GPUI.Test.html"},{"type":"function","title":"GPUI.Test.__setup_native__/2","doc":"Initializes the supervised native UI handle used by `use GPUI.Test, native: ...`.","ref":"GPUI.Test.html#__setup_native__/2"},{"type":"function","title":"GPUI.Test.advance/2","doc":"Advances GPUI's deterministic clock by the given milliseconds.","ref":"GPUI.Test.html#advance/2"},{"type":"function","title":"GPUI.Test.assigns/2","doc":"Returns root-view assigns for a selected window.","ref":"GPUI.Test.html#assigns/2"},{"type":"function","title":"GPUI.Test.bounds/2","doc":"Returns the rendered bounds for a stable element ID.","ref":"GPUI.Test.html#bounds/2"},{"type":"function","title":"GPUI.Test.change/4","doc":"Dispatches a change event and returns the updated snapshot.","ref":"GPUI.Test.html#change/4"},{"type":"function","title":"GPUI.Test.click/2","doc":"Clicks a stable element ID or logical point in an interactive deterministic UI.","ref":"GPUI.Test.html#click/2"},{"type":"function","title":"GPUI.Test.click/3","doc":"Dispatches a click event and returns the updated snapshot.","ref":"GPUI.Test.html#click/3"},{"type":"function","title":"GPUI.Test.command/3","doc":"Dispatches an application command and returns the updated snapshot.","ref":"GPUI.Test.html#command/3"},{"type":"function","title":"GPUI.Test.copy_selected_line/3","doc":"Acknowledges deterministic copying of a selected code-viewer line.","ref":"GPUI.Test.html#copy_selected_line/3"},{"type":"function","title":"GPUI.Test.dispatch/2","doc":"Dispatches a normalized display event and returns handled events plus the new snapshot.","ref":"GPUI.Test.html#dispatch/2"},{"type":"function","title":"GPUI.Test.drag/3","doc":"Simulates a left-button drag between logical points and settles native work.","ref":"GPUI.Test.html#drag/3"},{"type":"function","title":"GPUI.Test.file_cancel/3","doc":"Cancels a deterministic display-side button file read.","ref":"GPUI.Test.html#file_cancel/3"},{"type":"function","title":"GPUI.Test.file_select/5","doc":"Selects deterministic file bytes for a display-side button file read.","ref":"GPUI.Test.html#file_select/5"},{"type":"function","title":"GPUI.Test.focus/2","doc":"Moves native keyboard focus to a stable element ID.","ref":"GPUI.Test.html#focus/2"},{"type":"function","title":"GPUI.Test.press/2","doc":"Presses a semantic key in an interactive deterministic UI.","ref":"GPUI.Test.html#press/2"},{"type":"function","title":"GPUI.Test.range/5","doc":"Delivers a deterministic source-backed virtual-list range request.","ref":"GPUI.Test.html#range/5"},{"type":"function","title":"GPUI.Test.release/4","doc":"Dispatches a slider release event and returns the updated snapshot.","ref":"GPUI.Test.html#release/4"},{"type":"function","title":"GPUI.Test.render/2","doc":"Renders a view directly with map or keyword assigns.","ref":"GPUI.Test.html#render/2"},{"type":"function","title":"GPUI.Test.render/3","doc":"Renders a view into an interactive deterministic native UI.","ref":"GPUI.Test.html#render/3"},{"type":"function","title":"GPUI.Test.resize/2","doc":"Resizes the deterministic native viewport.","ref":"GPUI.Test.html#resize/2"},{"type":"function","title":"GPUI.Test.scroll/3","doc":"Scrolls a stable target by a bounded logical-pixel delta.","ref":"GPUI.Test.html#scroll/3"},{"type":"function","title":"GPUI.Test.search/4","doc":"Dispatches a combobox search event and returns the updated snapshot.","ref":"GPUI.Test.html#search/4"},{"type":"function","title":"GPUI.Test.select/4","doc":"Selects a controlled form value and returns the updated snapshot.","ref":"GPUI.Test.html#select/4"},{"type":"function","title":"GPUI.Test.send_view/3","doc":"Delivers an OTP message to a root view and returns the updated snapshot.","ref":"GPUI.Test.html#send_view/3"},{"type":"function","title":"GPUI.Test.settle/1","doc":"Runs native UI work until GPUI is parked.","ref":"GPUI.Test.html#settle/1"},{"type":"function","title":"GPUI.Test.snapshot/1","doc":"Returns the runtime snapshot, or passes an existing snapshot through. Raises on render failure.","ref":"GPUI.Test.html#snapshot/1"},{"type":"function","title":"GPUI.Test.start_runtime!/2","doc":"Starts a supervised runtime backed by `GPUI.Test.Display`.","ref":"GPUI.Test.html#start_runtime!/2"},{"type":"function","title":"GPUI.Test.submit/4","doc":"Dispatches an input submission with its current string value.","ref":"GPUI.Test.html#submit/4"},{"type":"function","title":"GPUI.Test.table_cell_select/5","doc":"Dispatches a deterministic data-table cell selection.","ref":"GPUI.Test.html#table_cell_select/5"},{"type":"function","title":"GPUI.Test.table_sort/4","doc":"Dispatches a deterministic sortable data-table header selection.","ref":"GPUI.Test.html#table_sort/4"},{"type":"function","title":"GPUI.Test.tree/2","doc":"Returns the rendered tree for a selected window.","ref":"GPUI.Test.html#tree/2"},{"type":"function","title":"GPUI.Test.type/2","doc":"Types text into the focused native input.","ref":"GPUI.Test.html#type/2"},{"type":"function","title":"GPUI.Test.window_snapshot/2","doc":"Returns a window snapshot from a runtime or full snapshot.","ref":"GPUI.Test.html#window_snapshot/2"},{"type":"module","title":"GPUI.Test.Display","doc":"Deterministic in-memory display for session and runtime tests.\n\nThe display records synchronized snapshots and queues injected events without\nstarting the native renderer. Pass `owner: self()` to receive each sync as\n`{:gpui_snapshot, snapshot}`.","ref":"GPUI.Test.Display.html"},{"type":"type","title":"GPUI.Test.Display.state/0","doc":"","ref":"GPUI.Test.Display.html#t:state/0"},{"type":"function","title":"GPUI.Test.Display.child_spec/1","doc":"Returns a specification to start this module under a supervisor.\n\nSee `Supervisor`.","ref":"GPUI.Test.Display.html#child_spec/1"},{"type":"function","title":"GPUI.Test.Display.latest_snapshot/1","doc":"Returns the most recently synchronized snapshot.","ref":"GPUI.Test.Display.html#latest_snapshot/1"},{"type":"function","title":"GPUI.Test.Display.snapshots/1","doc":"Returns synchronized snapshots in chronological order.","ref":"GPUI.Test.Display.html#snapshots/1"},{"type":"module","title":"GPUI.Test.UI","doc":"An opaque interactive UI handle supplied by `use GPUI.Test, native: ...`.\n\nPass this value to the interaction helpers imported by `GPUI.Test`.","ref":"GPUI.Test.UI.html"},{"type":"type","title":"GPUI.Test.UI.t/0","doc":"","ref":"GPUI.Test.UI.html#t:t/0"},{"type":"extras","title":"Changelog","doc":"# Changelog","ref":"changelog.html"},{"type":"extras","title":"Unreleased - Changelog","doc":"","ref":"changelog.html#unreleased"},{"type":"extras","title":"0.2.0-rc.1 - 2026-09-02 - Changelog","doc":"","ref":"changelog.html#0-2-0-rc-1-2026-09-02"},{"type":"extras","title":"Breaking changes - Changelog","doc":"- Moved conventional controls and native loading into the separately installed\n  `gpui_components` and `gpui_native` packages.\n- `GPUI.Schema.registry/0` now contains only neutral core declarations;\n  additional native declarations compose explicitly through immutable schema\n  providers.\n- Raw `ui_*` tags remain internal template syntax and must be authored through\n  their public component builders.","ref":"changelog.html#breaking-changes"},{"type":"extras","title":"Added - Changelog","doc":"- Added first-class application identity and typed icon metadata with\n  deterministic process-wide conflict handling.\n- Added bounded runtime information, rendered-tree formatting, and shared\n  `GPUI.Tree` query functions for debugging and tests.\n- Added focused examples for declarative events, supervised updates, controlled\n  forms, multiple windows, BEAM inspection, image analysis, and supervised\n  pipelines.","ref":"changelog.html#added"},{"type":"extras","title":"Changed - Changelog","doc":"- Consolidated renderer-independent application, session, snapshot, text,\n  transfer, resource, accessibility, and style contracts in `gpui`.\n- Reworked examples around distinct Elixir and OTP behavior and enforced\n  readable explicit text contrast.","ref":"changelog.html#changed"},{"type":"extras","title":"0.1.1 - Changelog","doc":"- Corrected native dialog and tabs E2E assertions to prove focus through keyboard activation, moved tab key handling to the composite focus owner, and fixed the programmatic split event spelling.\n- Added native tab-list semantics with roving focus, wrapped arrow navigation, Home/End, Enter/Space, accessible Click actions, visible keyboard focus, and disabled interaction exclusion through the controlled change path.\n- Added native dialog E2E scenarios for forward and reverse focus containment, Escape policy, controlled closure, and keyboard reopening through the restored trigger.\n- Extended release validation with a clean renderer-independent consumer that compiles and runs `GPUI.Test` without building or loading the native NIF.\n- Hardened release security validation to fail Rust unsoundness advisories and updated the pinned native dependency graph past the affected `event-listener` release.\n- Replaced premature release-compatibility policy with explicit private, unreleased development boundaries while retaining exact current-process protocol validation.\n- Added a neutral controlled two-pane native resizable split with bounded sizes, monotonic resize requests, and consumer-owned persistence.\n- Added idiomatic optional `handle_window_event/3` callbacks with `{:close, assigns}` approval, plus declarative native window minimum size and resizability.\n- Added stable-ID monotonic focus requests and native focus/blur events for buttons, low-level inputs, controlled inputs, and text surfaces.\n- Added opt-in, stable-ID, deduplicated native element-bounds events for composing ordinary HTML/Tailwind-like layout with window-fitted layers.\n- Added a neutral bounded ` ` primitive backed by GPUI anchored positioning and deferred paint priority, documented as a native top-layer escape hatch while ordinary positioning and static presentation remain HTML/Tailwind-like.\n- Added GPUI-native relative and absolute positioning with typed inset utilities, including axis, percentage, fractional, arbitrary-pixel, and auto values.\n- Added bounded non-editable block projections anchored before or after logical text lines without changing document coordinates.\n- Added bounded non-editable inline text projections anchored to explicit UTF-16 positions without completion policy or Rope mutation.\n- Hardened controlled dialogs with Escape policy requiring both keyboard and closable flags, native focus trapping, initial content focus, and safe trigger restoration on every close path.\n- Added controlled disabled semantics for generic accessible controls, removing pointer, keyboard, Tab, and AccessKit activation while reporting AccessKit disabled state.\n- Added generated accessibility interaction classes and native Tab/Enter/Space activation for simple generic controls, including visible keyboard focus.\n- Routed generic accessible Click actions through the same normalized native event helper as pointer activation, with no accessibility-specific Elixir event path.\n- Generated native accessibility enums, the grouped semantics struct, decoders, and GPUI conversion matches from the authoritative Elixir accessibility specification.\n- Added role-constrained neutral accessibility values, selected/expanded/checked states, and orientation with native AccessKit and remote state-transition coverage.\n- Centralized generic accessibility vocabulary, identity, and name/description validation in `GPUI.Accessibility` ahead of role-constrained state attributes.\n- Added bounded renderer-independent accessibility roles, labels, and descriptions for identified generic elements, including native AccessKit mapping and remote snapshot coverage.\n- Documented the pinned GPUI/AccessKit native accessibility boundary, existing component mappings, truthful schema direction, and unsupported claims.\n- Made development reload failures observable and recoverable, with pre-compilation syntax validation and atomic multi-window refresh publishing.\n- Preserved dynamic multi-window topology, keys, IDs, per-window assigns, and monotonic allocation across source reload; added end-to-end remote client display reconciliation coverage.\n- Hardened remote keyed multi-window snapshots with resume coverage, negotiated `:window_topology_v1`, and bounded window metadata and counts.\n- Added Phoenix/OTP-style typed view outcomes for opening and closing keyed windows from `handle_event/3` and `handle_info/2`.\n- Added local dynamic multi-window topology with stable application keys, monotonic session IDs, and declarative display reconciliation.\n- Added neutral bounded text style runs whose foreground color, weight, and style participate directly in native shaping.\n- Added explicit solid, dashed, and wavy underline variants to neutral text decorations.\n- Added neutral bounded text decorations with range-attached RGB backgrounds and underlines, without diagnostic or language policy.\n- Expanded requested range geometry into bounded per-visual-row rectangles for wrapped text.\n- Replaced the editable-text demonstration's fixed gutter with consumer-owned line numbers driven by native visible rows, line height, and scroll offset.\n- Added declarative scroll-to-position request tokens and asynchronous native point-to-position hit-test events for text surfaces.\n- Added bounded declarative range-geometry requests for text surfaces, limited to 64 ranges and emitted only for currently laid-out content.\n- Added revision-tagged, deduplicated text-surface viewport and primary-caret geometry events with public coordinate-space types.\n- Added native E2E coverage for two editable surfaces sharing one persistent buffer across local typing, external edits, undo, redo, and reconciliation without external-event echo.\n- Added the neutral ` ` renderer primitive, backed by `GPUI.Text.Buffer`, with native immediate input/IME behavior, focus-request tokens, external revision reconciliation, minimal local text transactions, plural selection events, and selection-only revisions that do not pollute document undo history.\n- Added generic persistent native Rope text buffers with explicit UTF-16 positions, revisioned atomic transactions, plural selections, idempotent delivery, stale-revision protection, and monotonic undo/redo, without introducing an opinionated editor component.\n\n- Added GPUI-native flex-basis, overflow clipping, whitespace, text alignment and ellipsis/truncation, and cursor utilities to the Tailwind-compatible style protocol.\n- Normalized Tailwind-compatible classes on programmatically constructed `GPUI.UI` component trees before serialization, matching template behavior, and fixed `mix gpui.dev --no-compile` native availability detection.\n\n- Expanded the Tailwind-compatible normalizer with flex shorthands, decimal spacing, fractional and percentage dimensions, safe arbitrary pixel/color/opacity values, exact arbitrary radii, and exact preservation of unsupported classes; replaced static minimum-height styles in the showcase applications with `min-h-0` utilities.\n\n- Added bounded renderer-independent window commands with cross-platform modified shortcuts, native post-dispatch matching that preserves focused input behavior, local/remote event routing, deterministic command tests, and command-driven Git, process, and log/trace dogfood workflows.\n- Added renderer-independent controlled field composition, visible help/error feedback, input Enter submission, monotonic native focus requests, deterministic submission testing, and validation-driven Settings Form and external-consumer examples.\n- Required semantic names for buttons, checkboxes, text inputs, selectors, comboboxes, popover/menu triggers, and dialogs; propagated controlled values, placeholders, password roles, selected labels, and expanded overlay state into native accessibility metadata.\n- Added schema-derived ExDoc option tables and precise named option-map types for every public UI and overlay builder, including required events, defaults, enums, accessibility labels, and named slots.\n- Added schema-driven public component attribute and event validation, required owner events for editable controlled components, unknown-option rejection, normalized string event keys, clearer stable-ID errors, corrected schema option types, and expanded common-builder documentation.\n- Required semantic labels for switches, radio groups, and sliders; propagated switch state, radio orientation, and slider grouping into native accessibility metadata; and enforced one-based tree accessibility positions at both public and native boundaries.\n- Restricted lowercase templates to renderer primitives and added compile-time diagnostics for internal component tags, unsupported tags, and duplicate attributes.\n- Made runtime mutation, frame, drain, and injection operations return structured custom-display failures without crashing, while retaining authoritative session state for frame retries.\n- Added centralized custom-display callback validation with structured startup, synchronization, drain, and event-injection failures.\n- Documented local and remote supervision options and now reject invalid polling intervals and remote session TTLs during startup instead of silently disabling lifecycle behavior.\n- Made native compilation an explicit consumer opt-in outside tests, so `GPUI.Test`, renderer-independent sessions, and remote servers compile and run without Rust or a native library.\n- Added bounded per-connection and per-session remote request admission, explicit overload errors, caller monitoring, and concurrent 50-session load coverage.\n- Moved the remote server, acceptor, connection owners, delegated tasks, session coordinators, and application sessions into explicit OTP supervision subtrees with significant-child shutdown semantics and interruptible deferred mounting.\n- Split collection validation, native controls/forms, and dialog/tooltip/popover rendering into focused internal modules while preserving source-derived RustQ renderer routing.\n- Hardened native image, raster, text-painting, and IME paths against oversized payloads, arithmetic overflow, malformed selection ranges, and recoverable paint failures.\n- Hardened remote lifecycle handling with idempotent mount/event retries, resumable bounded event queues, monitored per-session coordinators, concurrent cross-session dispatch without head-of-line blocking, isolated connection owners, structured display synchronization failures, and stale-timer rejection.\n- Hardened internal display frame replies, explicit session callback contracts, event-history consistency, idempotent native window reconciliation, and shared collection keyboard navigation.\n- Added accessible source-backed data tables with fixed sortable headers, horizontal scrolling, controlled row/cell navigation, numeric alignment, grid metadata, and 100,000-row native coverage; used by BEAM Observatory for process and ETS inspection.\n- Added BEAM Observatory with supervised process and ETS sampling, bounded server-local discovery, asynchronous stale-safe object loading, source-owned models, small table slices, filtering, sorting, and selected-object details.\n- Moved option, image, text-child, and style decoding from handwritten Rust into typespec-derived contracts, ordinary Elixir macros, and typed Rusty-Elixir.\n- Added Elixir Workbench runtime logs with bounded Logger ingestion, asynchronous stale-safe filtering, pause/follow-tail controls, source-backed semantic log rendering, multiline details, retention rollover, and display-side copying.\n- Polished the example suite with clearer controlled-state feedback, filtered and empty process states, safe palette action availability, explicit local/remote path semantics, richer settings review, and modernized resource-reference rendering.\n- Added an accessible source-backed monospaced code and unified-diff viewer with line numbers, controlled selection/reveal, horizontal scrolling, tab expansion, display-side copying, and 100,000-line native coverage; used by Elixir Workbench for source and diff previews.\n- Consolidated component defaults and native attribute types in the shared schema, normalized explicit nil attributes, indexed renderer metadata in one pass, and derived disabled NIF signatures and generated atom references through RustQ metadata.\n- Moved primitive element and event-value decoding into typed Rusty-Elixir and centralized source-backed list/tree mechanics in a handwritten uniform-collection core.\n- Added accessible source-backed trees with controlled expansion, hierarchical keyboard navigation, structural accessibility metadata, and 100,000-item native coverage; used by Elixir Workbench for repository navigation.\n- Added accessible controlled progress, bounded display-side file selection with operation IDs and remote-safe byte payloads, and display-side clipboard buttons.\n- Updated Image Palette to use native file selection, reusable progress, user-side CSS clipboard copying, cancellable analysis, and stale-result-safe replacement.\n- Added source-backed virtual-list ranges with overscan, unloaded selection/reveal indexes, coalesced native range events, remote forwarding, and deterministic 100,000-row coverage.\n- Added Elixir Workbench with bounded repository scans, source-owned tree/diff models, small snapshot slices, status filtering, runtime logs, diagnostics, command actions, and explicit server-local filesystem semantics.\n- Added accessible virtualized uniform-height collections with stable row identities, controlled selection, keyboard navigation, and programmatic reveal.\n- Added common image decoding into `GPUI.Raster` values and a supervised image-palette application with progress, bounded previews, resource reuse, and CSS export.\n- Upgraded generation to RustQ `1.0.0-rc.3` and migrated compiled `defrust` access to the documented `RustQ.Meta.AST` API.\n- Derived native atoms, resources, event values and kinds, renderer routing, disabled-feature NIFs, and boundary policy structurally through RustQ and Rust source metadata.\n- Moved window and component term decoding into typed Rusty-Elixir `defrust` helpers.\n- Added alias-aware component tags, HEEx named slots, and controlled `GPUI.UI.Overlay.popover/1` with keyboard and dismissal focus behavior.\n- Added `GPUI.UI.Overlay.tooltip/1` with textual named-slot content, configurable native show delay, and hoverable lifecycle support.\n- Added controlled `GPUI.UI.Overlay.dialog/1` with arbitrary GPUI content, optional triggers, focus trapping/restoration, keyboard and overlay dismissal, and local/remote native coverage.\n- Added controlled `GPUI.UI.Overlay.dropdown_menu/1` with named items, native popup-menu selection, disabled and checked states, controlled dismissal, and local/remote coverage.\n- Added process-global light and dark component theme switching across native displays.\n- Added controlled `GPUI.UI.input/1` with stable native entity reconciliation.\n- Added native coverage for input editing, rerendering, and theme changes.\n- Added packaged `GPUI.Test` ExUnit helpers and `GPUI.Test.Display` for consumer tests.\n- Added a typed persistent native component registry with structural duplicate-ID validation.\n- Added controlled `GPUI.UI.select/1`, native popup interaction, and semantic test support.\n- Generated native component contracts, element variants, decoder/render dispatch, and state-registry accessors from the component schema.\n- Added persistent `GPUI.UI.combobox/1` with search events, dynamic filtering, clearing, and local/remote native coverage.\n- Added controlled `GPUI.UI.slider/1` with generated numeric contracts, change/release events, persistent reconciliation, and native coverage.\n- Added controlled `GPUI.UI.tabs/1` backed directly by `gpui-component` tab bars.\n- Added controlled `GPUI.UI.accordion/1` and `accordion_item/1` with arbitrary nested GPUI content.\n- Split advanced native component renderers by responsibility and expanded local/remote lifecycle coverage.\n- Added controlled `GPUI.UI.switch/1` and `radio_group/1` with disabled radio options and local/remote native coverage.\n- Added Switch Enter/Space activation, Radio Group arrow navigation with disabled-option skipping, roving tab stops, and visual Switch loading feedback.\n- Added monitored local-runtime and remote-client update subscriptions with typed revisions, normalized events, and synchronized snapshots.\n- Added generation-based native frame barriers for local and remote displays without blocking runtime message processing.\n- Added completed-frame tokens for deterministic synchronization of native-only hover, focus, tooltip, and input transitions.\n- Added deterministic Xvfb visual captures for components and overlays, plus explicit single-purpose screenshot helpers.\n- Replaced E2E state polling and sleep-based synchronization with monitored update messages, frame barriers, and mailbox assertions.\n- Added structured ExDoc guides and replaced the long-form README with a concise project introduction.\n- Preserved upstream component style defaults and constrained full-size Select, Combobox, and Slider controls to their intended layout heights.","ref":"changelog.html#0-1-1"},{"type":"extras","title":"0.1.0 - Changelog","doc":"- Added controlled `GPUI.UI` button and checkbox controls backed by `gpui-component`.\n- Added application-wide component assets, initialization, root wrapping, and theme defaults.\n- Added local and remote native interaction coverage for component rendering and events.","ref":"changelog.html#0-1-0"},{"type":"extras","title":"0.1.0-rc - Changelog","doc":"Initial release candidate.\n\n- Renderer-independent `GPUI.Session` and typed `GPUI.Snapshot` boundaries.\n- Local native and remote TCP displays with isolated application sessions.\n- One process-global GPUI application loop with acknowledged window lifecycle commands.\n- HEEx-style templates and a constrained Tailwind-compatible style normalizer.\n- Native input, raster resources, resource references, and interactive events.\n- RustQ 0.11-generated Rustler exports, Elixir stubs, component metadata, and style dispatch.\n- Structured ExUnit E2E coverage under Xvfb/Lavapipe, including pointer, editing, clipboard, controlled-input, and window-close interaction.\n- Isolated fast, RustQ freshness, native E2E, and release validation environments.\n- Linux CI quality/release gates and an attested, checksummed x86-64 precompiled-NIF release pipeline.","ref":"changelog.html#0-1-0-rc"},{"type":"extras","title":"GPUI","doc":"# GPUI\n\n**Native desktop applications in Elixir, rendered by [GPUI](https://www.gpui.rs).**\n\nGPUI keeps application state, window topology, UI declarations, and event\nhandling in Elixir. Rust owns the native event loop, windows, rendering, focus,\nIME, accessibility, and latency-sensitive interaction. Conventional controls\nare provided separately by `gpui_components`\nand rendered with\n[`gpui-component`](https://github.com/longbridge/gpui-component).\n\n> **Elixir owns the application. GPUI owns the native interaction. Snapshots\n> connect them.**\n\nOne declarative application can run against a local native display, a\ndeterministic test display, or a native display on another machine.\n\nGPUI 0.2.0-rc.2 is published with precompiled Linux, Apple silicon macOS,\nand x86-64 Windows hosts. APIs may still change before 0.2.0. See\n[Platform support and development status](guides/internals/platform-support.md).","ref":"readme.html"},{"type":"extras","title":"Write native UI like Elixir - GPUI","doc":"```elixir\ndefmodule CounterView do\n  use GPUI.View\n  alias GPUI.UI\n\n  def render(assigns) do\n    ~GPUI\"\"\"\n     \n       Count: {assigns.count} \n       \n     \n    \"\"\"\n  end\n\n  def handle_event(\"increment\", _event, assigns) do\n    {:noreply, %{assigns | count: assigns.count + 1}}\n  end\nend\n\ndefmodule CounterApp do\n  use GPUI.Application\n\n  def mount(_args) do\n    {:ok,\n     [\n       window \"Counter\" do\n         size(420, 280)\n         root(CounterView, count: 0)\n       end\n     ]}\n  end\nend\n```\n\n`assigns` remains authoritative Elixir state. Pointer, keyboard, and accessible\nactivation return through the same typed event path; Rust does not become a\nsecond application state system.\n\nRun the progressive examples from the repository:\n\n```bash\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/01_hello_window.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/02_events.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/03_supervised_updates.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/04_controlled_form.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/05_multiple_windows.exs\n```\n\nSee [Your first application](guides/getting-started/first-application.md) for supervision,\ncontrolled components, event handling, and native prerequisites.","ref":"readme.html#write-native-ui-like-elixir"},{"type":"extras","title":"Why GPUI - GPUI","doc":"- **Native, not a web view.** GPUI provides native layout, text shaping,\n  windows, focus, platform input, and AccessKit semantics without a DOM or\n  embedded browser runtime.\n- **Declarative, not raw Rust bindings.** Elixir renders bounded, serializable\n  snapshots; displays interpret them and return strict typed events.\n- **OTP remains the application runtime.** Controlled values stay in Elixir.\n  Native code retains only interaction mechanics such as focus, IME, text\n  selection, drag sessions, and measurement.","ref":"readme.html#why-gpui"},{"type":"extras","title":"One application, multiple displays - GPUI","doc":"```text\nGPUI.Application\n        │\n        ▼\nGPUI.WindowSpec\n        │\n        ▼\nGPUI.Session ──────► GPUI.Snapshot\n                          │\n             ┌────────────┼────────────┐\n             ▼            ▼            ▼\n       Native display  Test display  Remote display\n```\n\nA native display presents real windows. A test display makes view behavior\ndeterministic without Rust or a display server. The remote protocol can keep\nthe authoritative session on one machine while another presents its native\nwindows. Dynamic multi-window topology remains snapshot-driven in every mode.\n\nSee [Sessions, snapshots, and displays](guides/concepts/sessions-snapshots-and-displays.md),\n[Remote displays](guides/remote/remote-displays.md), and\n[Testing GPUI applications](guides/testing/overview.md).","ref":"readme.html#one-application-multiple-displays"},{"type":"extras","title":"Highlights - GPUI","doc":"- **Persistent native text:** Rope buffers, zero-based UTF-16 positions,\n  revisioned atomic transactions, plural selections, undo/redo, IME, shaped\n  style runs, and asynchronous geometry. ` ` supports editors and\n  auto-growing composers without moving document policy into Rust.\n- **Rich virtualized content:** variable-height collections preserve scroll\n  anchors while items are prepended or remeasured; selectable rich text adds\n  native shaping, clipboard behavior, and keyboard-activatable opaque links.\n- **Native semantics across displays:** accessibility roles and states, stable\n  identities, bounded clipboard/file/drop values, and unified pointer,\n  keyboard, and AccessKit activation all travel through renderer-independent\n  snapshots and typed events.\n- **A small handwritten native core:** RustQ generates Rustler declarations,\n  decoders, renderer and style dispatch, and registry glue from the Elixir\n  schema. Handwritten Rust is reserved for platform integration,\n  reconciliation, and latency-sensitive native mechanics.\n\nRead more in [Editable text internals](guides/internals/editable-text-internals.md),\n[Accessibility internals](guides/internals/accessibility.md),\nand [UI components](guides/ui/components.md).","ref":"readme.html#highlights"},{"type":"extras","title":"Examples - GPUI","doc":"| Example | What it demonstrates |\n| --- | --- |\n| [Rich transcript](examples/features/rich_transcript.exs) | Variable-height virtualization, selectable rich text, links, and a native composer |\n| [BEAM Control Room](examples/beam_control_room/README.md) | Supervised runtime sampling driving controlled native views |\n| [Image Lab](examples/image_lab/README.md) | Display-side file reads, image decoding, and bounded raster resources |\n| [Component Gallery](examples/component_gallery/README.md) | Canonical component states, combinations, and accessibility behavior |\n\nRun the `gpui.dev` Mix task with the larger examples for state-preserving\nElixir source reload through `GPUI.Dev.Reload`. See the [examples index](examples/README.md) for the\ncomplete catalog.","ref":"readme.html#examples"},{"type":"extras","title":"Packages - GPUI","doc":"The repository is a Mix umbrella with three independently publishable packages\nand one private maintainer application:\n\n| Package | Purpose |\n| --- | --- |\n| `gpui` | Renderer-independent applications, sessions, snapshots, schemas, remote displays, and test APIs |\n| `gpui_components` | Conventional declarative controls backed by `gpui-component` |\n| `gpui_native` | RustlerPrecompiled vanilla and `gpui-component` native hosts |\n| Root tooling | Private RustQ generation, repository checks, native testing, and release validation |\n\nRustQ runs only in the source umbrella. Generated Elixir and Rust are committed,\nand supported consumer targets download checksum-verified NIF archives through\n`RustlerPrecompiled`; consumer compilation does not run RustQ.","ref":"readme.html#packages"},{"type":"extras","title":"Installation - GPUI","doc":"For a renderer-independent application or remote server:\n\n```elixir\ndef deps do\n  [\n    {:gpui, \"== 0.2.0-rc.2\"}\n  ]\nend\n```\n\nFor a native application using conventional controls, `gpui_native` brings the\nmatching `gpui` and `gpui_components` versions transitively:\n\n```elixir\ndef deps do\n  [\n    {:gpui_native, \"== 0.2.0-rc.2\"}\n  ]\nend\n```\n\nSelect the `gpui-component` host when component elements are used:\n\n```elixir\n# config/config.exs\nconfig :gpui_native, GPUI.Native, host: :gpui_component\n```\n\nUse `host: :vanilla` for the complete vanilla-GPUI host. Exactly one host\nartifact is loaded; the component-capable artifact statically links its\nrenderer rather than attaching a second native library at runtime.\nRenderer-independent sessions, remote servers, and `GPUI.Test`\nrequire neither Rust nor a native library or display server. See\n[Native builds and deployment](guides/deployment/native-builds.md).","ref":"readme.html#installation"},{"type":"extras","title":"Documentation - GPUI","doc":"- [Documentation home](guides/documentation.md)\n- [Your first application](guides/getting-started/first-application.md)\n- [UI components](guides/ui/components.md)\n- [Templates and elements](guides/ui/templates-and-elements.md)\n- [Forms and controls](guides/ui/forms-and-controls.md)\n- [Collections and data views](guides/ui/collections-and-data-views.md)\n- [Text and editing](guides/ui/text-and-editing.md)\n- [Editable text surfaces](guides/ui/editable-text.md)\n- [Layout, styling, and presentation](guides/ui/layout-styling-and-presentation.md)\n- [Presentation primitives](guides/ui/presentation-primitives.md)\n- [Commands and keyboard shortcuts](guides/ui/commands-and-shortcuts.md)\n- [Overlays and menus](guides/ui/overlays-and-menus.md)\n- [Testing GPUI applications](guides/testing/overview.md)\n- [Application tests](guides/testing/application-tests.md)\n- [Deterministic native tests](guides/testing/native-tests.md)\n- [Desktop E2E and visual evidence](guides/testing/desktop-e2e.md)\n- [Platform support and development status](guides/internals/platform-support.md)\n- [API reference](https://hexdocs.pm/gpui/api-reference.html)","ref":"readme.html#documentation"},{"type":"extras","title":"Development - GPUI","doc":"```bash\nmix deps.get\nmix gpui.test.packages\nmix ci\nMIX_ENV=e2e xvfb-run -a dbus-run-session -- mix test --only e2e apps/gpui_native/test/e2e\n```\n\n`mix gpui.test.packages` builds the three public Hex payloads and compiles their\nexact unpacked contents in clean downstream consumers, with repository-only\ntooling and Cargo unavailable. `mix ci` includes this package-isolation gate and\ncovers Elixir, generated Rust freshness, Cargo feature matrices,\nClippy, native unit tests, Dialyzer, Credo, duplication, and architecture\nchecks. Hosted push validation splits the renderer-independent and native\nchecks; the full native interaction and release-package suite is an explicit\n`Release candidate` workflow rather than a scheduled or per-push job. See\n[Testing GPUI applications](guides/testing/overview.md) for native\nXvfb/Lavapipe interaction coverage.","ref":"readme.html#development"},{"type":"extras","title":"License - GPUI","doc":"MIT","ref":"readme.html#license"},{"type":"extras","title":"GPUI documentation","doc":"# GPUI documentation\n\nGPUI keeps application state and declarative UI policy in Elixir while native\ndisplays interpret immutable snapshots. Choose a path below based on what you\nare trying to do.","ref":"documentation.html"},{"type":"extras","title":"Build an application - GPUI documentation","doc":"1. Start with [Your first application](first-application.html).\n2. Learn the runtime model in\n   [Sessions, snapshots, and displays](sessions-snapshots-and-displays.html).\n3. Browse [UI components](components.html),\n   [Commands and keyboard shortcuts](commands-and-shortcuts.html), and\n   [Overlays and menus](overlays-and-menus.html).\n4. Choose the appropriate test tier in\n   [Testing GPUI applications](overview.html).","ref":"documentation.html#build-an-application"},{"type":"extras","title":"Use framework capabilities - GPUI documentation","doc":"- [Remote displays](remote-displays.html) connects an application session to a\n  display in another process or machine.\n- [Native builds and deployment](native-builds.html) covers source builds,\n  feature sets, and release validation.\n- [Editable text surfaces](editable-text.html) explains persistent buffers,\n  revisioned edits, submission, geometry, and annotations.\n- [Presentation primitives](presentation-primitives.html) explains bounded edge\n  fades, frost fallbacks, and custom paint for application authors.\n- [Test coverage ownership](coverage-ownership.html) identifies which test tier\n  owns each kind of behavior.","ref":"documentation.html#use-framework-capabilities"},{"type":"extras","title":"Contribute to GPUI - GPUI documentation","doc":"These documents describe implementation boundaries rather than the normal\napplication-authoring workflow:\n\n- [Platform support and development status](platform-support.html)\n- [Editable text internals](editable-text-internals.html)\n- [Native text projections](text-projections.html)\n- [Accessibility](accessibility.html)\n- [Transfer payloads](transfers.html)\n- [Presentation extension contracts](presentation-contracts.html)\n- [Declarative motion decision](declarative-motion.html)\n- [Window chrome decision](window-chrome.html)\n\nThe project is private and unreleased. Internal payloads may change directly;\nversion fields detect mismatched running components rather than promise support\nfor historical revisions.","ref":"documentation.html#contribute-to-gpui"},{"type":"extras","title":"Your first application","doc":"# Your first application\n\nGPUI keeps application state in ordinary Elixir processes and uses a display to\npresent serializable snapshots. A local application combines one or more\n`GPUI.View` modules, a `GPUI.Application`, and an OTP-supervised `GPUI.Runtime`.","ref":"first-application.html"},{"type":"extras","title":"Prerequisites - Your first application","doc":"GPUI requires Elixir 1.20 or later. Native Linux builds require Rust and the XKB\ndevelopment packages:\n\n```bash\nsudo apt-get install libxkbcommon-dev libxkbcommon-x11-dev\n```\n\nEnable the native display outside tests in your application's configuration:\n\n```elixir\n# config/config.exs\nconfig :gpui_native, build_native: config_env() != :test\nconfig :gpui_native, GPUI.Native, host: :gpui_component\n```\n\nThis keeps renderer-independent tests free of Rust and native-library\nrequirements. If `fontconfig.pc` is unavailable for a native build, compile\nwith:\n\n```bash\nRUST_FONTCONFIG_DLOPEN=1 mix compile\n```","ref":"first-application.html#prerequisites"},{"type":"extras","title":"Run the examples - Your first application","doc":"The getting-started examples form a short progression:\n\n```bash\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/01_hello_window.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/02_events.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/03_supervised_updates.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/04_controlled_form.exs\nRUST_FONTCONFIG_DLOPEN=1 mix run apps/gpui/examples/getting_started/05_multiple_windows.exs\n```\n\n- **Hello Window** introduces a view, an application, and supervision.\n- **Focus Timer** combines controlled events with periodic OTP messages.\n- **Settings Form** demonstrates native controls, validation state, dynamic\n  styling, and a controlled dialog.\n\nTheir application modules live under `examples/getting_started/support/`, so\nthey can also be loaded without starting a native display and tested through\n`GPUI.Test`.","ref":"first-application.html#run-the-examples"},{"type":"extras","title":"Define a view - Your first application","doc":"A view renders a `%GPUI.Element{}` tree from assigns. The `~GPUI` sigil accepts\nHEEx-shaped tags, expressions, aliases, native components, and named slots.\n\n```elixir\ndefmodule MyApp.WelcomeView do\n  use GPUI.View\n\n  @impl GPUI.View\n  def render(_assigns) do\n    ~GPUI\"\"\"\n     \n       Hello from the BEAM \n       ● Runtime connected \n     \n    \"\"\"\n  end\nend\n```","ref":"first-application.html#define-a-view"},{"type":"extras","title":"Define an application - Your first application","doc":"An application's `mount/1` callback returns its initial windows. Each root view\nowns its own assigns.\n\n```elixir\ndefmodule MyApp.Desktop do\n  use GPUI.Application\n\n  @impl GPUI.Application\n  def mount(_args) do\n    {:ok,\n     [\n       window \"My application\" do\n         size(520, 320)\n         root(MyApp.WelcomeView)\n       end\n     ]}\n  end\nend\n```\n\nUsing the application module as a child starts a `GPUI.Runtime` with the native\ndisplay by default:\n\n```elixir\nSupervisor.start_link([MyApp.Desktop], strategy: :one_for_one)\n```\n\nPass runtime options through the normal `{module, options}` child form when the\napplication needs mount arguments, a registered runtime, or a custom display:\n\n```elixir\nchildren = [\n  {MyApp.Desktop,\n   name: MyApp.Runtime,\n   args: %{account_id: account_id},\n   display: GPUI.Display.Native,\n   poll_interval: 16}\n]\n\nSupervisor.start_link(children, strategy: :one_for_one)\n```","ref":"first-application.html#define-an-application"},{"type":"extras","title":"Controlled events - Your first application","doc":"Interactive native controls are controlled by root-view assigns:\n\n```elixir\n \n   \n \n```\n\n```elixir\n@impl GPUI.View\ndef handle_event(\"name_changed\", %{value: name}, assigns),\n  do: {:noreply, %{assigns | name: name}}\n```\n\n`phx-submit` is optional and emits Enter activation with the input's current\nstring value. Increment `focus_request` when an application-owned transition,\nsuch as failed validation, should move native focus back to the input.\n\nStable string IDs preserve native focus, editing state, popup state, and\nselection across snapshots. Duplicate IDs are rejected before reaching a\ndisplay.","ref":"first-application.html#controlled-events"},{"type":"extras","title":"Updates from OTP processes - Your first application","doc":"Views can also handle application messages independently of pointer and keyboard\ninput:\n\n```elixir\n@impl GPUI.View\ndef handle_info(:tick, assigns),\n  do: {:noreply, %{assigns | elapsed: assigns.elapsed + 1}}\n```\n\nA supervised worker delivers the message through the runtime:\n\n```elixir\n{:ok, _snapshot} = GPUI.Runtime.send_view(MyApp.Runtime, 1, :tick)\n```\n\n`send_view/3` updates the selected root view, synchronizes the display, and\npublishes the same typed runtime update used by other state transitions. The\nFocus Timer shows this pattern with a `GenServer` using `Process.send_after/3`.","ref":"first-application.html#updates-from-otp-processes"},{"type":"extras","title":"Test without a native window - Your first application","doc":"The examples use normal application modules, so the same behavior can be tested\nwithout a NIF or display server:\n\n```elixir\ndefmodule MyApp.TimerTest do\n  use GPUI.Test, async: true\n\n  test \"advances from an OTP message\" do\n    runtime = start_runtime!(GettingStarted.FocusTimer.App, args: %{seconds: 2})\n\n    click(runtime, \"start\")\n    send_view(runtime, :tick)\n\n    assert %{remaining: 1, status: :running} = assigns(runtime)\n  end\nend\n```\n\nContinue with [UI components](components.html),\n[Overlays and menus](overlays-and-menus.html), [Testing GPUI applications](overview.html),\nand [Sessions, snapshots, and displays](sessions-snapshots-and-displays.html).","ref":"first-application.html#test-without-a-native-window"},{"type":"extras","title":"Sessions, snapshots, and displays","doc":"# Sessions, snapshots, and displays\n\nGPUI separates renderer-independent application state from display lifecycle.\nThis keeps view logic deterministic, permits local and remote presentation, and\nprevents native window ownership from leaking into application processes.","ref":"sessions-snapshots-and-displays.html"},{"type":"extras","title":"Session - Sessions, snapshots, and displays","doc":"`GPUI.Session` owns:\n\n- window specifications and root-view assigns;\n- rendered `%GPUI.Snapshot{}` values;\n- resource metadata;\n- event dispatch and bounded event history.\n\nA session does not own a native event loop or platform window. Its boundary is\nserializable Elixir data.","ref":"sessions-snapshots-and-displays.html#session"},{"type":"extras","title":"Dynamic window topology - Sessions, snapshots, and displays","doc":"Initial windows may declare a stable application-owned key independently of the\nsession's monotonic native window ID:\n\n```elixir\nwindow \"main\", \"Workspace\" do\n  size 1100, 720\n  root WorkspaceView\nend\n```\n\nA running local session can add and remove windows without remounting the\napplication:\n\n```elixir\ndetails =\n  GPUI.WindowSpec.new(\"Repository details\",\n    key: \"repository-details\",\n    root: {RepositoryDetailsView, repository: repository}\n  )\n\n{:ok, window_id, snapshot} = GPUI.Runtime.open_window(runtime, details)\n{:ok, snapshot} = GPUI.Runtime.close_window(runtime, \"repository-details\")\n```\n\nKeys are optional for initial windows, but dynamically managed windows should\nuse non-empty unique strings of at most 128 bytes. Titles are limited to 512\nbytes and a session can own at most 32 windows. IDs are never reused in a\nsession, including after a keyed window closes and reopens. Closing\nby key or ID removes only that window; other root assigns and native windows\nremain intact. Snapshot synchronization performs the actual platform close.\nSource refresh rerenders the current topology and preserves every window's\nexisting assigns, key, and session ID. It does not remount the application or\nresurrect windows that have already closed. Dynamic ID allocation also remains\nmonotonic across reload, while newly compiled render and callback code applies\nto every retained root module. Refresh renders the complete snapshot before\npublishing it, so a failure in any window produces no display or subscriber\nupdate. Development watchers can optionally report bounded `:gpui_reload`\nresults; syntax-invalid files leave the loaded module and live session usable\nfor the next valid edit.\n\nThese mutation calls are local runtime/session capabilities. Remote topology\nmutation is intentionally not added as an imperative transport operation.\nView-driven topology works remotely through ordinary `:event` requests: the\nhosted session applies the typed outcome and returns one authoritative\nmulti-window snapshot. Protocol negotiation advertises `:window_topology_v1`,\nand session resume restores existing keys, IDs, assigns, and monotonic ID\nallocation.","ref":"sessions-snapshots-and-displays.html#dynamic-window-topology"},{"type":"extras","title":"Display - Sessions, snapshots, and displays","doc":"A `GPUI.Display` receives snapshots and returns normalized events. The package\nships two principal implementations:\n\n- `GPUI.Display.Native` presents snapshots through Rust GPUI windows;\n- `GPUI.Test.Display` records snapshots and accepts deterministic injected events.\n\nRemote clients can also mount a native display against a session hosted by\n`GPUI.Remote.Server`.\n\nCustom displays implement `GPUI.Display`. Startup must return `{:ok, pid}` or\n`{:error, reason}`; synchronization must return `:ok` or `{:error, reason}`;\nevent draining must return `{:ok, events}` or `{:error, reason}`; and injection\nmust return an `:ok` or `:error` tuple. GPUI rejects invalid returns as\n`{:invalid_display_return, callback, value}` and catches callback failures as\n`{:display_callback_failed, callback, kind, reason}`.","ref":"sessions-snapshots-and-displays.html#display"},{"type":"extras","title":"Runtime - Sessions, snapshots, and displays","doc":"`GPUI.Runtime` composes one session with one display. It synchronizes snapshots,\npolls display events, dispatches event batches through the session, and sends\nthe resulting snapshot back to the display.\n\n```elixir\n{:ok, runtime} =\n  GPUI.Runtime.start_link(\n    app: MyApp.Desktop,\n    args: %{account_id: account_id},\n    display: GPUI.Display.Native,\n    poll_interval: 16\n  )\n```\n\nNormally the application module is placed directly in a supervision tree and\nits generated child specification starts the runtime.\n\nSnapshot queries use the same tagged result convention as remote clients and\nother fallible runtime operations:\n\n```elixir\n{:ok, snapshot} = GPUI.Runtime.snapshot(runtime)\n```\n\n`GPUI.Runtime.snapshot!/1` is available when rendering failure should raise a\n`GPUI.Runtime.Error` instead. A local snapshot query can fail only while\nrendering the authoritative session state. `GPUI.Remote.Client.snapshot/1`\nadditionally reports transport, protocol, and local display-synchronization\nfailures through its `{:error, reason}` result.\n\nSupervised workers can update a root view without pretending that application\nwork is native input:\n\n```elixir\n{:ok, snapshot} = GPUI.Runtime.send_view(runtime, window_id, {:loaded, records})\n```\n\nThe selected view receives the message in `handle_info/2`. The runtime then\nsynchronizes and publishes the resulting snapshot. `handle_event/3` and\n`handle_info/2` can also return typed window outcomes without receiving a\nruntime PID or a generic effect bus:\n\n```elixir\ndef handle_event(\"open-details\", _event, assigns) do\n  details =\n    GPUI.WindowSpec.new(\"Repository details\",\n      key: \"repository-details\",\n      root: {RepositoryDetailsView, repository: assigns.repository}\n    )\n\n  {:open_window, details, assigns}\nend\n\ndef handle_event(\"close-details\", _event, assigns) do\n  {:close_window, \"repository-details\", assigns}\nend\n```\n\nSupported view results are `{:noreply, assigns}`, `{:close, assigns}`,\n`{:open_window, spec, assigns}`, and\n`{:close_window, key_or_id, assigns}`. A topology outcome and its originating\nassigns update form one session transition and one synchronized snapshot.\nDuplicate keys and missing close targets leave the authoritative topology and\noriginating assigns unchanged and are returned as normalized event errors.\n\nRuntime updates are available through ordinary OTP messages:\n\n```elixir\n:ok = GPUI.Runtime.subscribe(runtime)\n\nreceive do\n  {:gpui, ^runtime, %GPUI.Runtime.Update{revision: revision, events: events, snapshot: snapshot}} ->\n    # The snapshot has already been synchronized to the active display.\nend\n\n:ok = GPUI.Runtime.unsubscribe(runtime)\n```\n\nRevisions increase monotonically within a runtime. Subscribers are monitored\nand removed automatically when they exit. Frame synchronization is separate\nfrom snapshot synchronization: `GPUI.Runtime.await_frame/3` waits until the\ncurrent snapshot generation has completed a frame, without blocking the runtime\nfrom processing messages.\n\nNative-only changes such as hover, focus, tooltip timers, and IME state can be\nsynchronized with a completed-frame token:\n\n```elixir\n{:ok, generation} = GPUI.Runtime.frame_token(runtime, window_id)\n# Send native platform input here.\n:ok = GPUI.Runtime.await_frame_after(runtime, window_id, generation)\n```\n\n`GPUI.Runtime.request_frame/1` resynchronizes the current snapshot when callers\nneed an explicit frame without changing application state. Runtime operations\nreturn structured `:display_start_failed`, `:display_sync_failed`,\n`:display_drain_failed`, and `:display_inject_failed` errors instead of crashing\nwhen a custom display fails its contract. Session mutations happen before display\nsynchronization; after a synchronization error, `request_frame/1` retries the\ncurrent authoritative snapshot.","ref":"sessions-snapshots-and-displays.html#runtime"},{"type":"extras","title":"Native process model - Sessions, snapshots, and displays","doc":"Native displays share one process-global GPUI application loop. That loop owns\nall platform windows, while commands and registry state remain scoped to their\noriginating runtime and window IDs.\n\nLoop acquisition is platform-specific behind the same host boundary:\n\n- on macOS, the host asks ERTS to hand the original process main thread to GPUI,\n  satisfying AppKit's main-thread requirement without moving application state\n  out of Elixir;\n- on Linux and Windows, the host starts one permanent dedicated native GUI\n  thread and keeps every window on that thread.\n\nStartup is acknowledged only after the GPUI application has initialized. The\nlast runtime releasing its windows does not tear down and recreate the\nprocess-global application loop.\n\nCommands are acknowledged: callers receive success, timeout, disconnection, or\nstopped-runtime outcomes instead of enqueue-only success. Native frame barriers\ntrack snapshot and completed-frame generations per window and acknowledge after\nthe corresponding frame has passed prepaint and presentation submission. Closing the\nfinal window does not terminate the shared loop. Runtime shutdown is\nnon-blocking and cleans up only that runtime's windows and component state.","ref":"sessions-snapshots-and-displays.html#native-process-model"},{"type":"extras","title":"Declarative viewport - Sessions, snapshots, and displays","doc":"Each rendered window tree is wrapped by a renderer-internal `:viewport` node in\nElixir before it crosses a display boundary. The node establishes the current\nwindow content bounds; it is included in native and remote snapshots but is not\na public template tag. Rust interprets those declared viewport semantics and\ndoes not inject application-specific layout wrappers.\n\nApplication roots should declare how they consume the viewport. A flex root\nnormally uses `grow` and `w-full`; definite `w-full` and `h-full` lengths remain\navailable where the parent layout provides a definite corresponding dimension.\nThis keeps viewport declaration, layout values, defaults, and component policy\nin Elixir while leaving platform window state in GPUI.","ref":"sessions-snapshots-and-displays.html#declarative-viewport"},{"type":"extras","title":"Snapshot reconciliation - Sessions, snapshots, and displays","doc":"A snapshot contains the complete declarative window set. Native synchronization:\n\n1. creates missing windows;\n2. updates existing window trees and resources;\n3. removes windows absent from the next snapshot;\n4. reconciles stateful controls by component kind and stable ID;\n5. drops registry entries no longer present in the rendered tree.\n\nWindow snapshots also carry bounded command ID/shortcut pairs. The native root\nobserves matched keystrokes after normal GPUI dispatch, emits `:command` events,\nand suppresses standard editing combinations while an input is focused.\n\nComponent IDs never depend on traversal position. Duplicate structural IDs are\nrejected before crossing the display boundary.","ref":"sessions-snapshots-and-displays.html#snapshot-reconciliation"},{"type":"extras","title":"RustQ boundary - Sessions, snapshots, and displays","doc":"RustQ generates repetitive native boundaries from GPUI's internal schema:\n\n- source-discovered atoms and Rustler NIF exports;\n- resource contracts and decoders;\n- window, primitive-element, component, and event-value decoders authored as typed Rusty-Elixir where their behavior is renderer-independent;\n- source-derived renderer routing and element dispatch;\n- style dispatch;\n- disabled-feature NIF implementations;\n- stateful registry kinds and typed accessors.\n\nComponent defaults and attribute constraints come from the same schema used by\npublic builders and native decoding. Renderer metadata is indexed from the real\nRust functions in one pass instead of maintained in a parallel route table.\n\nGPUI lifecycle, entities, focus, callbacks, reconciliation behavior, and\nplatform integration remain handwritten Rust. Shared native collection\nmechanics live in a handwritten uniform-collection core; listbox and tree\nkeyboard and accessibility semantics remain in their focused renderers. This\nkeeps generated code structural while preserving readable native behavior.\n\nSee [Remote displays](remote-displays.html) for transport topology and\n[Testing GPUI applications](overview.html) for renderer-independent testing.","ref":"sessions-snapshots-and-displays.html#rustq-boundary"},{"type":"extras","title":"UI components","doc":"# UI components\n\nConventional controls live in the separately published `gpui_components`\npackage. `GPUI.UI` provides controlled Elixir components rendered by the\n`:gpui_component` `gpui_native` host with `gpui-component`. Components build\nordinary `%GPUI.Element{}` data; the native display owns GPUI entities, focus,\nand transient interaction state.\n\nChoose a guide by the task you are working on:\n\n- [Templates and elements](templates-and-elements.html) explains HEEx-style\n  templates, primitive tags, component aliases, stable identities, and\n  schema-backed validation.\n- [Forms and controls](forms-and-controls.html) covers inputs, choices, grouped\n  controls, tabs, accordions, sliders, splits, progress, and focus.\n- [Collections and data views](collections-and-data-views.html) covers variable\n  and uniform virtualization, source-backed lists, tables, trees, and code\n  viewers.\n- [Text and editing](text-and-editing.html) covers selectable rich text.\n- [Editable text surfaces](editable-text.html) covers persistent buffers,\n  revisioned transactions, submission, geometry, and text annotations.\n- [Layout, styling, and presentation](layout-styling-and-presentation.html)\n  covers Tailwind-compatible classes, layers, motion, bounds, and themes.\n- [Presentation primitives](presentation-primitives.html) covers bounded edge\n  fades, frost fallbacks, and rectangle-and-line custom paint.\n- [Resources and display actions](resources-and-display-actions.html) covers\n  display-local file selection, clipboard writes, image decoding, and raster\n  resources.\n- [Windows and lifecycle](windows-and-lifecycle.html) covers declarative window\n  constraints and lifecycle callbacks.\n- [Commands and keyboard shortcuts](commands-and-shortcuts.html) and\n  [Overlays and menus](overlays-and-menus.html) document those focused\n  interaction systems.\n\nEvery native component requires a stable, non-empty string `id`. Controlled\nvalues remain application-owned; displays retain only transient interaction\nstate and emit events requesting authoritative Elixir updates.","ref":"components.html"},{"type":"extras","title":"Templates and elements","doc":"# Templates and elements\n\n\n`GPUI.UI` provides controlled Elixir components backed directly by\n`gpui-component`. Components build ordinary `%GPUI.Element{}` data; the native\ndisplay owns GPUI entities, callbacks, focus, and transient interaction state.","ref":"templates-and-elements.html"},{"type":"extras","title":"Aliases and templates - Templates and elements","doc":"Component aliases are resolved from the calling module:\n\n```elixir\nalias GPUI.UI\n\n~GPUI\"\"\"\n \n   \n   \n \n\"\"\"\n```\n\nLowercase tags are renderer primitives such as `div`, `text`, `input`, and\n`img`. Native components are always called through `GPUI.UI` or\n`GPUI.UI.Overlay`; internal tags such as ` ` are rejected at template\ncompile time instead of bypassing their public builder contracts. Duplicate\nattributes and unknown tags are also compile errors.\n\nComponent events use `phx-*` attributes because the template and callback\nvocabulary intentionally follows Phoenix conventions familiar to Elixir users;\nthere is no Phoenix process or browser event behind them. The attribute value is\na stable application event name delivered to the `GPUI.View` `handle_event/3`\ncallback.\n\nEvery native component requires a stable, non-empty string `id`. Component\nbuilders reject unknown options and validate schema-backed attributes and event\nnames before a snapshot reaches any display, including `GPUI.Test.Display`.\nInvalid values raise an `ArgumentError` that names the component, attribute,\nexpected type or enum, and received value—for example,\n`ui_button :disabled must be a boolean; got: \"yes\"`.\n\nEach builder's ExDoc entry includes a schema-derived option table showing types,\nrequired attributes and events, enum values, and defaults. The corresponding\nnamed option type—for example `t:GPUI.UI.slider_options/0` or\n`t:GPUI.UI.Overlay.dialog_options/0`—provides the same contract to editors,\nDialyzer, and library consumers. Both projections come from the shared internal\ncomponent schema, so native decoding, validation, types, and documentation\ncannot drift into separate option registries.","ref":"templates-and-elements.html#aliases-and-templates"},{"type":"extras","title":"Forms and controls","doc":"# Forms and controls\n\nControlled components emit requested changes while Elixir remains authoritative\nover persisted values. Native displays retain transient focus, selection,\npointer, and drag state across ordinary rerenders.","ref":"forms-and-controls.html"},{"type":"extras","title":"Inputs and choices - Forms and controls","doc":"```elixir\n \n\n \n\n \n```\n\nButtons, checkboxes, inputs, selects, and comboboxes require non-empty semantic\nlabels. Native text-input metadata exposes the controlled value and placeholder;\nmasked inputs expose a password-input role without leaking their value. Select\nmetadata exposes the visible label of the controlled option, while searchable\ncomboboxes retain the upstream expanded-state semantics inside a labeled group.\nInput, select, and combobox labels are semantic rather than visible captions.\nUse `UI.field/1` when the interface also needs a visual label and controlled help\nor error feedback:\n\n```elixir\n \n   \n \n```\n\nA field accepts exactly one control. Error feedback replaces help text and stays\nowned by the view. Enter emits optional `phx-submit` with the input's current\nvalue. Increment `focus_request` to focus the input after a failed submission;\nusing a monotonically increasing request avoids repeatedly stealing focus during\nordinary rerenders.\n\nSelect, combobox, tabs, and radio-group options accept strings,\n`{label, value}` tuples, or `%{label: label, value: value}` maps. Radio maps may\nalso set `disabled: true`. Option values must be unique.\n\nEditable controlled components require a non-empty `phx-change`. This includes\ncheckboxes, inputs, selects, comboboxes, switches, radio groups, accordions,\ntabs, and sliders. Requiring an owner for native edits prevents a control from\nappearing editable while silently snapping back to an unchanged Elixir value.\n`phx-search` on comboboxes and `phx-release` on sliders remain optional secondary\nevents.\n\nInputs preserve native focus, cursor, selection, clipboard, and IME state while\ncontrolled snapshots are reconciled. Combobox search and option replacement can\nbe asynchronous.","ref":"forms-and-controls.html#inputs-and-choices"},{"type":"extras","title":"Boolean and grouped controls - Forms and controls","doc":"```elixir\n \n\n \n```\n\nSwitch and radio-group labels are semantic contracts, not decorative captions:\nthey provide stable native accessibility names. Switches support Enter and\nSpace. Radio groups use roving tab stops and Left/Up/Right/Down navigation,\nwrapping around disabled options.","ref":"forms-and-controls.html#boolean-and-grouped-controls"},{"type":"extras","title":"Tabs, accordions, sliders, and splits - Forms and controls","doc":"Tabs expose a native tab-list with labeled tab roles and controlled selected\nstate. The selected tab is the single Tab stop; Left/Up and Right/Down move and\nselect with wrapping, Home/End select endpoints, and Enter/Space activate the\nfocused tab. Pointer selection focuses the chosen tab. Disabled tab bars expose\ndisabled semantics and install no pointer, keyboard, Tab, or accessible Click\naction. Every activation path emits the same controlled `phx-change` value.\n\n```elixir\n \n\n \n   \n     Account details \n   \n \n\n \n \n   Navigation \n   Content \n \n```\n\nTab changes carry one string value. Accordion changes carry an ordered list of\nexpanded item IDs. A slider label names the accessibility group around the\nnative slider, which exposes its controlled value, range, step, and orientation.\nSlider changes are continuous and `phx-release` fires once\npointer interaction ends. Linear and logarithmic slider scales are supported.\n\nA split has exactly two children and a horizontal or vertical native resize\naxis. `sizes`, `min_sizes`, and `max_sizes` are bounded two-element pixel lists.\nThe display owns pointer drag mechanics and emits the resulting two sizes\nthrough `phx-change`; applications persist those values in assigns. Controlled\nsizes are not reapplied during ordinary rerenders, which avoids resetting an\nactive drag. Increment the monotonic `resize_request` token to programmatically\napply `sizes`. This primitive carries no sidebar, editor, dock, or persistence\npolicy.","ref":"forms-and-controls.html#tabs-accordions-sliders-and-splits"},{"type":"extras","title":"Progress - Forms and controls","doc":"`progress/1` renders a native accessible progress indicator. Values are\ncontrolled and must remain between zero and `max`; set `indeterminate={true}`\nwhen the amount of completed work is unknown.\n\n```elixir\n \n```","ref":"forms-and-controls.html#progress"},{"type":"extras","title":"Button variants and sizes - Forms and controls","doc":"Button variants are `default`, `primary`, `secondary`, `danger`, `warning`,\n`success`, `info`, `ghost`, `link`, and `text`. Component sizes are `xs`, `sm`,\n`md`, and `lg` where supported.","ref":"forms-and-controls.html#button-variants-and-sizes"},{"type":"extras","title":"Focus - Forms and controls","doc":"The semantic ` ` primitive is a styled native button region with built-in\nbutton accessibility role, Tab focus, Enter/Space activation, and unified\npointer/AccessKit activation. Use ` ` when GPUI Component variants,\nsizing, loading, selection, or clipboard capabilities are needed.\n\nFocusable primitives use a monotonic request token instead of an\napplication-controlled focus boolean:\n\n```elixir\n \n  ...\n \n```\n\nIncrement `focus_request` to request native focus once. Rerendering with the\nsame token does not steal focus again. Native pointer and keyboard focus remain\nnative state and emit `phx-focus`/`phx-blur` only when focus actually changes.\nThe event value contains the stable element ID:\n\n```elixir\n%{type: :focus, value: %{id: \"search-trigger\"}}\n%{type: :blur, value: %{id: \"search-trigger\"}}\n```\n\nThe first generic contract supports ` `, low-level ` `,\n` `, and ` `. Focus behavior requires a non-empty stable\n`id`; arbitrary focusable containers and controlled `focused={true}` state are\nintentionally unsupported.\n\nUse ` ` only for an unlabelled low-level single-line primitive.\nOrdinary forms should prefer ` `; revisioned editor and composer\nsurfaces should use ` `.","ref":"forms-and-controls.html#focus"},{"type":"extras","title":"Collections and data views","doc":"# Collections and data views\n\nGPUI provides variable-height and uniform source-backed collection primitives.\nStable identities and explicit bounds let native displays virtualize layout\nwithout moving application data policy out of Elixir.","ref":"collections-and-data-views.html"},{"type":"extras","title":"Virtualized collections - Collections and data views","doc":"`virtual_collection/1` is the variable-height primitive for transcripts,\nactivity feeds, and other heterogeneous collections. The complete logical\ncollection stays in the renderer-independent snapshot, while the native display\nmeasures and renders the visible region with bounded pixel `overdraw`.\n\n```elixir\n \n  {Enum.map(assigns.messages, fn message ->\n    UI.virtual_item(%{\n      id: message.id,\n      revision: message.revision,\n      children: [render_message(message)]\n    })\n  end)}\n \n```\n\n`alignment=\"bottom\"` starts short collections at the lower edge. `follow=\"tail\"`\ntracks appended or growing tail content until the user scrolls away; increment\n`follow_request` to explicitly return to the tail. To reveal a retained item,\nset `reveal` and increment `reveal_request`. The truthful placement strategies\nfor the variable-height primitive are `nearest` and `top`.\n\nEvery item ID is stable, unique, non-empty, and at most 128 bytes. Increment an\nitem's `revision` when retained content may change measured height. Appends,\nprepends, removals, and revision changes preserve GPUI's measured-height cache\nfor unaffected IDs. Collections are bounded to 100,000 items and `overdraw` to\n4,096 pixels.\n\n`phx-range` emits deduplicated visible `%{first: first, last: last}` ranges with\nan exclusive\n`last`. Unlike the uniform source-backed primitive below, variable collections\ncurrently require all logical item nodes in each snapshot: unknown offscreen\nheights have no truthful placeholder geometry. Remote displays receive this\nsame complete serializable contract.\n\n`virtual_list/1` presents source-backed uniform collections through GPUI's native uniform-list\nlayout. Every row has a stable ID and the same declared height; only the visible\nrange is constructed and laid out natively.\n\n```elixir\n \n  {Enum.map(assigns.processes, fn process ->\n    UI.virtual_list_item(%{\n      id: process.pid,\n      children: [process.label]\n    })\n  end)}\n \n```\n\n`selected` is controlled and changes emit the selected item ID. `reveal`\nrequests programmatic scrolling with `nearest`, `top`, `center`, or `bottom`\nplacement. Up/Down, Home/End, Enter, and Space operate from one listbox tab stop\nand skip disabled rows. Pointer selection focuses the list, and native\naccessibility exposes listbox and option roles with an active descendant.\n\nThe list itself must have a fixed or maximum height. Rows that differ from\n`item_height` violate the uniform-list contract. Filtering and sorting may\nreplace or reorder children while scroll state remains attached to the list's\nstable ID.","ref":"collections-and-data-views.html#virtualized-collections"},{"type":"extras","title":"Source-backed lists - Collections and data views","doc":"Rendering is not the only cost of a large collection: putting every child—or\nthe full source model—into view assigns also serializes it into every local or\nremote snapshot. Source-backed lists keep the full model in a supervised source\nprocess and put only the current loaded slice in assigns.\n`GPUI.UI.Collection.Source` and `GPUI.UI.Collection.Selection` can keep the\ncorrelated count/offset/children and ID/index pairs together before those values\nare passed to a component.\n\n```elixir\n \n  {Enum.map(assigns.loaded_rows, &row/1)}\n \n```\n\n`phx-range` emits `%{first: first, last: last}` with an exclusive `last` index.\nThe requested range already includes `overscan`. The source responds by loading\na contiguous slice, setting `offset` to `first`, and replacing `loaded_rows`.\n`selected_index` and `reveal_index` preserve controlled selection and distant\nreveal while the selected row is unloaded. Placeholder rows retain the correct\nscroll geometry until the requested slice arrives. Arrow navigation uses the\nloaded overscan; Home and End activate only when the corresponding endpoint is\nloaded. Applications can perform global jumps by changing the controlled\nselection and reveal index.\n\nUse `GPUI.Test.range/5` to request ranges without a native display. Native range\nevents are coalesced per render cycle and use the same protocol over remote\ndisplays.","ref":"collections-and-data-views.html#source-backed-lists"},{"type":"extras","title":"Source-backed data tables - Collections and data views","doc":"`data_table/1` adds fixed headers, horizontal scrolling, explicit column widths,\nnumeric alignment, sorting events, and grid accessibility to the uniform\nsource-backed collection contract. Column definitions precede loaded rows, and\nevery row contains one child per column:\n\n```elixir\n \n   \n   \n  {Enum.map(assigns.loaded_rows, fn row ->\n    UI.table_row(%{id: row.id, children: [row.pid, row.memory]})\n  end)}\n \n```\n\nWidths are display pixels and each column is bounded from 40 to 2,000 pixels.\nSortable headers emit their stable column ID through `phx-sort`; controlled\n`sort_column` and `sort_direction` (`ascending` or `descending`) expose the\ncurrent order. Row selection emits the row ID. Cell selection and Left/Right\nnavigation emit `[row_id, column_id]` through `phx-cell-change`, while Up/Down,\nHome/End, Enter, and Space follow the source-backed row rules.\n\nThe native accessibility tree uses grid, row, column-header, and grid-cell roles\nwith one-based row and column indexes and total grid dimensions. Use\n`GPUI.Test.table_sort/4`, `GPUI.Test.table_cell_select/5`, and\n`GPUI.Test.range/5` for deterministic tests. Full row models remain in the\nsource process; snapshots contain only column definitions and the loaded row\nslice.","ref":"collections-and-data-views.html#source-backed-data-tables"},{"type":"extras","title":"Accessible trees - Collections and data views","doc":"`tree/1` and `tree_item/1` apply the same uniform-height, source-backed range,\nselection, and reveal contracts to hierarchical collections. The source emits a\nflattened visible slice; each item declares its hierarchy and accessibility\nmetadata:\n\n```elixir\n \n  {Enum.map(assigns.loaded_entries, fn entry ->\n    UI.tree_item(%{\n      id: entry.id,\n      parent_id: entry.parent_id,\n      level: entry.level,\n      branch: entry.branch?,\n      expanded: entry.expanded?,\n      position: entry.position,\n      set_size: entry.set_size,\n      children: [entry.label]\n    })\n  end)}\n \n```\n\n`phx-change` emits the selected item ID. `phx-toggle` requests controlled branch\nexpansion or collapse; the application updates its source model and visible\nslice. Left collapses an expanded branch or selects its loaded parent. Right\nexpands a collapsed branch or selects its first loaded, enabled child. Up and\nDown skip disabled items, while Home and End retain the source-backed endpoint\nrules. Native accessibility exposes tree and tree-item roles plus level,\nexpanded, selected, position-in-set, and set-size metadata.\n\nUse `GPUI.Test.change/4`, `GPUI.Test.select/4`, and `GPUI.Test.range/5` for\ndeterministic tree tests. All three event paths are forwarded unchanged by\nremote displays.","ref":"collections-and-data-views.html#accessible-trees"},{"type":"extras","title":"Source-backed code and diff viewers - Collections and data views","doc":"`code_viewer/1` specializes the uniform source-backed collection contract for\nmonospaced source text and unified diffs. Lines do not wrap. `max_columns`\nprovides stable horizontal geometry even when the longest line is not in the\nloaded slice, while `tab_width` controls deterministic tab expansion.\n\n```elixir\n \n  {Enum.map(assigns.loaded_lines, fn line ->\n    UI.code_line(%{\n      id: line.id,\n      number: line.number,\n      text: line.text,\n      kind: line.kind\n    })\n  end)}\n \n```\n\nModes are `plain` and `diff`. Diff line kinds are `addition`, `deletion`,\n`context`, and `hunk`; plain viewers can additionally use `debug`, `info`,\n`warning`, and `error` for theme-aware semantic log presentation. Line numbers\nare optional. Up/Down, Page Up/Page Down,\nHome/End, Enter, and Space use one accessible listbox tab stop and preserve the\nsource-backed endpoint rules. Pointer selection emits the stable line ID.\n\nCtrl/Cmd+C writes the selected loaded line to the clipboard on the display\nmachine. This is intentionally display-local for remote sessions. If\n`phx-copy` is set, the viewer emits an acknowledgement after requesting the\nplatform write. Tests use `GPUI.Test.select/4`, `GPUI.Test.range/5`, and\n`GPUI.Test.copy_selected_line/3`; deterministic helpers do not access the host\nclipboard.","ref":"collections-and-data-views.html#source-backed-code-and-diff-viewers"},{"type":"extras","title":"Text and editing","doc":"# Text and editing\n\nText primitives preserve renderer-independent content and coordinates while the\nnative display owns transient selection, shaping, and interaction mechanics. For\nrevisioned editable surfaces, see\n[Editable text surfaces](editable-text.html). Implementation details remain in\n[Editable text internals](editable-text-internals.html).","ref":"text-and-editing.html"},{"type":"extras","title":"Rich selectable text - Text and editing","doc":"`rich_text/1` renders consumer-produced text and shaping runs without moving\nMarkdown, HTML, syntax, or product policy into the native display. Elixir owns\nparsing and supplies plain UTF-8 text plus sorted, non-overlapping\n`GPUI.Text.RichRun` values:\n\n```elixir\nalias GPUI.Text.{Position, Range, RichRun}\n\nruns = [\n  RichRun.new(\n    Range.new(Position.new(0, 0), Position.new(0, 7)),\n    font_weight: :bold,\n    color: 0xF8FAFC\n  ),\n  RichRun.new(\n    Range.new(Position.new(1, 0), Position.new(1, 12)),\n    color: 0x60A5FA,\n    underline: 0x60A5FA,\n    link: \"message://details\"\n  )\n]\n\n \n```\n\nRun positions use zero-based `{line, utf16_offset}` coordinates and are\nconverted to native UTF-8 shaping ranges without splitting surrogate pairs.\nRuns can set foreground/background color, font weight/style, solid or wavy\nunderline, strikethrough, and an opaque bounded link value. Unstyled gaps\ninherit ordinary component text styles.\n\nSelection is transient native state. Pointer dragging paints selection, the\nplatform select-all and copy shortcuts operate on it, Escape clears it, and a\npointer press outside the component discards it. Retained content keeps an\nunchanged selected byte range across ordinary rerenders and streaming appends;\nchanging the selected bytes or removing the stateful component discards the\nselection.\n\nA link requires `phx-link` and emits its opaque value as an ordinary `:link`\nevent; the framework does not open URLs or interpret schemes. Linked rich text\nis one native Tab stop. Left/Up and Right/Down navigate links in document order,\nand Enter or Space uses the same typed event path as pointer activation.\nAccessKit currently exposes the continuous value as `Role::Document` with a\nsynthetic `Role::TextRun`, character lengths, and directional selection facts.\nPer-link AccessKit actions remain unsupported because pinned GPUI does not\nprovide public action listeners for synthetic child node IDs; the framework\ndoes not advertise synthetic clickable links it cannot activate truthfully.\n\nRich text is bounded to\n1 MiB and 2,048 runs and is serialized normally for local, test, and remote\ndisplays.","ref":"text-and-editing.html#rich-selectable-text"},{"type":"extras","title":"Editable text surfaces","doc":"# Editable text surfaces\n\n` ` is a neutral, revisioned text editor primitive backed by a\npersistent `GPUI.Text.Buffer`. Both require the `gpui_native` package; a\nrenderer-independent `gpui`-only process can carry text value types but cannot\ncreate the native Rope resource. The surface provides native keyboard, IME,\nselection,\nundo, scrolling, shaping, hit testing, and geometry mechanics without adding\nfile, language, syntax, gutter, tab, completion, or IDE policy.\n\nUse `GPUI.UI.input/1` for ordinary controlled single-line form fields. Use\n` ` when text needs persistent Rope storage, revisioned external\nedits, native undo and redo, multiline composition, or document geometry.\n\nFor implementation ownership and deferred projection work, see\n[Editable text internals](editable-text-internals.html) and\n[Native text projections](text-projections.html).","ref":"editable-text.html"},{"type":"extras","title":"Create and mount a buffer - Editable text surfaces","doc":"Create the buffer when mounting the application and retain it in view assigns:\n\n```elixir\nalias GPUI.Text.Buffer\n\n{:ok, buffer} = Buffer.new(\"# Notes\\n\\nEdit me with native keyboard input.\\n\")\n\nassigns = %{\n  buffer: buffer,\n  revision: 0,\n  focus_request: 1\n}\n```\n\nRender the persistent resource directly:\n\n```elixir\n \n```\n\nThe stable component ID retains surface interaction state across ordinary\nrerenders. The buffer owns the current Rope text, revision, selections, and\nhistory independently of any one mounted surface. Multiple surfaces may refer\nto the same buffer.\n\nNative editing updates the buffer before emitting `phx-transaction`, so a\nkeystroke does not wait for a BEAM round trip before repainting. The event's\n`value` is the applied `GPUI.Text.Transaction`, and the top-level `revision` is\nthe resulting buffer revision:\n\n```elixir\ndef handle_event(\n      \"document-transaction\",\n      %{value: transaction, revision: revision},\n      assigns\n    ) do\n  persist_or_forward(transaction)\n  {:noreply, %{assigns | revision: revision}}\nend\n```\n\nThe application may persist, acknowledge, or forward the transaction. It should\nnot replay the already-applied local transaction into the same buffer.","ref":"editable-text.html#create-and-mount-a-buffer"},{"type":"extras","title":"Positions, ranges, and selections - Editable text surfaces","doc":"Public text positions are zero-based `{line, utf16_offset}` values:\n\n```elixir\nposition = GPUI.Text.Position.new(3, 12)\nrange = GPUI.Text.Range.new(GPUI.Text.Position.new(1, 0), position)\nselection = GPUI.Text.Selection.caret(\"primary\", position, primary: true)\n```\n\nOffsets count UTF-16 code units within the logical line. This matches LSP-style\ncoordinates but is not a grapheme or UTF-8 byte index. Positions in the middle\nof a surrogate pair, beyond a line, or inside a CRLF line ending are rejected.\nCombining code points remain independently addressable.\n\nTransactions and events represent selections as a list. The current native\nsurface supports one primary selection; the plural protocol leaves room for\nfuture multi-selection mechanics without changing the transaction shape.\nSelection-only movement advances the buffer revision but does not add a document\nedit to undo history.","ref":"editable-text.html#positions-ranges-and-selections"},{"type":"extras","title":"External edits - Editable text surfaces","doc":"Application code changes text through the same revisioned transaction model:\n\n```elixir\nalias GPUI.Text.{Buffer, Edit, Position, Selection, Transaction}\n\n{:ok, snapshot} = Buffer.snapshot(buffer)\ninsert_at = Position.new(0, 0)\n\ntransaction =\n  Transaction.new(snapshot,\n    id: \"prepend-title-#{System.unique_integer([:positive])}\",\n    edits: [Edit.insert(insert_at, \"Title\\n\")],\n    selections: [Selection.caret(\"primary\", Position.new(1, 0), primary: true)]\n  )\n\n{:ok, %{revision: revision}} = Buffer.transact(buffer, transaction)\n```\n\nA transaction has a stable non-empty ID, the exact current base revision,\nnon-overlapping edits, and the complete resulting selection list. All edits are\napplied atomically against the same base text.\n\nRepeating an identical transaction ID and payload is idempotent. Reusing an ID\nwith different content returns a conflict, and applying against an old revision\nreturns a stale-revision error. Take a fresh snapshot and reconcile application\nintent rather than silently overwriting newer text.\n\nA mounted surface observes successful external edits through the shared buffer\nrevision. External transactions, undo, and redo are not echoed as local\n`phx-transaction` events.","ref":"editable-text.html#external-edits"},{"type":"extras","title":"Snapshots, undo, and redo - Editable text surfaces","doc":"A snapshot contains the current text, revision, selections, and history\navailability:\n\n```elixir\n{:ok, snapshot} = Buffer.snapshot(buffer)\n\nif snapshot.can_undo do\n  {:ok, updated} = Buffer.undo(buffer, snapshot.revision)\nend\n```\n\nRedo follows the same base-revision contract:\n\n```elixir\n{:ok, snapshot} = Buffer.snapshot(buffer)\n{:ok, updated} = Buffer.redo(buffer, snapshot.revision)\n```\n\nUndo and redo create new monotonic revisions. They never roll the revision\nnumber backward. File saving, dirty state, conflict presentation, and command\nrouting remain application concerns.","ref":"editable-text.html#snapshots-undo-and-redo"},{"type":"extras","title":"Submission and auto-growing composers - Editable text surfaces","doc":"A surface can act as a bounded multiline composer:\n\n```elixir\n \n```\n\nWith `submit_policy=\"submit\"`, plain Enter emits `phx-submit` with the current\nbuffer text and does not insert a newline. Shift+Enter remains an ordinary edit.\nThe policy requires a non-empty `phx-submit`. Submission does not clear the\nbuffer; the application must apply an external transaction after accepting the\nvalue.\n\nThe default `submit_policy=\"newline\"` treats Enter as editing. With `auto_grow`,\nthe intrinsic height follows wrapped rows from `min_lines` through `max_lines`,\nthen retains native internal scrolling. Both line bounds must be positive, at\nmost 64, and satisfy `min_lines <= max_lines`.","ref":"editable-text.html#submission-and-auto-growing-composers"},{"type":"extras","title":"Focus and scrolling requests - Editable text surfaces","doc":"Native focus is transient state, not an application-controlled boolean.\nIncrement `focus_request` to request focus once:\n\n```elixir\n{:noreply, %{assigns | focus_request: assigns.focus_request + 1}}\n```\n\nReusing the same token does not steal focus during later rerenders. Optional\n`phx-focus` and `phx-blur` events report actual native focus changes.\n\nTo move the primary caret and reveal a logical position, update both\n`scroll_to` and a monotonic `scroll_request`:\n\n```elixir\n \n```\n\nThis request changes selection and visibility through native input mechanics; it\ndoes not change document text.","ref":"editable-text.html#focus-and-scrolling-requests"},{"type":"extras","title":"Viewport, caret, range, and hit-test geometry - Editable text surfaces","doc":"Geometry events are optional and asynchronous. They are emitted only after\nnative layout exists, are tagged with the buffer revision, and are deduplicated\nwhile their bounded values remain unchanged.\n\n```elixir\n \n```\n\nDecode event values with the public structs:\n\n```elixir\ndef handle_event(\"viewport-changed\", %{value: value}, assigns) do\n  viewport = GPUI.Text.Viewport.from_event(value)\n  {:noreply, %{assigns | first_visible_row: viewport.first_visible_row}}\nend\n\ndef handle_event(\"caret-changed\", %{value: value}, assigns) do\n  caret = GPUI.Text.CaretGeometry.from_event(value)\n  {:noreply, %{assigns | caret: {caret.line, caret.utf16_offset}}}\nend\n\ndef handle_event(\"ranges-changed\", %{value: values}, assigns) do\n  ranges = Enum.map(values, &GPUI.Text.RangeGeometry.from_event/1)\n  {:noreply, %{assigns | range_geometry: ranges}}\nend\n```\n\n`GPUI.Text.Viewport` reports visible visual rows, line height, and native scroll\noffsets. `GPUI.Text.CaretGeometry` reports the primary caret's logical position\nand window-relative native-pixel rectangle. Up to 64 requested\n`geometry_ranges` return at most 256 visual rectangles per range. `phx-hit-test`\nreports the logical UTF-16 position selected by native pointer hit testing.\n\nDo not use geometry events as synchronous layout queries or infer document text\nfrom pixels.","ref":"editable-text.html#viewport-caret-range-and-hit-test-geometry"},{"type":"extras","title":"Decorations, shaping, and projections - Editable text surfaces","doc":"The application can attach bounded presentation data while retaining parser,\nlanguage, diagnostic, completion, and product policy:\n\n```elixir\n \n```\n\n- Up to 256 `GPUI.Text.Decoration` values add backgrounds or solid, dashed, and\n  wavy underlines to logical ranges.\n- Up to 512 `GPUI.Text.StyleRun` values add foreground color, font weight, and\n  font style before native shaping.\n- Up to 128 `GPUI.Text.InlineProjection` values attach bounded non-editable text\n  to logical positions.\n- Up to 64 `GPUI.Text.BlockProjection` values attach bounded before/after-line\n  overlays.\n\nDecorations and style runs do not alter text, selections, revisions, or history.\nProjection text does not enter the Rope or change document coordinates. Current\ninline and block projections are presentation annotations; they do not provide\na general layout-affecting display map. Arbitrary hidden ranges remain deferred.","ref":"editable-text.html#decorations-shaping-and-projections"},{"type":"extras","title":"Testing - Editable text surfaces","doc":"Test buffer transactions directly when behavior does not require a mounted\nsurface:\n\n```elixir\nassert {:ok, buffer} = GPUI.Text.Buffer.new(\"one two\")\nassert {:ok, %{revision: 0, text: \"one two\"}} = GPUI.Text.Buffer.snapshot(buffer)\n```\n\nUse ordinary `GPUI.Test` to assert declarative attributes and application event\nhandling. Use deterministic native tests for real input, focus, selection,\nsubmission, geometry, and reconciliation mechanics. Use desktop E2E only for\noperating-system keyboard, IME, clipboard, focus, or platform-window behavior.\n\nSee `examples/features/editable_text_surface.exs` for geometry, decorations,\nexternal edits, undo, redo, scrolling, and an application-owned gutter. See\n`examples/features/rich_transcript.exs` for a bounded submit composer.","ref":"editable-text.html#testing"},{"type":"extras","title":"Layout, styling, and presentation","doc":"# Layout, styling, and presentation\n\nStatic design values use Tailwind-compatible classes. Runtime-derived native\ngeometry uses explicit bounded attributes rather than dynamic CSS expressions.","ref":"layout-styling-and-presentation.html"},{"type":"extras","title":"Ordinary positioning versus anchored layers - Layout, styling, and presentation","doc":"The default positioning model remains HTML-like composition with\nTailwind-compatible classes. Keep content in the ordinary element tree whenever\nnormal layout and paint order are sufficient:\n\n```elixir\n \n   ... \n \n```\n\nUse the neutral ` ` primitive only as a native top-layer/portal escape\nhatch when content must paint after its ancestors or remain fitted to the native\nwindow. A layer accepts exactly one child and does not own open state,\ndismissal, focus, or menu behavior. It is not a replacement for `relative`,\n`absolute`, or inset utilities.\n\n```elixir\n \n   ... \n \n```\n\nAll static presentation inside the layer—dimensions, spacing, colors, borders,\nand typography—still belongs in classes. Runtime-derived native geometry stays\nin explicit attributes such as `position_x` and `position_y`.\n\n`position_mode=\"window\"` interprets `position_x` and `position_y` as native\nwindow-relative pixels. `position_mode=\"local\"` interprets them relative to the\nlayer's normal tree location; omitted coordinates use that location directly.\nAnchors identify which corner or edge-center of the child meets the position.\nThe fit strategy either switches anchors when possible, snaps to the window, or\nsnaps with a uniform margin. Priority is bounded to `0..1024`; it maps to GPUI's\ndeferred paint order rather than pretending to be CSS `z-index`. Consequently,\n`z-*`, `fixed`, and similar CSS utilities remain unsupported instead of being\nmapped to different GPUI behavior. Enum values retain the project's established\nunderscore convention (`bottom_left`, `snap_with_margin`), while element\ncomposition and static styling remain HTML/Tailwind-like.","ref":"layout-styling-and-presentation.html#ordinary-positioning-versus-anchored-layers"},{"type":"extras","title":"Motion - Layout, styling, and presentation","doc":"Ordinary containers support bounded entrance motion with a stable `id` and a\nmonotonic request token:\n\n```heex\n \n  ...\n \n```\n\nThe native display interpolates opacity and window-native-pixel x/y offsets to\nthe ordinary destination presentation. Reusing the token does not restart the\nanimation. Motion respects GPUI's reduced-motion preference and emits no\ncompletion event; application behavior must not depend on presentation timing.\nSee [Decision: declarative native motion](declarative-motion.html)\nfor bounds, ownership, interruption, and remote-display behavior.","ref":"layout-styling-and-presentation.html#motion"},{"type":"extras","title":"Element bounds - Layout, styling, and presentation","doc":"Ordinary containers can opt into asynchronous native bounds events when a\nconsumer needs to anchor a layer to actual layout rather than guessed\ncoordinates:\n\n```elixir\n \n  ...\n \n```\n\nThe event includes the stable element ID and window-relative native-pixel\ngeometry:\n\n```elixir\n%{\n  type: :bounds,\n  value: %{\n    id: \"completion-anchor\",\n    x: 240.0,\n    y: 118.0,\n    width: 96.0,\n    height: 28.0,\n    coordinate_space: \"window_native_pixels\"\n  }\n}\n```\n\nObservation is opt-in and requires a non-empty `id`. Native events are emitted\nonly after layout, deduplicated while geometry is unchanged, and updated after\nrerender or resize. The event is asynchronous; it does not expose GPUI layout\nIDs or provide a synchronous measurement NIF. Use the resulting geometry as\nruntime ` ` coordinates while keeping static presentation in classes.","ref":"layout-styling-and-presentation.html#element-bounds"},{"type":"extras","title":"Styling - Layout, styling, and presentation","doc":"Templates normalize a constrained Tailwind-compatible vocabulary into typed\nnative style attributes. Static layout and design values should use classes;\nreserve `style` for runtime values that cannot be known in the template.\n\nViews import compile-time hexadecimal color sigils for explicit style values:\n\n```elixir\nstyle={[background: ~RGB\"0f172a\", border_color: ~RGBA\"ffffff1a\"]}\n```\n\n`~RGB` accepts three or six hexadecimal digits; `~RGBA` accepts four or eight.\nThe sigil name replaces the leading `#`, and short literals expand each digit.\nThey produce the same `{:rgb, value}` and `{:rgba, value}` values accepted by\nthe canonical style schema.\n\nSupported groups include:\n\n- flex display, direction, wrapping, alignment, growth, shrink, the\n  `flex-1`, `flex-auto`, `flex-initial`, and `flex-none` shorthands, and basis\n  values such as `basis-1/2` or `basis-[240px]`;\n- relative and absolute positioning with pixel, percentage, fractional, full, or\n  `auto` insets through `inset-*`, `inset-x-*`, `inset-y-*`, `top-*`,\n  `right-*`, `bottom-*`, and `left-*`; numeric and arbitrary pixel/percentage\n  insets also accept Tailwind's leading-negative form;\n- foreground, background, and border colors from the complete Tailwind CSS\n  3.4.17 default palette, plus `black`, `white`, and `transparent`; named colors\n  accept percentage alpha modifiers such as `bg-black/40` and\n  `text-slate-300/70`;\n- bounded arbitrary hexadecimal colors in three-, four-, six-, or eight-digit\n  form, such as `bg-[#abc]`, `text-[#abcd]`, and `border-[#101828]`; arbitrary\n  colors also accept percentage alpha modifiers;\n- typography, weight, line height, alignment, whitespace, ellipsis/truncation,\n  and opacity, including safe arbitrary pixel and numeric values;\n- padding, margin, and gaps using Tailwind's numeric spacing convention where\n  one unit is four pixels, including decimal values such as `gap-1.5`;\n- width, height, and minimum/maximum dimensions using spacing values, arbitrary\n  pixels, percentages, or fractions such as `w-1/2`;\n- borders, border colors, and radius, including exact arbitrary pixel radii;\n- hidden overflow clipping and native cursor feedback.\n\n```elixir\n \n   Draft \n   \n    Settings\n   \n \n```\n\n` ` and ` ` carry no hidden flex, gap, or padding policy; declare\nlayout explicitly with classes. ` ` is likewise a neutral container.\n\nUse the semantic ` ` element for scrolling. An overflow utility is not\naccepted as a substitute because GPUI scrolling requires native scroll state and\nbehavior. Unsupported variants and CSS expressions, such as `hover:*`,\n`mx-auto`, or `w-[calc(...)]`, follow `GPUI.Tailwind`'s configured unknown-class\npolicy instead of being silently reinterpreted. Programmatic trees explicitly\nimport helpers such as `GPUI.Builder.style/3`, `GPUI.Builder.px/1`, and\n`GPUI.Builder.rgb/1`.","ref":"layout-styling-and-presentation.html#styling"},{"type":"extras","title":"Themes - Layout, styling, and presentation","doc":"The component theme belongs to the process-global native display environment and\nrefreshes every open native window:\n\n```elixir\n{:ok, display} = GPUI.Display.Native.start_link(theme: :dark)\n:ok = GPUI.Display.Native.set_theme(display, :light)\n```\n\nSee `GPUI.UI` for the complete option-level API and\n[Overlays and menus](overlays-and-menus.html) for named-slot components.","ref":"layout-styling-and-presentation.html#themes"},{"type":"extras","title":"Presentation primitives","doc":"# Presentation primitives\n\nPresentation primitives add bounded native decoration without moving application\nstate, topology, parsing, or accessibility policy out of Elixir. They are\nordinary schema-owned `GPUI.UI` components and serialize through the same local,\ntest, and remote snapshot path as other elements.\n\nUse these primitives for neutral presentation mechanics:\n\n- `edge_fade/1` decorates the edges around arbitrary child content;\n- `frost/1` renders a surface with an explicit solid or translucent fallback;\n- `paint/1` renders a bounded rectangle-and-line display list.\n\nThey are not native callbacks, shader APIs, plugins, or arbitrary component\ninjection. See [Presentation extension contracts](presentation-contracts.html)\nfor renderer versions, capability advertisement, and generated decoding.","ref":"presentation-primitives.html"},{"type":"extras","title":"Edge fades - Presentation primitives","doc":"An edge fade wraps arbitrary children. It does not own scrolling, clipping,\ncollection state, or content topology, so place the semantic ` ` element\ninside it when the content should scroll:\n\n```elixir\n \n   \n     Activity content \n   \n \n```\n\n`edges` is a unique subset of `:top`, `:right`, `:bottom`, and `:left`. `size`\nis bounded to `1..256` native pixels and `opacity` to `0.0..1.0`. Static\ncontainer presentation still belongs in Tailwind-compatible classes.\n\nWhen a display does not implement the optional fade enhancement, the child tree\ncontinues to render normally.","ref":"presentation-primitives.html#edge-fades"},{"type":"extras","title":"Frost surfaces - Presentation primitives","doc":"A frost surface requires an explicit fallback policy:\n\n```elixir\n \n   Summary \n \n```\n\n`fallback` is either `\"solid\"` or `\"translucent\"`, and `opacity` is bounded to\n`0.0..1.0`. The pinned native stack has no element-level backdrop-blur API, so\n`frost/1` does not claim one.\n\nAccessibility policy remains application-owned. Set `reduced_transparency` from\nthe serialized application policy; when true, it forces an opaque surface even\nif the ordinary fallback is translucent. A native display must not infer or\nreplace that policy by changing snapshot topology.","ref":"presentation-primitives.html#frost-surfaces"},{"type":"extras","title":"Bounded custom paint - Presentation primitives","doc":"`paint/1` renders a serializable display list in the bounds established by its\nclasses:\n\n```elixir\n \n```\n\nVersion 1 accepts only closed `:rect` and `:line` command maps:\n\n```elixir\n%{type: :rect, x: x, y: y, width: width, height: height, color: rgba}\n%{type: :line, x1: x1, y1: y1, x2: x2, y2: y2, width: width, color: rgba}\n```\n\nColors are 32-bit RGBA integers. The list is limited to 256 commands;\ncoordinates and dimensions are bounded, rectangle dimensions may be zero, and\nline widths must be greater than zero. Unknown commands, shader source,\ncallbacks, and opaque payloads are rejected before a snapshot reaches a\ndisplay.\n\nThe canvas fills its styled wrapper. Declare its width and height through\nclasses or other ordinary bounded layout; paint commands do not establish the\ncomponent's layout size. A display without paint support renders an empty canvas\nrather than executing an alternative payload.","ref":"presentation-primitives.html#bounded-custom-paint"},{"type":"extras","title":"Fallbacks and remote displays - Presentation primitives","doc":"Presentation support is informational and display-local. It never determines\nwhether a snapshot may mount and never permits a display to add, remove, or\nrearrange application elements. Fallbacks travel in the serialized component,\nso local and remote displays receive the same topology and application policy.\n\nPresentation support is available through the active display for diagnostics or\nintrospection. Do not branch application topology on it.","ref":"presentation-primitives.html#fallbacks-and-remote-displays"},{"type":"extras","title":"Example and testing - Presentation primitives","doc":"The complete composition example is\n`examples/features/presentation_primitives.exs`. Renderer-independent tests can\nquery these components as ordinary elements:\n\n```elixir\nruntime = start_runtime!(Features.PresentationPrimitives.App)\n\nassert %{type: :ui_frost} = runtime |> tree() |> find!(id: \"summary-frost\")\nassert %{type: :ui_edge_fade} = runtime |> tree() |> find!(id: \"activity-fades\")\nassert %{type: :ui_paint} = runtime |> tree() |> find!(id: \"sparkline\")\n```\n\nUse deterministic native tests for decoder rejection, actual GPUI layout, and\nnative presentation mechanics. Pixel-level readability and appearance remain\nvisual-evidence claims rather than renderer-independent assertions.","ref":"presentation-primitives.html#example-and-testing"},{"type":"extras","title":"Resources and display actions","doc":"# Resources and display actions\n\nFile pickers, clipboards, and external paths belong to the machine running the\n`GPUI.Display`. This matters for remote applications: a user's display machine\nmay not share a filesystem or clipboard with the BEAM host.\n\nGPUI transfers bounded facts rather than product policy. The framework does not\ninfer MIME types, open dropped paths, upload attachments, traverse directories,\nor decide whether application code should trust a selected resource.","ref":"resources-and-display-actions.html"},{"type":"extras","title":"Display-machine ownership - Resources and display actions","doc":"Treat every external path as meaningful only on the display machine. Application\ncode remains responsible for:\n\n- file type and size validation;\n- reading files and handling permissions;\n- upload and attachment lifecycle;\n- MIME and application-format interpretation;\n- project-relative path conversion;\n- command execution and security policy.\n\nWhen an application needs portable bytes rather than a display-local path, use\nthe bounded file-picker action described below. It reads on the display machine\nand emits bytes through the normal event path.","ref":"resources-and-display-actions.html#display-machine-ownership"},{"type":"extras","title":"Read a selected file - Resources and display actions","doc":"`phx-file-read` on `button/1` opens the platform picker on the display machine,\nreads one selected file, and emits bytes rather than a path:\n\n```elixir\n \n```\n\n`file_max_bytes` defaults to 10 MiB and is bounded to `1..25 MiB`. The event\nvalue is one of:\n\n```elixir\n%{\n  operation_id: 42,\n  status: :selected,\n  name: \"photo.png\",\n  size: 12_345,\n  data: encoded_bytes\n}\n\n%{operation_id: 42, status: :cancelled}\n%{operation_id: 42, status: :error, reason: \"permission denied\"}\n```\n\nThe stable operation ID distinguishes separate picker requests. `name` is the\nselected display-side basename, not an application-authoritative identity.\nValidate the bytes and format before using them.\n\nIf the button also declares clipboard and ordinary click events, clipboard\noperations run first, then the file read, then `phx-click`.","ref":"resources-and-display-actions.html#read-a-selected-file"},{"type":"extras","title":"Read and write clipboard text - Resources and display actions","doc":"An explicit clipboard-read action retrieves bounded text from the display\nmachine:\n\n```elixir\n \n```\n\nThe handler receives a canonical public payload:\n\n```elixir\ndef handle_event(\n      \"clipboard-read\",\n      %{value: %GPUI.Transfer.Payload{text: text, external_paths: []}},\n      assigns\n    ) do\n  {:noreply, %{assigns | pasted_text: text}}\nend\n```\n\nClipboard text is valid UTF-8 and bounded to 1 MiB. A non-text, empty, or\noversized clipboard produces `text: nil`; arbitrary MIME bytes are not exposed.\nWhen `phx-click` is also present, the clipboard event is emitted first.\n\nWrite bounded text by pairing `clipboard_text` with\n`phx-clipboard-write`:\n\n```elixir\n \n```\n\nThe named event acknowledges that the platform write was requested. It does not\nturn clipboard contents into application-owned state or promise that another\nprocess has consumed them.\n\nNative editable text keeps its immediate platform copy, paste, selection, and\nIME behavior. Use explicit clipboard payload events only when application policy\nneeds to inspect text before accepting it.","ref":"resources-and-display-actions.html#read-and-write-clipboard-text"},{"type":"extras","title":"External path drops - Resources and display actions","doc":"`GPUI.UI.drop_target/1` receives external-path drag sessions without reading the\nfiles:\n\n```elixir\n \n   Drop files here \n \n```\n\nHandlers receive a normalized `GPUI.Transfer.Event`:\n\n```elixir\ndef handle_event(\n      \"attachments-dropped\",\n      %{value: %GPUI.Transfer.Event{} = transfer},\n      assigns\n    ) do\n  paths = transfer.payload.external_paths\n  {:noreply, %{assigns | pending_display_paths: paths}}\nend\n```\n\nA transfer event contains:\n\n```elixir\n%GPUI.Transfer.Event{\n  session_id: 42,\n  target_id: \"attachments\",\n  position: {320.0, 180.0},\n  coordinate_space: :window_native_pixels,\n  payload: %GPUI.Transfer.Payload{\n    text: nil,\n    external_paths: [\"/display/tmp/document.pdf\"]\n  }\n}\n```\n\n`:drag_enter` and `:drop` include the bounded payload. `:drag_move` carries the\nlatest position without repeating paths and may be display-side coalesced.\n`:drag_leave` terminates the session. The renderer-owned monotonic session ID\nprevents stale movement or leave events from being associated with a later\ndrag.\n\nPayload bounds are:\n\n| Value | Bound |\n| --- | ---: |\n| Text | 1 MiB |\n| Unique external paths | 64 |\n| One UTF-8 path | 4 KiB |\n| All unique paths | 256 KiB |\n| Stable target ID | 128 bytes |\n\nDuplicate paths are removed in first-seen order. Invalid or unrepresentable\nUTF-8 paths are rejected rather than converted lossily.","ref":"resources-and-display-actions.html#external-path-drops"},{"type":"extras","title":"Remote behavior - Resources and display actions","doc":"File reads, clipboard operations, and drops occur where the display runs. Remote\npaths are never rewritten as BEAM-host paths. Bounded bytes and canonical event\nvalues travel through the ordinary remote protocol.\n\nRemote clipboard and external-path support use additive capabilities. A peer\nthat did not advertise the relevant capability cannot send the corresponding\nevent. Capabilities are renegotiated after reconnection before queued transfer\nevents resume.\n\nApplications should not branch their window or element topology based on where\nthe display runs. Provide an ordinary controlled fallback when a display cannot\nperform an optional platform action.","ref":"resources-and-display-actions.html#remote-behavior"},{"type":"extras","title":"Images and raster resources - Resources and display actions","doc":"`GPUI.Image.decode/1` converts common encoded image bytes into a validated\n`GPUI.Raster`. Decoding runs on a dirty CPU scheduler; keep file access and\nlarger workflows in supervised tasks:\n\n```elixir\nwith {:ok, raster} <- GPUI.Image.decode(encoded_bytes) do\n  GPUI.Runtime.put_resource(runtime, \"preview\", GPUI.Raster.to_payload(raster))\nend\n```\n\nRender a one-off inline raster with:\n\n```elixir\n \n```\n\n`label` provides the native accessibility name. For images retained across\nmultiple view updates, install the raster once and render a reference:\n\n```elixir\n \n```\n\nResource references avoid copying the complete pixel payload through each later\nsnapshot and work across local and remote displays.","ref":"resources-and-display-actions.html#images-and-raster-resources"},{"type":"extras","title":"Deterministic testing - Resources and display actions","doc":"Renderer-independent helpers exercise application policy without opening native\npickers or touching the host clipboard:\n\n```elixir\nfile_select(runtime, \"image-selected\", \"fixture.png\", encoded_png)\nfile_cancel(runtime, \"image-selected\")\n```\n\nConstruct typed transfer values directly when testing drop policy:\n\n```elixir\npayload = GPUI.Transfer.Payload.new(external_paths: [\"/display/tmp/report.csv\"])\n\ntransfer = %GPUI.Transfer.Event{\n  session_id: 17,\n  target_id: \"attachments\",\n  position: {20.0, 30.0},\n  coordinate_space: :window_native_pixels,\n  payload: payload\n}\n```\n\nUse renderer-independent tests for bounds, normalization, routing, and\napplication decisions. Use desktop E2E for real clipboard, picker, and external\ndrop behavior. Deterministic native tests may cover target layout and routing,\nbut they do not establish operating-system transfer facts.\n\nSee [Transfer payload internals](transfers.html) for the pinned native capability\nanalysis, renderer session mechanics, remote capability enforcement, and\nintentionally deferred formats.","ref":"resources-and-display-actions.html#deterministic-testing"},{"type":"extras","title":"Windows and lifecycle","doc":"# Windows and lifecycle\n\nWindow topology remains application-owned. Window specifications declare\nplatform constraints, while optional view callbacks decide how lifecycle\nrequests affect application state.","ref":"windows-and-lifecycle.html"},{"type":"extras","title":"Window lifecycle - Windows and lifecycle","doc":"Window declarations contain only declarative platform constraints:\n\n```elixir\nwindow \"Workspace\" do\n  size 1100, 720\n  min_size 760, 480\n  resizable true\n  chrome :content\n  root WorkspaceView\nend\n```\n\nViews opt into lifecycle handling through an idiomatic optional behaviour\ncallback rather than arbitrary routing strings:\n\n```elixir\ndef handle_window_event(:close_request, _event, assigns) do\n  {:noreply, %{assigns | close_dialog_open: true}}\nend\n\ndef handle_window_event(:focus, _event, assigns) do\n  {:noreply, %{assigns | window_active: true}}\nend\n\ndef handle_window_event(:blur, _event, assigns) do\n  {:noreply, %{assigns | window_active: false}}\nend\n```\n\nExporting `handle_window_event/3` enables interception and activation delivery\nfor the view's window. A close request remains asynchronous. Return\n`{:noreply, assigns}` to keep the window open—for example while rendering an\napplication-owned confirmation dialog—or `{:close, assigns}` to approve closure.\nOrdinary `handle_event/3` handlers may also return `{:close, assigns}` after a\nconfirmation action.\n\n`min_size`, `resizable`, and `chrome` are declarative creation options interpreted by the\nnative display. `chrome :system` is the default; `chrome :content` extends\napplication content into native chrome and enables ordinary containers marked\nwith `window_control=\"drag|close|maximize|minimize\"`. Creation-option changes\nreopen the native window while retaining its session ID. See\n[Decision: declarative native window chrome](window-chrome.html)\nfor platform behavior and fallback rules. The existing `:window_closed` event remains an internal final\nnotification after a window actually closes. Window `:focus` and `:blur`\ncallbacks report native activation and are separate from element focus.","ref":"windows-and-lifecycle.html#window-lifecycle"},{"type":"extras","title":"Commands and keyboard shortcuts","doc":"# Commands and keyboard shortcuts\n\nGPUI windows can bind application-owned command IDs to modified keyboard\nshortcuts. A command uses the same event ID as a button or menu action, so the\nroot view keeps one handler regardless of how the user invokes it.","ref":"commands-and-shortcuts.html"},{"type":"extras","title":"Declare window shortcuts - Commands and keyboard shortcuts","doc":"Declare shortcuts next to the window rather than inside transient rendered\ncontrols:\n\n```elixir\nwindow \"Repository\" do\n  size(1200, 760)\n  shortcut(\"reload_repository\", \"primary-r\")\n  shortcut(\"focus_repository_filter\", \"primary-f\")\n  root(MyApp.RepositoryView, filter: \"\", filter_focus_request: 0)\nend\n```\n\n`primary` means Command on macOS and Control on other platforms. `ctrl`, `alt`,\nand `shift` may also be combined, for example `primary-shift-k`. Shortcuts must\ncontain `primary`, `ctrl`, or `alt` plus one lowercase key. Each window supports\nat most 64 commands, and command IDs and shortcuts must be unique.\n\nCommand definitions are renderer-independent window metadata. They cross local\nand remote display boundaries with the rest of the snapshot while native GPUI\nperforms platform keystroke matching.","ref":"commands-and-shortcuts.html#declare-window-shortcuts"},{"type":"extras","title":"Share command IDs with controls - Commands and keyboard shortcuts","doc":"A button can emit the same ID:\n\n```elixir\n \n```\n\nBoth the button click and `primary-r` reach the same handler:\n\n```elixir\n@impl GPUI.View\ndef handle_event(\"reload_repository\", _event, assigns) do\n  {:noreply, %{assigns | scan_status: :scanning}}\nend\n```\n\nKeyboard commands use event type `:command`; buttons use `:click`. The event ID\nand view handler are shared. Native command observation runs after normal GPUI\nkey handling and does not intercept propagation. Editing shortcuts handled by a\nfocused input therefore retain their native behavior instead of also invoking a\nwindow command.","ref":"commands-and-shortcuts.html#share-command-ids-with-controls"},{"type":"extras","title":"Focus commands - Commands and keyboard shortcuts","doc":"Combine a command with a monotonic input focus request:\n\n```elixir\n@impl GPUI.View\ndef handle_event(\"focus_repository_filter\", _event, assigns) do\n  {:noreply, %{assigns | filter_focus_request: assigns.filter_focus_request + 1}}\nend\n```\n\n```elixir\n \n```\n\nThe repository workspace, process table, and log stream examples use this\npattern for refresh, pause, clear, and filter-focus commands.","ref":"commands-and-shortcuts.html#focus-commands"},{"type":"extras","title":"Deterministic tests - Commands and keyboard shortcuts","doc":"`GPUI.Test.command/3` invokes the same application handler without Rust or a\ndisplay server:\n\n```elixir\ncommand(runtime, \"reload_repository\")\nassert %{scan_status: :scanning} = assigns(runtime)\n```\n\nUse native E2E tests only for platform shortcut matching and focus-sensitive\ninteraction.","ref":"commands-and-shortcuts.html#deterministic-tests"},{"type":"extras","title":"Overlays and menus","doc":"# Overlays and menus\n\n`GPUI.UI.Overlay` contains controlled native overlays. Public templates use\nordinary HEEx named slots; internal slot nodes remain an implementation detail.\n\n```elixir\nalias GPUI.UI\nalias GPUI.UI.Overlay\n```","ref":"overlays-and-menus.html"},{"type":"extras","title":"Tooltips - Overlays and menus","doc":"Tooltips accept one arbitrary trigger and one textual content slot:\n\n```elixir\n \n  <:trigger> </:trigger>\n  <:content>Save the current document</:content>\n \n```\n\n`delay` is measured in milliseconds from `0` through `60_000`. Tooltip content\nis intentionally textual because the upstream native tooltip owns a separate\nview lifecycle.","ref":"overlays-and-menus.html#tooltips"},{"type":"extras","title":"Popovers - Overlays and menus","doc":"Popovers accept arbitrary GPUI trees in both named slots:\n\n```elixir\n \n  <:trigger> </:trigger>\n  <:content>\n     \n       Account settings \n     \n  </:content>\n \n```\n\nThe required `label` names the popover trigger and its controlled expanded\nstate. Popover state is controlled by `open`. Pointer interaction and\nEnter/Space can request a state change. Escape and outside clicks dismiss\nclosable popovers and restore focus. Set `closable={false}` to disable outside-click dismissal.\n\nAnchors are `top_left`, `top_center`, `top_right`, `bottom_left`,\n`bottom_center`, `bottom_right`, `left_center`, and `right_center`.","ref":"overlays-and-menus.html#popovers"},{"type":"extras","title":"Dialogs - Overlays and menus","doc":"Dialogs are modal and can be opened by a trigger or entirely through assigns:\n\n```elixir\n \n  <:trigger> </:trigger>\n  <:content>\n     \n  </:content>\n \n```\n\nThe required non-empty `title` supplies the dialog heading and names an optional\ntrigger. The content slot accepts arbitrary GPUI content. Native dialogs expose\na dialog role, trap focus, restore prior focus, and support configurable Escape,\noverlay dismissal, close buttons, and width. The trigger slot is optional for programmatic dialogs.","ref":"overlays-and-menus.html#dialogs"},{"type":"extras","title":"Dropdown menus - Overlays and menus","doc":"Dropdown menus use an arbitrary trigger and repeated textual item slots:\n\n```elixir\n \n  <:trigger> </:trigger>\n  <:item value=\"new\">New file</:item>\n  <:item value=\"open\" checked={assigns.recent}>Open recent</:item>\n  <:item value=\"delete\" disabled={true}>Delete</:item>\n \n```\n\nThe required `label` names the menu trigger and its controlled expanded state.\nItem values must be non-empty and unique. Selection emits the value through\n`phx-select`; open-state changes use `phx-change`. The upstream popup menu\nprovides menu roles, checked state, disabled-item interaction, arrow-key\nnavigation, Enter confirmation, Escape dismissal, outside-click dismissal, and\nfocus restoration.\n\nA typical view handles both events explicitly:\n\n```elixir\ndef handle_event(\"file_menu_open_changed\", %{value: open}, assigns),\n  do: {:noreply, %{assigns | file_menu_open: open}}\n\ndef handle_event(\"file_menu_selected\", %{value: action}, assigns) do\n  {:noreply, %{assigns | file_menu_open: false, selected_action: action}}\nend\n```","ref":"overlays-and-menus.html#dropdown-menus"},{"type":"extras","title":"Controlled reconciliation - Overlays and menus","doc":"Elixir assigns remain authoritative, while persistent native registry entries\npreserve transient state between snapshots. A local interaction is applied\nimmediately and recorded until the corresponding controlled snapshot arrives.\nStale snapshots are ignored rather than visibly undoing the interaction.\n\nRemoving an overlay from the tree removes its registry entry. Stable IDs must\nnot be reused for a different component kind in the same tree.","ref":"overlays-and-menus.html#controlled-reconciliation"},{"type":"extras","title":"Remote displays","doc":"# Remote displays\n\nGPUI can run the Elixir application session in one node or process and present\nits windows through a native display client elsewhere. The application remains\nrenderer-independent; snapshots and normalized events cross the transport.","ref":"remote-displays.html"},{"type":"extras","title":"Topology - Remote displays","doc":"`GPUI.Remote.Server` is an OTP supervision tree. It owns a supervised acceptor,\nper-connection subtrees, and one isolated session subtree per mounted client.\nEach connection subtree owns its protocol process, connection owner, and task\nsupervisor. Each session subtree owns bounded request workers, its coordinator,\nand its `GPUI.Session`; losing an essential child tears down that whole isolated\nsubtree. Session work is delegated out of the central server and connection\nowner, so a slow mount or event in one session does not delay unrelated\nsessions—even when requests share one transport connection.\n`GPUI.Remote.Client` negotiates the protocol, mounts the application, forwards\nsnapshots to its configured display, and sends display events back to the\nserver.\n\nThe transport uses SafeRPC over framed TCP or SSL with request IDs, timeouts,\ncapability negotiation, and safe ETF decoding. A protocol `hello` is required\nbefore other operations.","ref":"remote-displays.html#topology"},{"type":"extras","title":"Start a server - Remote displays","doc":"```elixir\n{:ok, server} =\n  GPUI.Remote.Server.start_link(\n    app: MyApp.Desktop,\n    port: 5050,\n    max_in_flight_requests_per_connection: 64,\n    max_in_flight_requests_per_session: 16\n  )\n```\n\n`GPUI.Remote.Server` has a supervisor child specification, so production\napplications can place the same options directly in their root tree:\n\n```elixir\nchildren = [\n  {GPUI.Remote.Server, app: MyApp.Desktop, port: 5050, name: MyApp.RemoteServer}\n]\n\nSupervisor.start_link(children, strategy: :one_for_one)\n```\n\nEach client gets its own supervised session and root assigns. A disconnected\nclient cannot mutate another client's application state. The request limits\nshown above are the defaults; both accept positive values up to 4,096.","ref":"remote-displays.html#start-a-server"},{"type":"extras","title":"Start a native display client - Remote displays","doc":"```elixir\n{:ok, client} =\n  GPUI.Remote.Client.start_link(\n    host: \"127.0.0.1\",\n    port: 5050,\n    display: GPUI.Display.Native,\n    poll_interval: 16\n  )\n\n{:ok, snapshot} = GPUI.Remote.Client.mount(client)\n```\n\n`GPUI.Remote.Client` likewise supports `{GPUI.Remote.Client, options}` in a\ndisplay-side supervision tree. The native event loop remains local to the\ndisplay client. Only declarative\nsnapshots, resources, events, and protocol operations cross the connection.\nDisplay-side file pickers read bounded bytes on the client and send those bytes\nwith an operation ID; they never claim that a client-local path is readable by\nthe application server. Clipboard buttons likewise write to the display\nclient's clipboard. Application-side filesystem work has the opposite\nsemantics: paths identify files on the application server. Applications must\nlabel that distinction explicitly instead of presenting a server-local\ndirectory as a remote-client directory. Source-backed virtual-list range requests also\noriginate on the display and cross the same event channel, allowing the server\nto return only an overscanned slice.\n\nRemote display updates use the same typed OTP message shape as local runtimes:\n\n```elixir\n:ok = GPUI.Remote.Client.subscribe(client)\n\nreceive do\n  {:gpui, ^client, %GPUI.Runtime.Update{events: events, snapshot: snapshot}} ->\n    # The snapshot is already synchronized to the client's display.\nend\n```\n\nAfter synchronizing a remote snapshot, callers can wait for its local native\nframe with `GPUI.Remote.Client.await_frame/3`. Native-only changes use\n`GPUI.Remote.Client.frame_token/2` and `await_frame_after/4`. The remote client\nremains responsive while either display barrier is pending. Subscribers are\nmonitored and removed when they exit.","ref":"remote-displays.html#start-a-native-display-client"},{"type":"extras","title":"SSL - Remote displays","doc":"A server can use a certificate and key:\n\n```bash\nGPUI_REMOTE_SSL=1 \\\nGPUI_REMOTE_SSL_CERTFILE=/path/to/server.pem \\\nGPUI_REMOTE_SSL_KEYFILE=/path/to/server.key \\\nGPUI_APP_PORT=5050 \\\nmix run examples/remote/app_server.exs\n```\n\nThe client uses a CA certificate and optional SNI name:\n\n```bash\nGPUI_REMOTE_SSL=1 \\\nGPUI_REMOTE_SSL_CACERTFILE=/path/to/ca.pem \\\nGPUI_REMOTE_SSL_SERVER_NAME=localhost \\\nGPUI_APP_HOST=localhost \\\nGPUI_APP_PORT=5050 \\\nmix run examples/remote/display_client.exs\n```","ref":"remote-displays.html#ssl"},{"type":"extras","title":"Failure behavior - Remote displays","doc":"Requests have explicit IDs and timeouts. Negotiation rejects mismatched current\nprotocol versions or missing capabilities before mounting a session. The\nversion identifies the one wire shape implemented by the current source tree;\nno older private-project wire revisions are retained. See\n[Platform support and development status](platform-support.html)\nfor the current package and protocol policy. Mounts and events carry stable operation IDs,\nso retrying after a lost reply does not mount twice or apply the same event\ntwice. A disconnected session remains resumable until its configured session\nTTL expires; terminating a connection immediately removes only its connection\nowner.\n\nDisplay events drained during an outage are retained in a bounded 1,024-event\nclient queue and retried in order after reconnection. If that bound is exceeded,\nthe newest events are retained. Poll timers use generation tokens, while each\nsession coordinator owns and resets its expiry timer directly. Stale timer\nmessages cannot create duplicate polling loops or expire a recently active\nsession. Work above either configured in-flight limit is rejected with\n`{:error, :overloaded}` before it reaches the session. Automatically forwarded\ndisplay events remain in the client's bounded queue and retry on a later poll.\nClient reconnect behavior remains isolated from the native window loop and does\nnot create shared application state.\n\nRemote transport and protocol tests live under `test/integration/`; local and\nremote native behavior is also exercised under Xvfb in `test/e2e/`.","ref":"remote-displays.html#failure-behavior"},{"type":"extras","title":"Testing GPUI applications","doc":"# Testing GPUI applications\n\n`GPUI.Test` supports three complementary ExUnit layers. Choose the lowest layer\nthat can truthfully prove the behavior:\n\n| Test layer | Use it for | Do not claim |\n| --- | --- | --- |\n| Renderer-independent `use GPUI.Test` | Elixir state, callbacks, validation, snapshots, topology, and protocol policy | GPUI layout, native focus, or OS behavior |\n| Deterministic `use GPUI.Test, native: ...` | GPUI layout, bounds, hit testing, focus, keyboard dispatch, and component mechanics | OS clipboard, IME, compositor, or real-window behavior |\n| Repository desktop E2E | Window-server, accessibility-adapter, clipboard, IME, external transfer, and compositor facts | A replacement for focused component tests |\n\nPixel-level appearance belongs to synchronized visual capture rather than a\nbehavioral assertion.","ref":"overview.html"},{"type":"extras","title":"Choose a guide - Testing GPUI applications","doc":"- [Application tests](application-tests.html) covers fast renderer-independent\n  view and runtime tests, tree queries, semantic event helpers, and repository\n  test layout.\n- [Deterministic native tests](native-tests.html) covers real GPUI component\n  mechanics through `TestAppContext` without a desktop window.\n- [Desktop E2E and visual evidence](desktop-e2e.html) covers platform-aware\n  orchestration, real native windows, and synchronized captures.\n- [Test coverage ownership](coverage-ownership.html) is the authoritative\n  behavior-by-layer matrix.\n\nConfigure consumers with:\n\n```elixir\nconfig :gpui_native, build_native: config_env() != :test\n```\n\nThis keeps ordinary application tests free of GPUI compilation and native\nlibrary loading. The repository isolates the ordinary, deterministic-native,\nand desktop native artifacts, so switching modes does not require deleting a\nshared NIF.","ref":"overview.html#choose-a-guide"},{"type":"extras","title":"Avoid duplicated coverage - Testing GPUI applications","doc":"Do not duplicate deterministic component assertions in desktop E2E. Once native\ninteraction coverage proves a component mechanic, retain only the\nplatform-specific smoke assertion in E2E. Conversely, renderer-independent\nevent injection does not prove focus, hit testing, keyboard dispatch, or native\nlayout.","ref":"overview.html#avoid-duplicated-coverage"},{"type":"extras","title":"Full quality gate - Testing GPUI applications","doc":"Run the normal project gate with:\n\n```bash\nmix ci\n```\n\nIt checks generated Rust freshness, Cargo formatting and feature matrices,\nClippy with warnings denied, Rust unit tests, ExUnit, Credo, Dialyzer,\nduplication, and architecture policy.\n\nReal-window coverage is deliberately separate because it requires platform\norchestration:\n\n```bash\nmix gpui.test.e2e apps/gpui_native/test/e2e/gpui/native\n```\n\nWhen changing native modes or validating artifact isolation, run:\n\n```bash\nmix gpui.test.mode_switch\n```\n\nSee [Desktop E2E and visual evidence](desktop-e2e.html) for focused\ncommands and platform requirements.","ref":"overview.html#full-quality-gate"},{"type":"extras","title":"Application tests","doc":"# Application tests\n\nRenderer-independent tests prove Elixir application behavior without compiling\nor loading GPUI. They are the default for state transitions, callbacks,\nvalidation, snapshots, topology, event policy, and supervised integration.\n\nUse asynchronous ExUnit where the application under test permits it:\n\n```elixir\ndefmodule MyApp.FocusTimerTest do\n  use GPUI.Test, async: true\n\n  test \"starts and receives OTP ticks\" do\n    runtime = start_runtime!(GettingStarted.FocusTimer.App, args: %{seconds: 2})\n\n    click(runtime, \"start\")\n    send_view(runtime, :tick)\n\n    assert %{remaining: 1, status: :running} = assigns(runtime)\n  end\nend\n```\n\n`start_runtime!/2` starts a supervised `GPUI.Runtime` backed by\n`GPUI.Test.Display`. Events pass through the same runtime polling and session\ndispatch boundaries used by a real display.","ref":"application-tests.html"},{"type":"extras","title":"Render and query views - Application tests","doc":"Render a view directly when runtime behavior is not needed:\n\n```elixir\ntree = render(MyApp.ProfileView, name: \"Ada\", editing: false)\nassert %GPUI.Element{type: :ui_input} = find!(tree, id: \"name\")\nassert [_button] = all(tree, type: :ui_button)\n```\n\nRuntime helpers expose snapshots, selected windows, assigns, and rendered trees:\n\n```elixir\nsnapshot = snapshot(runtime)\nwindow = window_snapshot(snapshot, \"Settings\")\nassigns = assigns(runtime)\ntree = tree(runtime)\n```\n\n`render/2` returns the original `%GPUI.Element{}` tree, while `tree/2` returns\nthe snapshot-encoded map sent across display boundaries. `find/2`, `find!/2`,\nand `all/2` intentionally query either representation; pattern matches should\nuse the corresponding struct or map shape.","ref":"application-tests.html#render-and-query-views"},{"type":"extras","title":"Dispatch semantic events - Application tests","doc":"Use the helper matching the component contract:\n\n```elixir\nclick(runtime, \"save\")\ncommand(runtime, \"reload_repository\")\nchange(runtime, \"name_changed\", \"Ada\")\nsubmit(runtime, \"save\", \"Ada\")\nselect(runtime, \"language_changed\", \"elixir\")\nsearch(runtime, \"framework_searched\", \"live\")\nchange(runtime, \"notifications_changed\", true)\nchange(runtime, \"details_changed\", [\"account\"])\nchange(runtime, \"volume_changed\", 75.0)\nrelease(runtime, \"volume_released\", 75.0)\nchange(runtime, \"dialog_open_changed\", true)\nfile_select(runtime, \"source_selected\", \"fixture.png\", encoded_png)\nfile_cancel(runtime, \"source_selected\")\nrange(runtime, \"records_range\", 1_000, 1_048)\n```\n\nAll helpers return the resulting snapshot. `dispatch/2` remains available for a\ncustom explicit typed event:\n\n```elixir\ndispatch(runtime, %{\n  type: :select,\n  window_id: 1,\n  event: \"file_menu_selected\",\n  value: \"new\"\n})\n```\n\nEvery routed event requires a positive `window_id`, a non-empty `event` name,\nand the value shape required by its `type`; a missing `type` never defaults to\n`:click`. Convenience helpers construct those fields for tests.\n\n`send_view/3` delivers an OTP message through `GPUI.Runtime.send_view/3`, making\nbackground-process behavior deterministic without running timers in a test.","ref":"application-tests.html#dispatch-semantic-events"},{"type":"extras","title":"What this layer proves - Application tests","doc":"Application tests should own:\n\n- `mount/1`, `handle_event/3`, `handle_info/2`, and window callback behavior;\n- controlled assigns and authoritative rerenders;\n- schema, template, and payload validation;\n- snapshot topology and dynamic windows;\n- source-backed range and selection policy;\n- remote protocol, reconnection, and synchronization policy;\n- text-buffer transactions that do not require a mounted native surface.\n\nThey do not prove where GPUI laid out an element, whether native focus moved,\nwhether a key reached a component, or whether the operating system completed a\nclipboard or IME operation. Use [Deterministic native tests](native-tests.html)\nor [Desktop E2E and visual evidence](desktop-e2e.html) for those facts.","ref":"application-tests.html#what-this-layer-proves"},{"type":"extras","title":"Repository test layout - Application tests","doc":"The repository separates tests by purpose:\n\n- `test/gpui/` contains focused units, deterministic view-state tests,\n  schema/template tests, and deterministic native interaction tests;\n- `test/integration/gpui/examples/` contains example source processes,\n  Logger/ETS/filesystem work, cancellation, and other cross-process flows;\n- `test/integration/` contains remaining runtime, transport, and remote flows;\n- `apps/gpui_native/test/e2e/` contains real native-window tests;\n- `test/support/` contains reusable displays and native drivers.\n\nRun focused renderer-independent layers with:\n\n```bash\nmix test test/gpui test/gpui_test.exs\nmix test test/integration\n```\n\nTests under `test/gpui/test/native/` are selected by the dedicated native-test\nMix target and are not part of the ordinary renderer-independent command.","ref":"application-tests.html#repository-test-layout"},{"type":"extras","title":"Deterministic native tests","doc":"# Deterministic native tests\n\nUse deterministic native tests when behavior depends on GPUI layout, focus, hit\ntesting, keyboard dispatch, or real `gpui-component` mechanics but not on a\ndesktop window or operating-system integration.\n\n```elixir\ndefmodule MyApp.SettingsNativeTest do\n  use GPUI.Test, native: [size: {320, 160}]\n\n  test \"radio navigation skips disabled choices\", %{ui: ui} do\n    render(ui, SettingsView, plan: \"free\", notifications: false)\n\n    focus(ui, \"plan\")\n    press(ui, :arrow_right)\n\n    assert_receive {:gpui, ^ui,\n                    {:event,\n                     %{type: :change, event: \"plan_changed\", value: \"team\"}}}\n\n    # Controlled state remains authoritative in Elixir.\n    render(ui, SettingsView, plan: \"team\", notifications: false)\n  end\nend\n```\n\n`use GPUI.Test, native: ...` marks the module `:native`, requires synchronous\nExUnit execution, starts a supervised UI for each test, and supplies its opaque\nhandle as `%{ui: ui}`.","ref":"native-tests.html"},{"type":"extras","title":"Interaction vocabulary - Deterministic native tests","doc":"The imported command set is deliberately small:\n\n```elixir\nrender(ui, SettingsView, plan: \"free\")\nfocus(ui, \"plan\")\npress(ui, :space)\nclick(ui, \"notifications\")\nclick(ui, {24, 48})\ndrag(ui, {80, 12}, {240, 12})\nscroll(ui, \"records\", delta: {0, -240})\ntype(ui, \"Ada\")\nresize(ui, {800, 600})\nadvance(ui, 250)\nassert %{width: width, height: height} = bounds(ui, \"notifications\")\nsettle(ui)\n```\n\nStable targets come from declarative `id` attributes. `click/2` targets the\ncenter of rendered bounds or an explicit `{x, y}` logical point. `bounds/2`\nreturns `%{x:, y:, width:, height:}` in logical pixels. `drag/3` simulates a\nleft-button press, movement, and release between two logical points and settles\nnative work. Derive those points from `bounds/2` rather than OS window coordinates.\n`scroll/3` dispatches a\nbounded logical-pixel wheel delta at the target center. `type/2` dispatches text\ninput to the focused control, while `resize/2` changes the deterministic\nviewport.\n\n`advance/2` moves GPUI's test clock by a bounded number of milliseconds and\nsettles pending work. `press/2` accepts semantic keys such as `:arrow_left`,\n`:arrow_right`, `:arrow_up`, `:arrow_down`, `:space`, `:enter`, `:escape`, and\n`:tab`, or a GPUI keystroke string.","ref":"native-tests.html#interaction-vocabulary"},{"type":"extras","title":"Assert ordinary events - Deterministic native tests","doc":"Native events use the ExUnit mailbox rather than a custom assertion language:\n\n```elixir\nclick(ui, \"notifications\")\n\nassert_receive {:gpui, ^ui,\n                {:event,\n                 %{type: :change,\n                   event: \"notifications_changed\",\n                   value: true}}}\n```\n\nThe Elixir test owns fixtures, assigns, actions, controlled rerenders, and\nassertions. Rust remains a generic interpreter around GPUI's `TestAppContext`;\ncomponent-specific fixtures and assertions do not belong in the native harness.","ref":"native-tests.html#assert-ordinary-events"},{"type":"extras","title":"Choosing the right test layer - Deterministic native tests","doc":"Keep control behavior in this harness: edits, change/release events, controlled\nrerenders, disabled states, focus, and layout do not require OS mouse automation.\nExtend an existing native test rather than duplicate it in desktop E2E. Use\n`advance/2` instead of sleeping for animations.\n\nDesktop E2E should cover actual OS event delivery, window chrome/lifecycle, and\nrepresentative visual captures. Set themes explicitly and use matching surfaces\nwith visible labels and values; a process-alive assertion is not a visual check.\n\nFor asynchronous runtime integration tests, subscribe before triggering the\noperation, then use `assert_receive` on the relevant `%GPUI.Runtime.Update{}`\nmessage. Assert its committed snapshot rather than repeatedly querying current\nstate. Pin the runtime identity and match the specific transition; unrelated\nupdates are not completion acknowledgements. Keep actions outside wait loops.","ref":"native-tests.html#choosing-the-right-test-layer"},{"type":"extras","title":"Run native tests - Deterministic native tests","doc":"```bash\nmix gpui.test.native\nmix gpui.test.native test/gpui/test/native/controls_test.exs\nmix gpui.test.native test/gpui/my_settings_native_test.exs\n```\n\nThe task selects `MIX_TARGET=native_test`, leaving ordinary `MIX_ENV=test`\nrenderer-independent. RustQ generates the NIF exports and Elixir stubs for the\nclosed native-test command boundary.\n\nTo verify ordinary, deterministic-native, and desktop artifact isolation without\ncleaning build directories or deleting NIFs, run:\n\n```bash\nmix gpui.test.mode_switch\n```","ref":"native-tests.html#run-native-tests"},{"type":"extras","title":"What this layer proves - Deterministic native tests","doc":"Deterministic native tests own facts such as:\n\n- stable GPUI layout bounds and hit testing;\n- pointer activation and wheel delivery into components;\n- native focus and keyboard navigation;\n- disabled and loading interaction suppression;\n- controlled-value reconciliation after native edits;\n- source-backed range requests and collection transitions;\n- dialog top-layer mechanics through the real `gpui_component::Root`;\n- real `gpui_component::InputState` typing and submit behavior;\n- bounded animation state through the deterministic GPUI clock.\n\nThe harness installs the production-like hierarchy:\n\n```text\ngpui_component::Root\n└── ElixirRoot\n```\n\nThis permits real dialog, input, and component mechanics without alternate\ntest-only product implementations.\n\nThe pinned GPUI revision includes\n[zed-industries/zed#62775](https://github.com/zed-industries/zed/pull/62775), so\n`TestWindow` reports `raw_window_handle::HandleError::NotSupported` instead of\npanicking when a component requests an unavailable raw window or display handle.\nNative content-type integration can therefore degrade cleanly in this harness.","ref":"native-tests.html#what-this-layer-proves"},{"type":"extras","title":"What remains desktop-owned - Deterministic native tests","doc":"These tests do not prove operating-system facts. Keep the following in\n[Desktop E2E and visual evidence](desktop-e2e.html):\n\n- real native window creation and application-owned chrome;\n- OS close requests and process-global activation;\n- accessibility-adapter behavior;\n- clipboard and external file transfer;\n- native IME and platform content types;\n- compositor output and synchronized pixels;\n- real-window focus containment and restoration.\n\nOnce deterministic coverage proves a component mechanic, desktop E2E should\nretain only the platform-specific smoke assertion rather than duplicating the\nfull interaction sequence.","ref":"native-tests.html#what-remains-desktop-owned"},{"type":"extras","title":"Desktop E2E and visual evidence","doc":"# Desktop E2E and visual evidence\n\nDesktop E2E runs ordinary ExUnit against real native windows. Use it only for\nfacts owned by the operating system, window server, accessibility adapter,\nclipboard, IME, external transfer machinery, or compositor.\n\n```elixir\ndefmodule MyApp.NativeWindowTest do\n  use ExUnit.Case, async: false\n\n  alias GPUITest.Desktop\n\n  @moduletag :e2e\n\n  setup context do\n    Desktop.setup(context, [])\n  end\nend\n```\n\nDesktop tests are serial because native windows share process-global pointer,\nkeyboard, focus, and desktop state.","ref":"desktop-e2e.html"},{"type":"extras","title":"Run through the local orchestrator - Desktop E2E and visual evidence","doc":"Use the platform-aware Mix task rather than assembling environment variables by\nhand:\n\n```sh\nmix gpui.test.e2e apps/gpui_native/test/e2e/gpui/native/motion_test.exs\nmix gpui.test.e2e apps/gpui_native/test/e2e/gpui/native\n```\n\nOn Linux, the task checks `xvfb-run`, D-Bus, and `xdotool`, then runs with Xvfb\nand Lavapipe. On macOS, it checks Accessibility and Screen Recording permission,\nbuilds the Swift desktop driver, and uses the active WindowServer and real Metal\nrenderer. The repository-owned E2E support is intentionally separate from the\npublic `GPUI.Test` API: it drives operating-system facilities and is not shipped\nin the Hex packages. Both paths execute ordinary ExUnit through\n`GPUITest.Desktop`; there is no separate test-runner architecture.\n\nThe source-built native artifacts for ordinary, deterministic-native, and\ndesktop modes are isolated. Verify the transition sequence without cleaning\nbuild directories or deleting NIF artifacts:\n\n```sh\nmix gpui.test.mode_switch\nmix gpui.test.mode_switch apps/gpui_native/test/e2e/gpui/native/form_controls_test.exs\n```\n\nThe task runs ordinary ExUnit, deterministic native ExUnit, one focused desktop\nE2E, ordinary ExUnit again, and deterministic native ExUnit again.","ref":"desktop-e2e.html#run-through-the-local-orchestrator"},{"type":"extras","title":"Linux environment - Desktop E2E and visual evidence","doc":"Install the Linux desktop-test dependencies with:\n\n```bash\nsudo apt-get install xvfb xdotool libxkbcommon-dev libxkbcommon-x11-dev\n```\n\nThe orchestrator is preferred, but the underlying ExUnit command is:\n\n```bash\nMIX_ENV=e2e RUST_FONTCONFIG_DLOPEN=1 LIBGL_ALWAYS_SOFTWARE=1 \\\n  GALLIUM_DRIVER=llvmpipe xvfb-run -a dbus-run-session -- \\\n  mix test --only e2e apps/gpui_native/test/e2e\n```\n\nMesa Lavapipe provides software rendering and `xdotool` supplies XTest pointer\nand keyboard input. The suite must not require a desktop environment or window\nmanager.\n\nOn a workstation with an existing display, `MIX_ENV=e2e mix test ...` can run\nthe ExUnit layer directly, but it remains the caller's responsibility to provide\na suitable desktop session and permissions.","ref":"desktop-e2e.html#linux-environment"},{"type":"extras","title":"What desktop E2E owns - Desktop E2E and visual evidence","doc":"The suite should focus on:\n\n- native window creation, resizing, activation, and closure;\n- platform chrome and content-chrome behavior;\n- real pointer, wheel, keyboard, and focus delivery;\n- native IME and content-type integration;\n- OS selection and clipboard behavior;\n- external file drops and display-machine file pickers;\n- accessibility adapters and platform actions;\n- remote-native window smoke behavior;\n- compositor stability and target-window capture.\n\nDo not repeat full component-navigation or controlled-rerender suites already\ncovered by [Deterministic native tests](native-tests.html). A\nrenderer-independent injected event also does not substitute for desktop input.","ref":"desktop-e2e.html#what-desktop-e2e-owns"},{"type":"extras","title":"Visual capture - Desktop E2E and visual evidence","doc":"Behavioral E2E assertions are not pixel-level design review. Deterministic\ncapture definitions live under `test/visual/scenarios/`; they support the\ncapture tool rather than acting as independent test runners.\n\nCapture one scenario or the complete suite with:\n\n```bash\nRUST_FONTCONFIG_DLOPEN=1 mix gpui.visual.capture \\\n  --scenario component_gallery \\\n  --theme dark \\\n  --output tmp/gpui-visual-dark\n\nRUST_FONTCONFIG_DLOPEN=1 mix gpui.visual.capture \\\n  --scenario beam_control_room \\\n  --theme dark \\\n  --output tmp/beam-observatory-visual\n\nRUST_FONTCONFIG_DLOPEN=1 mix gpui.visual.capture \\\n  --all \\\n  --output tmp/gpui-visual\n```\n\nEach scenario declares its application, initial arguments, window title,\ncapture names, and synchronized actions such as event dispatch, root-view\nmessages, and hover. The generic runner owns Xvfb startup, completed-frame\nbarriers, pointer movement, file naming, and cleanup. Scenarios must not sample\nlive state when stable screenshots are required.\n\nInspect images for:\n\n- spacing and alignment;\n- clipping and overflow;\n- contrast and hierarchy;\n- popup placement;\n- focus visibility;\n- theme and state variants.\n\nThe repository-only `GPUITest.Desktop.capture!/2` helper remains available to\nfocused E2E tests. It does not wait, inspect environment variables, or choose\noutput paths.","ref":"desktop-e2e.html#visual-capture"},{"type":"extras","title":"Coverage boundaries - Desktop E2E and visual evidence","doc":"Visual evidence can establish what rendered pixels look like in a declared\nscenario. It does not prove application state policy, and deterministic native\nbounds do not prove readability. Keep each claim in the layer that can observe\nit truthfully; see [Test coverage ownership](coverage-ownership.html) for the\nauthoritative matrix.","ref":"desktop-e2e.html#coverage-boundaries"},{"type":"extras","title":"Test coverage ownership","doc":"# Test coverage ownership\n\nGPUI behavior is divided by what each layer can truthfully prove.\n\n| Behavior | Renderer-independent ExUnit | Deterministic native ExUnit | Desktop E2E |\n| --- | --- | --- | --- |\n| Controlled application state and rerenders | Primary | Exercises real payload rerenders | Smoke only |\n| Switch and radio activation | Event policy | Pointer, focus, keyboard, disabled/loading | Real window rendering |\n| Tabs | Controlled event handling | Stable bounds, focus, keyboard, disabled | Real window rendering |\n| Accordion | Controlled expanded IDs and validation | Stable bounds, pointer toggles, controlled multiple expansion, disabled-group suppression | Real window rendering |\n| Tree and data table navigation | Source/state policy | Focus, keyboard, disabled skipping, emitted IDs | Virtualized real window and pointer delivery |\n| Uniform and variable collections | Range/source policy | Selection, ranges, scrolling, transitions | Compositor range and wheel delivery |\n| Dialog and overlays | Controlled open/close policy | Stable trigger bounds, keyboard open requests, native `gpui_component::Root` ownership, and Escape closure | Real window focus containment, content activation, and restoration |\n| Rich text | Runs, ranges, links, controlled events | Shaping and link-navigation Rust unit coverage; stable public target pending | Real shaped rendering |\n| Persistent editable text | Buffer, revisions, transactions | Controlled input typing, reconciliation, submit events, geometry, and projection mechanics through real `InputState` | Native IME, content types, OS selection and clipboard |\n| Clipboard and external transfer | Bounded payload and event policy | Boundary/routing unit coverage | OS clipboard and external transfer facts |\n| Remote display | Protocol, synchronization and reconnect policy | Not applicable | Real remote-native window smoke |\n| Pixel appearance and contrast | Not applicable | Not a behavioral assertion | Synchronized visual capture |\n\nWhen moving an assertion between layers, preserve the behavior in its owning layer.\nDo not retain coordinate-heavy desktop assertions for component mechanics already\ncovered deterministically. Conversely, deterministic tests must not claim OS\nclipboard, IME, compositor, accessibility-adapter, or external-drop behavior.","ref":"coverage-ownership.html"},{"type":"extras","title":"Native builds and deployment","doc":"# Native builds and deployment\n\nThe published `gpui_native` package owns the RustlerPrecompiled loader and two\nfixed, complete native hosts. `gpui` remains renderer-independent, while the\nseparately installed `gpui_components` package provides conventional component\nschemas and Elixir builders for applications that select the `:gpui_component`\nhost.","ref":"native-builds.html"},{"type":"extras","title":"Native hosts - Native builds and deployment","doc":"Both release hosts share one generated NIF boundary and one exact GPUI type\nuniverse:\n\n- `:vanilla` — a complete host built from vanilla GPUI and neutral primitives;\n- `:gpui_component` — a complete host that statically links the separate\n  `gpui-component` renderer library, assets, conventional controls, overlays,\n  and theme integration.\n\nSelect one host at compile time:\n\n```elixir\nconfig :gpui_native, GPUI.Native, host: :vanilla\n# or\nconfig :gpui_native, GPUI.Native, host: :gpui_component\n```\n\nExactly one final `cdylib` is loaded into `Elixir.GPUI.Native.NIF`. These are\nalternative complete hosts, not a core dylib followed by a dynamically attached\ncomponent plugin. The component-capable host statically composes its Rust\nlibraries against the same pinned GPUI graph so windows, entities, resources,\nand the event loop never cross a Rust shared-library ABI.","ref":"native-builds.html#native-hosts"},{"type":"extras","title":"Consumer compilation - Native builds and deployment","doc":"On a supported precompiled target, compilation proceeds as follows:\n\n1. Mix resolves `gpui_native` and its exact matching `gpui` release.\n2. `gpui` compiles the generated backend-neutral `GPUI.Native` facade.\n3. `gpui_native` reads the configured host at compile time.\n4. `RustlerPrecompiled` selects the host variant, NIF ABI, and target triple.\n5. It downloads the release archive, verifies its compressed SHA-256 against\n   `checksum-Elixir.GPUI.Native.NIF.exs`, extracts it, and loads the NIF.\n6. No RustQ, Cargo, Rust compiler, Node runtime, or source checkout is required.\n\nRustQ runs only in the source umbrella. Its generated Rust decoders and Elixir\nstubs/facade are committed before release.","ref":"native-builds.html#consumer-compilation"},{"type":"extras","title":"Current platform status - Native builds and deployment","doc":"Source builds and native window lifecycle behavior are validated on:\n\n- `x86_64-unknown-linux-gnu` under X11 with Xvfb and Mesa Lavapipe;\n- `aarch64-apple-darwin` with AppKit on ERTS's original process main thread;\n- `x86_64-pc-windows-msvc` with the dedicated native GUI-thread host.\n\nPublished precompiled hosts target `x86_64-unknown-linux-gnu`,\n`aarch64-apple-darwin`, and `x86_64-pc-windows-msvc`. Linux release artifacts\nare built on Ubuntu 22.04 and must not require GLIBC symbols newer than\n`GLIBC_2.35`; macOS archives are built and load-tested on Apple silicon;\nWindows archives are built with MSVC and load-tested on x86-64 Windows. Native\nWindows applications must be launched in an interactive desktop session;\nWindows OpenSSH sessions are suitable for package and NIF checks but not for\ncreating desktop windows. Intel macOS remains a source-build development target\nuntil matching native runtime evidence and release artifacts pass.","ref":"native-builds.html#current-platform-status"},{"type":"extras","title":"Source builds - Native builds and deployment","doc":"`gpui_native` packages the complete native workspace at\n`apps/gpui_native/native`. The repository pins Rust 1.95.0 in\n`rust-toolchain.toml`.\n\nForce source compilation with:\n\n```bash\nGPUI_BUILD_FROM_SOURCE=1 mix compile\n```\n\nDisable native compilation for renderer-independent tooling with:\n\n```bash\nGPUI_SKIP_NATIVE=1 mix compile\n```\n\nThe application-level configuration is:\n\n```elixir\nconfig :gpui_native, build_native: config_env() != :test\n```\n\nOn Linux install:\n\n```bash\nsudo apt-get install libxkbcommon-dev libxkbcommon-x11-dev\n```\n\nOn macOS install full Xcode and its Metal Toolchain. On Windows use the MSVC\nRust target with Visual Studio 2022 Build Tools and a current Windows SDK.\n`yeslogic-fontconfig-sys` can load Fontconfig dynamically:\n\n```bash\nRUST_FONTCONFIG_DLOPEN=1 GPUI_BUILD_FROM_SOURCE=1 mix compile\n```","ref":"native-builds.html#source-builds"},{"type":"extras","title":"Native development configurations - Native builds and deployment","doc":"Repository CI checks:\n\n- core-only, without real GPUI;\n- headless real GPUI, without desktop linker dependencies;\n- vanilla desktop GPUI without component integration;\n- `gpui-component` desktop host with component integration;\n- deterministic native-test and real desktop E2E configurations.\n\n`ZED_HEADLESS=1` selects real GPUI's headless backend for lifecycle work, but\nthat backend cannot open a platform window.","ref":"native-builds.html#native-development-configurations"},{"type":"extras","title":"Precompiled release flow - Native builds and deployment","doc":"The coordinated package tag `vVERSION` builds both host variants through\n`.github/workflows/precompiled-nif.yml`. Release archives include the host in\nthe RustlerPrecompiled variant suffix: `--vanilla` or `--gpui-component`.\nChecksums cover compressed `.tar.gz` bytes.\n\nThe release sequence is:\n\n1. run `mix ci` from the umbrella root;\n2. run native desktop ExUnit under Xvfb/Lavapipe on Linux;\n3. run `RUST_FONTCONFIG_DLOPEN=1 mix gpui.release.check`;\n4. tag the coordinated release and build both native hosts;\n5. attest and publish the GitHub release archives;\n6. generate `apps/gpui_native/checksum-Elixir.GPUI.Native.NIF.exs` from the\n   published bytes;\n7. validate a clean consumer with Cargo and Rust absent from `PATH`;\n8. publish `gpui`, then `gpui_components`, then `gpui_native` to Hex.\n\nThe public packages initially use one coordinated version and exact sibling\nrequirements. Package versions remain distinct from versioned presentation\ncontracts such as `paint@1`.","ref":"native-builds.html#precompiled-release-flow"},{"type":"extras","title":"Release validation - Native builds and deployment","doc":"Release validation verifies all three Hex package contents independently,\ndocumentation, dependency audits, RustQ freshness, source-build fallback,\nnative host artifacts, and a no-Rust precompiled consumer. Linux GNU artifacts\nretain the `GLIBC_2.35` ceiling.\n\nZed's Apache-licensed `sum_tree` uses only the `ztracing::instrument` surface,\nbut Zed declares its tracing facade as GPL-3. GPUI patches that facade with\n`apps/gpui_native/native/compat/ztracing`, which re-exports the upstream\nMIT/Apache `tracing` API. `gpui-component` is Apache-2.0.\n\nNative E2E remains a separate evidence layer. Release claims cover only\nplatforms validated with real windows and interaction.","ref":"native-builds.html#release-validation"},{"type":"extras","title":"Platform support and development status","doc":"# Platform support and development status\n\nGPUI is a private, unreleased project. It has no supported historical public\nAPI, wire payload, snapshot format, native artifact, or platform matrix.\nInternal contracts may change directly while the architecture is being built.\nVersion fields validate the components that are running now; they do not imply\nsupport for older project revisions.","ref":"platform-support.html"},{"type":"extras","title":"Runtime requirements - Platform support and development status","doc":"Development currently uses Elixir `~> 1.20` and the Rust toolchain pinned in\n`rust-toolchain.toml`. Renderer-independent sessions, remote servers, and\n`GPUI.Test` do not require a Rust toolchain, native library, or display server.\nNative source builds require the pinned Rust toolchain.\n\nThese are current development requirements, not release guarantees.","ref":"platform-support.html#runtime-requirements"},{"type":"extras","title":"Platform validation - Platform support and development status","doc":"The intended first distribution target is x86-64 GNU/Linux under X11. Native\ninteraction tests run under Xvfb with Mesa Lavapipe when a Linux host is\navailable.\n\nApple silicon macOS and x86-64 Windows are precompiled targets with native\nbuild, artifact inspection, NIF loading, and runtime startup evidence. Windows\nnative windows must run in an interactive desktop session rather than a Windows\nOpenSSH session. Linux ARM, musl, Intel macOS, Windows ARM, and other target\ntriples are not currently validated.\n\nSee [Native builds and deployment](native-builds.html) for current prerequisites\nand the proposed artifact process.","ref":"platform-support.html#platform-validation"},{"type":"extras","title":"Architectural boundary - Platform support and development status","doc":"The renderer-independent application pipeline is:\n\n```text\nGPUI.Application\n→ GPUI.WindowSpec\n→ GPUI.Session\n→ GPUI.Snapshot\n→ GPUI.Display\n```\n\nApplication callback tuples, event payloads, topology errors, text coordinates,\nand display errors remain authoritative in Elixir. Generated Rust files, native\nrenderer structs, GPUI entities, AccessKit internals, and RustQ code-generation\nmodules are implementation details.\n\nSnapshots are ephemeral renderer-independent runtime messages, not durable\nfiles. Applications persist domain state and render fresh snapshots.","ref":"platform-support.html#architectural-boundary"},{"type":"extras","title":"Remote protocol validation - Platform support and development status","doc":"The internal remote protocol version identifies the one wire shape implemented\nby the current source tree. Negotiation requires that exact version and the\nrequired peer capabilities so mismatched development processes fail before\nmounting a session.\n\nCapabilities describe optional behavior within the current wire shape. They do\nnot preserve older protocol revisions. When the protocol changes before the\nfirst release, the implementation, fixtures, and version may be changed or\nreplaced directly; no historical peer version is retained unless a concrete\ndevelopment need requires it.\n\nPresentation support advertisement is informational and cannot change mount\neligibility or application topology.","ref":"platform-support.html#remote-protocol-validation"},{"type":"extras","title":"Native and accessibility boundaries - Platform support and development status","doc":"The native NIF and pinned GPUI stack are implementation details. Elixir code\nmust not depend on generated Rust layouts or native renderer symbols.\n\nAccessibility claims are limited to the semantics and actions documented in\n[Accessibility](accessibility.html). GPUI does\nnot claim arbitrary ARIA, DOM behavior, or uniform screen-reader behavior across\noperating systems.","ref":"platform-support.html#native-and-accessibility-boundaries"},{"type":"extras","title":"Release policy - Platform support and development status","doc":"Public stability, semantic-versioning guarantees, deprecation periods, retained\nwire versions, supported artifacts, and a platform matrix will be defined when\nthe first release is prepared. Until then, optimize for a coherent design,\ntruthful behavior, and strong tests rather than hypothetical compatibility.","ref":"platform-support.html#release-policy"},{"type":"extras","title":"Editable text internals","doc":"# Editable text internals\n\nGPUI exposes editable text as neutral model and renderer primitives. It does not\nprovide an IDE, editor shell, document tab, language client, gutter, completion\nmenu, or `UI.code_editor` component. Consumers compose those policies from the\nsame low-level elements used by other applications.","ref":"editable-text-internals.html"},{"type":"extras","title":"Design sources and licensing - Editable text internals","doc":"The design follows useful architectural lessons from Zed's `text`, `editor`,\n`multi_buffer`, selection, and display-map code without depending on those\ncrates. Zed's GPUI crate is Apache-2.0, while its `text`, `editor`, `language`,\n`project`, and `multi_buffer` crates are GPL-3.0-or-later. Importing the latter\nwould change the distribution obligations of this library.\n\nThe pinned Apache-2.0 `gpui-component` input implementation provides useful\nimplementation machinery: Rope text, native input and IME, edit history,\nselection painting, scrolling, search, folding, highlighting, and decoration\npainting. Its convenience `InputState::code_editor` API and embedded language\nprovider assumptions do not define GPUI's public Elixir API.","ref":"editable-text-internals.html#design-sources-and-licensing"},{"type":"extras","title":"Ownership boundary - Editable text internals","doc":"Elixir owns application policy:\n\n- document identity, file persistence, dirty state, and conflict handling;\n- projects, tabs, pane topology, commands, and keymaps;\n- parser and language-service processes;\n- diagnostics, completions, navigation, formatting, and workspace edits.\n\nNative state owns latency-sensitive mechanics:\n\n- Rope text storage and monotonic revisions;\n- immediate edits, selections, undo, and redo;\n- later, IME composition, shaping, hit testing, and viewport geometry.\n\nA keystroke must not wait for a BEAM round trip before the native surface can\nrepaint. The BEAM receives revisioned transactions and can acknowledge, persist,\nor reconcile them asynchronously.","ref":"editable-text-internals.html#ownership-boundary"},{"type":"extras","title":"Coordinate system - Editable text internals","doc":"Protocol positions are zero-based `{line, utf16_offset}` values. The offset is a\nUTF-16 code-unit offset within the logical line and excludes its line ending.\nThis matches LSP coordinates while allowing the native implementation to keep\nbyte offsets internally.\n\nConversions must reject:\n\n- missing lines;\n- columns beyond the logical line;\n- positions in the middle of a surrogate pair;\n- ranges whose end precedes their start;\n- overlapping edits in one transaction.\n\nCRLF is one logical line ending. Its carriage return is not addressable as line\ncontent. Combining code points remain independently addressable because UTF-16\ncoordinates do not imply grapheme coordinates; renderer hit testing may clamp\nto grapheme boundaries separately.","ref":"editable-text-internals.html#coordinate-system"},{"type":"extras","title":"Model protocol - Editable text internals","doc":"The neutral model consists of:\n\n- `GPUI.Text.Position`\n- `GPUI.Text.Range`\n- `GPUI.Text.Edit`\n- `GPUI.Text.Selection`\n- `GPUI.Text.Transaction`\n- `GPUI.Text.Snapshot`\n- `GPUI.Text.Buffer`\n\nSelections are plural from protocol version one even though the first native\nimplementation supports a single primary selection. This avoids replacing the\nprotocol when multi-cursor editing is introduced.\n\nA transaction has a stable ID, a required base revision, an origin, atomic\nnon-overlapping edits, and the complete post-transaction selection set. A\nsuccessful transaction increments the revision exactly once. Repeating the same\ntransaction ID with the same payload is idempotent; reusing it for different\ncontent is rejected. A transaction based on any other revision is stale.\n\nUndo and redo are new monotonic revisions, never revision rollback. External\nedits use the same transaction mechanism as local edits and therefore preserve a\nsingle consistency model.","ref":"editable-text-internals.html#model-protocol"},{"type":"extras","title":"First milestone - Editable text internals","doc":"The first milestone intentionally has no visual surface. It provides a\npersistent native Rope resource with:\n\n- snapshots;\n- atomic revisioned edits;\n- one primary selection represented as a list;\n- bounded undo and redo history;\n- stale-revision and duplicate-delivery handling;\n- UTF-16/byte conversion with Unicode and CRLF coverage.\n\nIt does not know about files, saving, languages, syntax highlighting, or IDEs.","ref":"editable-text-internals.html#first-milestone"},{"type":"extras","title":"Renderer primitive - Editable text internals","doc":"The low-level ` ` primitive refers directly to a persistent\n`GPUI.Text.Buffer`. It keeps immediate keyboard and IME changes native, updates\nthe Rope before notifying Elixir, and emits revisioned transaction and plural\nselection events. Selection-only movement advances the shared buffer revision but\ndoes not create document undo entries or clear redo history. A monotonically\nchanging `focus_request` value requests focus without turning focus into\napplication-owned boolean state.\n\nThe primitive accepts generic input behavior such as soft wrapping, whitespace\ndisplay, tab width, hard tabs, and disabled state. With `auto_grow`, bounded\n`min_lines` and `max_lines` select the native input's intrinsic wrapped-row\nheight; after `max_lines` it retains native internal scrolling. The bounds are\npart of the declarative snapshot and must satisfy `min_lines <= max_lines`.\n\n`submit_policy=\"newline\"` leaves Enter and Shift+Enter as ordinary edits.\n`submit_policy=\"submit\"` requires `phx-submit` and makes plain Enter emit that event\nwith the current buffer text without inserting a newline, while Shift+Enter\ncontinues to insert one. Submission uses the native input action path, so active\nIME composition is handled by the input before application submission; Elixir\nremains responsible for clearing or otherwise mutating the buffer afterward.\n\nIt deliberately does not live\nunder `GPUI.UI` and does not render line numbers, tabs, diagnostics, completion\nmenus, or status bars. External buffer transactions, undo, and redo are\nreconciled into a mounted surface from the buffer revision without echoing them\nas local input.\n\nThe first geometry contract emits revision-tagged, deduplicated viewport and\nprimary-caret facts through optional `phx-viewport-change` and\n`phx-geometry-change` events. `GPUI.Text.Viewport` reports visible visual rows\nand native scroll offsets; `GPUI.Text.CaretGeometry` reports zero-based UTF-16\nlogical position plus window-relative native pixel bounds. Geometry is emitted\nonly after layout exists and only when its bounded value changes.\n\nThe first bounded request contract accepts up to 64 declarative\n`geometry_ranges` and emits currently laid-out bounds through\n`phx-range-geometry-change`. `GPUI.Text.RangeGeometry` preserves the requested\nlogical range and reports a bounded `GPUI.Text.Rectangle` for every visual row\ncrossed by wrapped text. Results remain limited to 64 requested ranges and 256\nrectangles per range.\n\nA monotonically increasing `scroll_request` token paired with `scroll_to` moves\nthe native primary caret to an explicit UTF-16 position and lets the existing\nnative input machinery reveal it without changing document text. Optional\n`phx-hit-test` events report the post-click logical UTF-16 position selected by\nnative text hit testing. Neither contract introduces synchronous pointer NIF\ntraffic.\n\nThe first decoration contract accepts up to 256 `GPUI.Text.Decoration` values.\nEach decoration attaches an optional RGB background and/or solid, dashed, or\nwavy RGB underline to a logical UTF-16 range. Native painting uses current\nlayout geometry while the consumer retains diagnostic severity, language,\nmessage, command, and service policy. Decorations do not alter text, selections,\nrevisions, or history.\n\nA separate shaping contract accepts up to 512 `GPUI.Text.StyleRun` values.\nEach run carries optional RGB foreground color, font weight, and font style for\na logical UTF-16 range. Runs are converted to native byte ranges and composed\ninto GPUI Component's text-decoration collections before line shaping, so font\nfallback, ligatures, wrapping, bidi layout, and glyph metrics use the styled\nruns directly. They are not painted as duplicate glyph overlays. Consumer code\nretains syntax, language, token, and diagnostic policy.\n\nThe first projection contract accepts up to 128 bounded\n`GPUI.Text.InlineProjection` values, each containing at most 4096 bytes. A\nprojection anchors non-editable display text and an RGB color to an explicit\nUTF-16 position. Projection text never enters the Rope, changes selection\ncoordinates, participates in history, or implies completion behavior.\n\nThe block projection contract accepts up to 64 bounded\n`GPUI.Text.BlockProjection` values containing at most 16384 bytes. Blocks are\nanchored before or after a logical line and carry explicit height, foreground,\nand optional background colors. They are overlay annotations: they do not add\nrows to native text layout or alter document coordinates. Consumers can use\nthem for notes and previews while retaining application policy.\n\nThe demonstration's gutter is now entirely consumer-owned: Elixir derives the\nvisible line-number elements, row heights, and fractional vertical offset from\n`GPUI.Text.Viewport`. The renderer still exposes no gutter or editor shell.\n\nThe feature demonstration includes application-owned external edit, undo, and\nredo controls so reconciliation can be exercised without treating persistence\nor document commands as renderer policy.","ref":"editable-text-internals.html#renderer-primitive"},{"type":"extras","title":"Later renderer primitives - Editable text internals","doc":"Independent projection inputs will eventually cover:\n\n- ranged decorations;\n- hidden ranges;\n- inline insertions;\n- block insertions;\n- visual-row and viewport geometry;\n- caret and range bounds;\n- point-to-position hit testing.\n\nTrue layout-affecting inline and block insertions remain deferred. The pinned\nnative input has no projection collection that participates consistently in\nwrapping, scroll extent, geometry, hit testing, and document/display mapping;\nsee [Native text projections](text-projections.html).\n\nConsumers can use those inputs to build code editors, prose editors, notebooks,\ndiff tools, terminal prompts, or domain-specific annotated text surfaces without\nadopting library-owned IDE policy.","ref":"editable-text-internals.html#later-renderer-primitives"},{"type":"extras","title":"Accessibility internals","doc":"# Accessibility internals\n\nGPUI snapshots are renderer-independent, but programmatic accessibility only\nbecomes real when a display maps snapshot semantics into its platform\naccessibility tree and handles assistive-technology actions. This guide records\nthe boundary for the pinned native stack:\n\n```text\nGPUI:           7bddd16a09cf0084cefb3d98468b178343b9f1e2\ngpui-component: 5c5eb1db04dc1146bdec903a8ac74407bd6c2098\n```\n\nIt is an implementation boundary, not a promise of web ARIA parity.","ref":"accessibility.html"},{"type":"extras","title":"What the native stack provides - Accessibility internals","doc":"The pinned GPUI integrates with AccessKit. A GPUI element contributes a node\nonly when it has both a stable element ID and an AccessKit role. GPUI currently\nexposes native builders for:\n\n- role, label, description, and announced keyboard shortcuts;\n- selected, expanded, and tri-state toggled state;\n- string, placeholder, numeric, minimum, maximum, and step values;\n- orientation, heading level, position/size in set, and table coordinates;\n- active-descendant focus for composite widgets;\n- synthetic accessibility children for custom text-like elements;\n- assistive-technology actions, with click handlers automatically accepting the\n  accessible click action.\n\nGPUI sends AccessKit trees and actions through platform adapters on:\n\n- macOS via `accesskit_macos`;\n- Windows via `accesskit_windows`;\n- Linux X11 and Wayland via `accesskit_unix` (the platform bridge used for\n  AT-SPI environments).\n\nThis means bounded semantic metadata can truthfully reach native assistive\ntechnology on all three target families. Actual behavior still depends on the\nplatform adapter, desktop accessibility services, and the completeness of each\ncomponent's role, identity, state, and action wiring.","ref":"accessibility.html#what-the-native-stack-provides"},{"type":"extras","title":"What gpui_ex already maps - Accessibility internals","doc":"The native renderer already emits accessibility nodes for several semantic\ncomponents:\n\n- images: image role and label;\n- buttons: component-owned button/link role and label behavior;\n- inputs: input-specific roles, labels, values, and placeholders; password\n  values are not exposed;\n- select and combobox: roles, labels, selected visible values, and placeholders;\n- switch and radio group: controlled state, labels, and orientation;\n- slider and progress/display values: numeric value bounds and orientation;\n- popover, dropdown, and dialog triggers: label and expanded state;\n- tree, virtual list, code viewer, and data table: collection roles, selected or\n  expanded state, active descendants, set positions, and table coordinates.\n\nThese mappings are component policy. Applications should not need to recreate\nnative roles for ordinary semantic controls.","ref":"accessibility.html#what-gpui_ex-already-maps"},{"type":"extras","title":"Renderer-independent contract direction - Accessibility internals","doc":"Accessibility facts added to the public schema must remain bounded,\nserializable, and useful to every display. The first neutral vocabulary should\nbe limited to facts directly supported by GPUI/AccessKit:\n\n```text\naccessibility_role\naccessibility_label\naccessibility_description\naccessibility_value\naccessibility_selected\naccessibility_expanded\naccessibility_checked\naccessibility_orientation\n```\n\nNumeric value metadata, heading/set/table coordinates, active descendants, and\nsynthetic text children should be added only with a concrete component need and\nbehavioral tests. Native semantic elements keep their implicit roles; explicit\nrole overrides are primarily for custom generic elements and must use a closed\nrole vocabulary.\n\nAccessibility metadata belongs in the element snapshot. Therefore test and\nremote displays can inspect and transport the same facts even though only a\nnative display publishes an operating-system accessibility tree.\n\nThe first neutral schema slice is available on generic container primitives:\n\n```elixir\n \n```\n\nExplicit metadata requires a non-empty stable `id`, and labels or descriptions\nrequire an explicit role. Roles use a closed vocabulary and labels/descriptions\nare bounded to 512/2048 bytes. Semantic native components retain their\ncomponent-owned roles; this generic contract does not override them. These\nfacts remain in the renderer-independent snapshot, survive remote transport,\nand map to GPUI's AccessKit role, label, and description builders.\n\nThe closed vocabulary, metadata detection, stable identity requirement, and\ncross-field name/description rules are centralized in `GPUI.Accessibility`.\nFuture selected, expanded, checked, value, and orientation attributes must add\ntheir role/state rules there rather than duplicating policy in serialization or\nnative rendering.\n\nThe state vocabulary now also includes:\n\n```text\naccessibility_value\naccessibility_selected\naccessibility_expanded\naccessibility_checked\naccessibility_orientation\n```\n\nChecked values are `true`, `false`, or `:mixed`; orientation is `horizontal` or\n`vertical`. Checked is restricted to checkbox/radio/switch, selected to list\nitems/tabs/tree items, expanded to buttons/tree items, orientation to\nsliders/splitters/tab lists/trees, and string values to progress/sliders/text\nboxes. State changes retain the same element ID and travel through ordinary\nlocal or remote snapshots before mapping to AccessKit.\n\nThe Elixir specification is also the RustQ source for native role, checked, and\norientation enums, the grouped `AccessibilitySemantics` struct, bounded\ndecoders, and GPUI conversion matches. Handwritten Rust only applies those\ngenerated typed values to GPUI's accessibility builders; it does not repeat the\ntransport vocabulary.\n\nGeneric elements with `phx-click` register both GPUI pointer activation and an\nexplicit AccessKit `Click` action listener. Both callbacks call the same native\nevent helper and therefore produce the ordinary `NativeEvent::Click` route;\nthere is no accessibility-specific Elixir callback or state mutation. A role\nwithout `phx-click` remains semantic and does not register an accessible Click\naction.\n\nRole specifications classify interaction as structural, simple activation,\ncomposite, or value-oriented. RustQ generates the native exhaustive\n`is_activatable` match. Generic button, checkbox, link, radio, and switch roles\nrequire `phx-click`; native rendering makes those controls Tab stops with a\nvisible keyboard focus border. GPUI's pinned click interactivity then handles\nEnter/Space on focused controls, including key-up-only activation and\nfocus-generation cancellation, through the same pointer click callback.\nStructural, composite, and value roles do not automatically become Tab stops.\n\n`accessibility_disabled` is a controlled boolean for button, checkbox, link,\nradio, slider, splitter, switch, and textbox roles. RustQ generates a concrete\n`disabled: bool` field defaulting to false. Disabled nodes retain stable IDs and\ncontrolled value/check state, report AccessKit disabled state, and install no\npointer, keyboard, Tab-stop, or explicit AccessKit Click activation. Re-enabling\nthe same node restores its normal generated interaction policy.\n\nControlled dialogs use gpui-component's native `Role::Dialog`, focus trap, and\nselection scope. Opening focuses the dialog content handle once. Every close\npath—Escape, overlay/close-button callback, or an application-controlled\nfalse transition—restores the stable trigger focus handle when that trigger is\nstill present. Triggerless dialogs skip restoration safely. Escape requests the\nordinary controlled close event only when both `keyboard` and `closable` are\ntrue; Rust does not independently own `open` state. The trigger continues to\nreport the title and controlled expanded state.","ref":"accessibility.html#renderer-independent-contract-direction"},{"type":"extras","title":"Validation rules - Accessibility internals","doc":"A future schema must reject misleading combinations instead of silently\napproximating them. At minimum:\n\n- a node with an explicit role must also have a stable renderer identity;\n- `checked` is restricted to check box, radio, switch, and equivalent toggle\n  roles;\n- `selected` is restricted to selectable collection items and tabs;\n- `expanded` is restricted to disclosures and expandable composite items;\n- orientation is restricted to roles such as slider, splitter, radio group,\n  tab list, and applicable collections;\n- masked input values must never be serialized as accessible values;\n- disabled, read-only, modal, focus restoration, and hidden/decorative behavior\n  must not be claimed until the pinned GPUI API and component action paths can\n  represent them truthfully.\n\nUnknown roles and unsupported state combinations should fail schema validation\nwhile preserving their source spelling in diagnostics.","ref":"accessibility.html#validation-rules"},{"type":"extras","title":"Identity is part of accessibility - Accessibility internals","doc":"A role alone is insufficient. GPUI derives AccessKit node identity from the\nhierarchy of element IDs. IDs must be unique within a frame and stable across\nframes; otherwise assistive technology observes removal and insertion rather\nthan a meaningful update. Collection renderers must not derive repeated text\nnodes from one source location without a stable per-item ancestor or explicit\nID.\n\nThe public accessibility contract must reuse the framework's renderer identity\nrather than introduce a second application-visible accessibility ID namespace.","ref":"accessibility.html#identity-is-part-of-accessibility"},{"type":"extras","title":"Keyboard and focus are separate obligations - Accessibility internals","doc":"An accessibility tree does not make a component operable. Each semantic\ncomponent also needs coherent:\n\n- Tab and Shift-Tab traversal;\n- Enter/Space or arrow-key activation where appropriate;\n- disabled-state exclusion from focus and actions;\n- composite active-descendant behavior;\n- modal focus containment and restoration;\n- monotonic focus requests for imperative restoration.\n\nAccessible actions must invoke the same consumer-owned event path as pointer or\nkeyboard activation. They must not mutate application assigns independently in\nRust.","ref":"accessibility.html#keyboard-and-focus-are-separate-obligations"},{"type":"extras","title":"Explicitly unsupported claims - Accessibility internals","doc":"Until separately implemented and validated, gpui_ex must not claim:\n\n- arbitrary ARIA attribute compatibility;\n- browser accessibility-tree or DOM behavior;\n- live regions, relationships such as `labelledby`/`describedby`, or arbitrary\n  ownership graphs;\n- portable disabled, read-only, modal, hidden, or invalid state solely because\n  similarly named HTML attributes exist;\n- complete screen-reader parity among VoiceOver, Narrator, and Linux AT-SPI;\n- accessibility for visual-only overlays that lack stable IDs, roles, actions,\n  and coherent focus behavior.","ref":"accessibility.html#explicitly-unsupported-claims"},{"type":"extras","title":"Recommended implementation order - Accessibility internals","doc":"1. Add a generated, closed accessibility vocabulary to renderer-independent\n   element attributes.\n2. Map label and description plus a small role set for generic elements.\n3. Preserve semantic component defaults and reject conflicting overrides.\n4. Add selected, expanded, checked, value, and orientation only alongside\n   role/state validation.\n5. Add snapshot and remote round-trip tests.\n6. Add native AccessKit-tree or platform inspection tests where the adapter\n   offers a deterministic harness.\n7. Audit keyboard and assistive-action equivalence for buttons, choices,\n   inputs, sliders, dialogs, tabs, trees, and split handles.\n\nVisual screenshots are not accessibility evidence. Native accessibility claims\nrequire inspecting the platform/AccessKit tree or driving an accessible action.","ref":"accessibility.html#recommended-implementation-order"},{"type":"extras","title":"Native text projections","doc":"# Native text projections\n\nThis note records the extension points available in the pinned native text stack\nbefore GPUI exposes layout-affecting projection primitives publicly.","ref":"text-projections.html"},{"type":"extras","title":"Scope - Native text projections","doc":"The desired renderer facts are:\n\n- inline insertions anchored to UTF-16 document positions that consume shaped\n  width and participate in wrapping;\n- block insertions anchored before or after logical lines that contribute real\n  display rows and scroll height;\n- deterministic document ↔ visual coordinate mapping while inserted content\n  remains outside the Rope, revisions, selections, and undo history.\n\nApplication policy such as completion acceptance, notebook execution, language\nservices, diagnostics, and editor chrome must remain consumer-owned.","ref":"text-projections.html#scope"},{"type":"extras","title":"Pinned stack - Native text projections","doc":"The native host currently pins:\n\n- GPUI at `7bddd16a09cf0084cefb3d98468b178343b9f1e2`;\n- `gpui-component` at `5c5eb1db04dc1146bdec903a8ac74407bd6c2098`.\n\nThe latter provides native text-decoration collections, which are sufficient for\nforeground style runs because those ranges do not change layout. They are not a\nprojection mechanism.","ref":"text-projections.html#pinned-stack"},{"type":"extras","title":"Display-map architecture - Native text projections","doc":"`gpui-component`'s editable input uses this pipeline:\n\n```text\nRope buffer positions\n  -> WrapMap (soft wrapping)\n  -> FoldMap (whole-line visibility)\n  -> DisplayMap (buffer/display row conversion)\n  -> Input element line shaping and painting\n```\n\n`DisplayMap` publicly maps `BufferPoint` and `DisplayPoint`, and `FoldRange`\npublicly represents inclusive whole-line candidates. Its wrapping input is still\nthe Rope text, and `InputState` keeps `DisplayMap` private. The pinned state API\npopulates candidates internally from tree-sitter and exposes only code-editor\nfolding enablement; current upstream `main` additionally exposes\n`apply_highlighter_fold_candidates/2`, but still does not expose authoritative\nactive-fold state for ordinary consumers. Consequently consumers cannot provide\ncomplete fold state without pretending to be a highlighter or reaching through\nprivate state. The generic API request is tracked in\n[gpui-component#2826](https://github.com/longbridge/gpui-component/issues/2826).\n\nThe fold projection only hides interior whole-line wrap rows while keeping both\nboundary lines visible. It does not render a placeholder or represent arbitrary\nintra-line hidden ranges. Hidden ranges therefore remain deferred separately.","ref":"text-projections.html#display-map-architecture"},{"type":"extras","title":"Inline completion is not a general insertion API - Native text projections","doc":"The input's inline-completion path shapes a completion suffix separately and\npaints it after the caret. Additional completion lines are painted as ghost rows\nand increase a local extra-height value.\n\nThis path cannot serve as a neutral inline insertion because:\n\n- it is tied to the focused primary caret and `CompletionProvider` policy;\n- state and setters are private to the input implementation;\n- only one completion item is represented;\n- the first line is painted after layout and does not participate in WrapMap;\n- its background intentionally covers existing text;\n- hit testing, selection, range geometry, and display mapping do not include it;\n- accepting it mutates the document as completion text.\n\nReusing this path would misrepresent completion mechanics as document projection\nand would produce incorrect wrapping and coordinates.","ref":"text-projections.html#inline-completion-is-not-a-general-insertion-api"},{"type":"extras","title":"Block insertions - Native text projections","doc":"No public block-decoration or custom-row collection exists in the pinned input.\nThe input assumes display rows are uniformly one `line_height` tall. Viewport\nrange calculation divides scroll offsets by that fixed height, and painting zips\nshaped lines with visible buffer lines. Inline-completion ghost rows are a local\nspecial case rather than entries in DisplayMap.\n\nA true block insertion therefore needs an explicit row-projection layer that\ncontributes variable or declared row heights to:\n\n- total scroll extent;\n- visible-range lookup;\n- line and gutter y offsets;\n- caret and range geometry;\n- pointer hit testing;\n- document/display conversion.\n\nPainting current `GPUI.Text.BlockProjection` overlays cannot satisfy those\nrequirements and remains intentionally documented as non-layout-affecting.","ref":"text-projections.html#block-insertions"},{"type":"extras","title":"Required upstream extension - Native text projections","doc":"A suitable native extension should add a projection layer owned by `InputState`,\nnot by application-specific editor code. A minimal shape would include:\n\n```text\nInlineInsertion { id, byte_anchor, text, shaping_style, affinity }\nBlockInsertion  { id, line_anchor, placement, height, element/text }\nProjectionSnapshot { stable bounded collections }\n```\n\nThe display map would become conceptually:\n\n```text\nRope buffer\n  -> InlineMap (external shaped insertions)\n  -> WrapMap\n  -> FoldMap\n  -> BlockMap (external rows)\n  -> DisplayMap\n```\n\nExact ordering may differ, but all conversion APIs must observe the same\nprojection snapshot. The native API must also define:\n\n- anchor affinity when edits occur at projection boundaries;\n- precedence for overlapping inline insertions;\n- mapping a pointer inside insertion content back to a document position;\n- selection behavior across insertion content;\n- reconciliation without resetting IME, focus, selection, or scroll;\n- bounded collection sizes and stable identities;\n- behavior under bidi text, ligatures, fallback fonts, and soft wrapping.","ref":"text-projections.html#required-upstream-extension"},{"type":"extras","title":"Decision - Native text projections","doc":"Do not expose `InlineInsertion` or `BlockInsertion` in Elixir yet. Neither GPUI\nnor the pinned `gpui-component` has a public extension that can implement their\nclaimed layout and coordinate semantics. Do not reuse inline completion, inject\nprojection text into the Rope, or promote overlay annotations as true rows.\n\nThe existing APIs remain truthful:\n\n- `GPUI.Text.InlineProjection` is a visual overlay anchored to geometry;\n- `GPUI.Text.BlockProjection` is a visual overlay around a logical line;\n- `GPUI.Text.StyleRun` participates in shaping but does not alter text length;\n- hidden ranges remain deferred pending a public consumer-fold contract.\n\nRevisit layout-affecting projections when the native input exposes a coherent\nprojection collection or after a narrowly reviewed upstream contribution adds\none.","ref":"text-projections.html#decision"},{"type":"extras","title":"Transfer payload internals","doc":"# Transfer payload internals\n\nThis guide records the native and transport boundary behind the public\napplication contract in\n[Resources and display actions](resources-and-display-actions.html). GPUI\ntransfers bounded clipboard and operating-system file facts without moving\nattachment, upload, filesystem, or product policy into the renderer.","ref":"transfers.html"},{"type":"extras","title":"Native capability boundary - Transfer payload internals","doc":"The pinned GPUI revision exposes:\n\n- `ClipboardItem` with string, image, and `ExternalPaths` entries;\n- platform clipboard read and write operations;\n- `FileDropEvent::{Entered, Pending, Submit, Exited}`;\n- `ExternalPaths` as display-machine `PathBuf` values;\n- drag-over, drag-move, and drop listeners for typed native values.\n\nGPUI translates an operating-system file drop into an internal drag containing\n`ExternalPaths`. `Entered` is the only platform phase carrying the paths; later\npending and submit phases carry position while GPUI retains the active drag\nvalue. The renderer therefore owns a bounded drag session that preserves the\npayload until terminal drop or leave.\n\nThe public clipboard layer does not expose arbitrary MIME byte entries\ncoherently across supported platforms. GPUI must not advertise a generic\nclipboard-format API by treating platform-specific or internal representations\nas a portable contract.","ref":"transfers.html#native-capability-boundary"},{"type":"extras","title":"Renderer-owned drag sessions - Transfer payload internals","doc":"The renderer assigns each native drag a monotonic session ID and retains:\n\n- the canonical bounded `GPUI.Transfer.Payload`;\n- the currently hit stable target ID;\n- the latest window-relative native-pixel position;\n- whether enter, movement, leave, or drop is pending.\n\nEnter and drop include the retained payload. Movement is high frequency and can\nbe coalesced to the latest position without losing ordered terminal facts. Leave\nand drop close the session so stale native callbacks cannot attach to a later\ndrag.\n\nRouting uses ordinary element hit testing and typed runtime events. It does not\nintroduce a generic effect bus, synchronous pointer NIF, or renderer-owned\napplication object.","ref":"transfers.html#renderer-owned-drag-sessions"},{"type":"extras","title":"Canonical protocol representation - Transfer payload internals","doc":"`GPUI.Transfer.Payload` and `GPUI.Transfer.Event` are the public normalized\nvalues. Their wire maps use bounded UTF-8 strings, finite numeric coordinates,\na closed `window_native_pixels` coordinate space, and explicit payload presence\nby event phase.\n\nValidation occurs before session dispatch for local and remote events. Paths\nthat cannot be represented as bounded UTF-8 are rejected rather than converted\nlossily. Duplicate paths are removed in first-seen order. No protocol decoder\nopens a path, reads a file, infers MIME, or constructs an attachment.\n\nGenerated event decoding and handwritten native session mechanics share this\ncanonical Elixir-owned schema; native structs are not a public protocol.","ref":"transfers.html#canonical-protocol-representation"},{"type":"extras","title":"Clipboard ordering - Transfer payload internals","doc":"Button activation can request clipboard read, clipboard write, a bounded file\nread, and an ordinary click. The renderer executes them in this order:\n\n```text\nclipboard operations\n→ file read\n→ ordinary click\n```\n\nThis makes the event sequence deterministic while preserving each operation as\nan explicit opt-in. Clipboard reads emit `GPUI.Transfer.Payload`; writes emit an\nacknowledgement after requesting the platform operation.\n\nNative editable text does not route ordinary paste through this explicit button\ncontract. Its immediate platform selection, paste, and IME behavior update the\nnative buffer, and the resulting revisioned transaction is the\napplication-visible fact.","ref":"transfers.html#clipboard-ordering"},{"type":"extras","title":"Remote capability enforcement - Transfer payload internals","doc":"Remote transfer support is additive within the exact current protocol version:\n\n```text\nclipboard_text_v1\nexternal_path_transfer_v1\n```\n\nA display forwards an event only when the corresponding capability was\nnegotiated. The server retains peer capabilities per connection and rejects\ntransfer events from a peer that did not advertise support. Reconnection runs a\nfresh hello before queued events resume.\n\nRemote paths remain facts about the display machine and are never rewritten as\nBEAM-host paths. Bounds are checked before transport and again before runtime\ndispatch. Movement may be display-side coalesced; enter, leave, and drop remain\nordered session facts.","ref":"transfers.html#remote-capability-enforcement"},{"type":"extras","title":"Resource and allocation ownership - Transfer payload internals","doc":"The bounded file-picker action reads one file on the display machine because its\npurpose is to produce portable bytes. Larger workflows remain supervised\napplication tasks rather than long-lived renderer operations.\n\nDecoded raster data can be installed as a runtime resource and referenced from\nlater snapshots. Native code owns allocation and image decoding mechanics;\nElixir owns resource identity, lifetime policy, replacement, and application\nmeaning.","ref":"transfers.html#resource-and-allocation-ownership"},{"type":"extras","title":"Deferred capabilities - Transfer payload internals","doc":"The current transfer contract intentionally excludes:\n\n- arbitrary MIME or custom clipboard bytes;\n- automatic reads of externally dropped paths;\n- automatic uploads or directory traversal;\n- drag sources and cross-application data promises;\n- clipboard history or global observation;\n- application-specific attachment objects;\n- clipboard images until they have a bounded renderer-independent resource\n  contract across platforms;\n- streaming transfer payloads without explicit flow control and cancellation.\n\nAdditions must remain bounded, serializable, display-machine-aware, and neutral\nabout product policy.","ref":"transfers.html#deferred-capabilities"},{"type":"extras","title":"Presentation extension contracts","doc":"# Presentation extension contracts\n\nGPUI presentation extensions are schema-owned optional renderer capabilities.\nThey are not plugins, dynamically loaded native modules, arbitrary component\ninjection, callbacks, or opaque payloads.","ref":"presentation-contracts.html"},{"type":"extras","title":"Architectural position - Presentation extension contracts","doc":"An extension augments an existing `GPUI.Schema.Component` while preserving the\nnormal renderer-independent path:\n\n```text\nGPUI.Schema.Component\n→ GPUI.Element payload\n→ GPUI.Snapshot\n→ GPUI.Display\n→ RustQ-generated decoder and dispatch\n→ handwritten GPUI renderer\n```\n\nApplication topology and fallback policy remain in Elixir. Displays may enhance\npresentation, but capability support never changes snapshot topology or blocks a\nmount.","ref":"presentation-contracts.html#architectural-position"},{"type":"extras","title":"Exact versions - Presentation extension contracts","doc":"Each extension contract declares a stable atom ID, a positive integer version,\nand a closed capability vocabulary. The current built-ins are:\n\n```text\nedge_fade@1  linear_gradient, theme_background\nfrost@1      solid_fallback, translucent_fallback,\n             reduced_transparency\npaint@1      rect, line\n```\n\nNew snapshots carry `__extension_version__` in the renderer-hidden `attrs` map.\nApplications cannot set the field through public builders. Native generated\ndecoders require the exact declared version before decoding the component;\nmissing, malformed, and mismatched versions are rejected.\n\nAn incompatible field or command change increments the contract version. A new\ncapability must not silently broaden an existing closed version when it changes\nthe wire schema. Before the first release, contracts may instead be redesigned\nor replaced directly because no historical payloads are supported.","ref":"presentation-contracts.html#exact-versions"},{"type":"extras","title":"Capabilities and fallbacks - Presentation extension contracts","doc":"A contract's capability list is vocabulary, not an implementation claim.\nDisplays report only behavior that the particular display implements, and that\nsupport is used for diagnostics rather than application topology. The pinned\nGPUI stack exposes blur only as a whole-window background appearance; it has no\nbackdrop filter. Frost therefore declares only the implemented solid,\ntranslucent, and reduced-transparency behavior.\n\nEvery optional enhancement requires a deterministic fallback in the serialized\ncomponent declaration:\n\n- edge fade falls back to ordinary child rendering;\n- frost follows its solid/translucent and reduced-transparency policy;\n- paint falls back to an empty bounded canvas when unsupported.\n\nAccessibility policy is serialized by Elixir and overrides native enhancement.\nIn particular, reduced transparency must disable translucent or blur effects.","ref":"presentation-contracts.html#capabilities-and-fallbacks"},{"type":"extras","title":"Remote advertisement - Presentation extension contracts","doc":"Remote hello messages may include a bounded informational `presentation` list.\nEntries use exact contract versions and capabilities validated against the\ncanonical schema. Missing presentation support is equivalent to an empty list and\nis never a handshake requirement.\n\nTransport capabilities such as `display_v1` and `window_topology_v1` remain\nseparate because they govern whether operations can be exchanged safely.\nPresentation support is diagnostic and display-local.","ref":"presentation-contracts.html#remote-advertisement"},{"type":"extras","title":"Evolution restrictions - Presentation extension contracts","doc":"Extensions may use bounded scalar fields, closed enums, bounded collections, and\nexisting typed resource references. They may not contain functions, modules,\nPIDs, ports, native pointers, arbitrary Rustler resources, shader source,\nunbounded maps, or recursive arbitrary terms.\n\nRustQ owns repetitive contract constants, typed payloads, decoders, atoms, and\ndispatch. Handwritten Rust owns actual GPUI rendering, platform support\ndetection, resource lookup, allocation, and lifecycle.","ref":"presentation-contracts.html#evolution-restrictions"},{"type":"extras","title":"Decision: declarative native motion","doc":"# Decision: declarative native motion\n\nGPUI motion is a bounded presentation request on ordinary declarative\ncontainers. Elixir owns when motion starts and the final tree; the display owns\ntransient interpolation and frame scheduling.","ref":"declarative-motion.html"},{"type":"extras","title":"Contract - Decision: declarative native motion","doc":"A motion request requires a stable element `id` and a monotonically increasing\n`motion_request` token:\n\n```heex\n \n  ...\n \n```\n\nA new token starts an entrance transition from the declared opacity and pixel\noffsets to the ordinary, fully opaque zero-offset presentation. Re-rendering\nwith the same token preserves native animation state rather than restarting it.\nThe first contract intentionally does not animate layout dimensions, text\nshaping, colors, scroll position, window geometry, or arbitrary style values.\n\nSupported bounds are:\n\n```text\nrequest token       non-negative integer\nduration            1–10,000 ms\ndelay                0–10,000 ms\nopacity              0.0–1.0\nx/y offset           -4,096–4,096 native pixels\neasing               linear | ease_in | ease_out | ease_in_out\npolicy               respect_system | disabled\n```\n\n`respect_system` is the default and uses GPUI's application-level reduced-motion\npreference. GPUI immediately renders the destination state and schedules no\nanimation frames when reduced motion is active. `disabled` also renders the\ndestination without wrapping the element in animation state. An `always` mode\nis intentionally absent because pinned GPUI animation wrappers always honor the\nsystem reduced-motion setting.","ref":"declarative-motion.html#contract"},{"type":"extras","title":"Ownership and interruption - Decision: declarative native motion","doc":"The snapshot carries only the request and bounded declaration. Native state\nowns the start time, easing progress, delay phase, and frame requests. Changing\nthe token replaces the animation identity and starts the new declaration.\nRemoving the element drops its transient state. No completion event is emitted:\napplication correctness must not depend on display timing, and remote displays\nmay observe different frame schedules.\n\nMotion does not alter hit-testing policy, accessibility identity, controlled\nvalues, or the authoritative snapshot. Static layout and destination styling\nremain ordinary classes and styles.","ref":"declarative-motion.html#ownership-and-interruption"},{"type":"extras","title":"Decision: declarative native window chrome","doc":"# Decision: declarative native window chrome\n\nGPUI exposes two renderer-independent window chrome modes:\n\n```elixir\nwindow \"Application\" do\n  chrome(:system)\n  root(MyView)\nend\n\nwindow \"Application\" do\n  chrome(:content)\n  root(MyView)\nend\n```\n\n`:system` is the default. The platform owns the title bar and its ordinary\ncontrols. `:content` extends application content into the title-bar region and\nlets the application compose its visible chrome from ordinary elements.","ref":"window-chrome.html"},{"type":"extras","title":"Neutral control regions - Decision: declarative native window chrome","doc":"Application-rendered chrome uses `window_control` on ordinary containers:\n\n```heex\n \n   Application \n   \n    –  \n    □  \n    ×  \n \n```\n\nThe closed vocabulary is:\n\n- `drag`\n- `minimize`\n- `maximize`\n- `close`\n\nThese are native window-control regions, not ordinary application events.\nVisible labels, icons, spacing, hover presentation, and control arrangement\nremain application-owned. Consumers must not assume identical control geometry\nor placement across platforms.","ref":"window-chrome.html#neutral-control-regions"},{"type":"extras","title":"Platform interpretation - Decision: declarative native window chrome","doc":"- **macOS:** content chrome uses a transparent full-size title bar and retains\n  the system traffic lights. Drag regions ask AppKit to perform the current\n  window drag. Application-rendered minimize/maximize/close controls are\n  supported but usually unnecessary when traffic lights remain visible.\n- **Windows:** content chrome removes the standard caption presentation.\n  Control regions use native non-client hit testing, preserving Snap Layouts\n  and ordinary caption-button behavior.\n- **Linux:** content chrome requests client-side decorations. Wayland\n  compositors may refuse the request; X11 may fall back to server decorations\n  when no compositor supports client decoration. When client decoration is\n  active, the application must render usable controls.\n\nChrome does not promise pixel-identical windows or identical system-control\nplacement.","ref":"window-chrome.html#platform-interpretation"},{"type":"extras","title":"Snapshot authority and reconciliation - Decision: declarative native window chrome","doc":"Chrome is a `GPUI.WindowSpec` fact and is serialized in ordinary snapshots,\nincluding remote snapshots. It is not an imperative remote-window RPC.\n\nChrome is a creation-time platform option in the pinned GPUI APIs. If a\nsnapshot changes a window's chrome or another creation-time window option, the\nlocal display closes and reopens that native window while retaining the same\nsession window ID. Native focus, position, and other transient platform state\nmay therefore reset. Applications should normally keep chrome stable for a\nwindow's lifetime.\n\nThe window title remains an accessibility and task-switcher fact even when the\nvisible title is application-rendered.","ref":"window-chrome.html#snapshot-authority-and-reconciliation"},{"type":"extras","title":"Close policy - Decision: declarative native window chrome","doc":"A content-chrome close control enters the same asynchronous close-request path\nas ordinary system chrome. It never blocks the GPUI loop waiting for the BEAM.\nViews with `handle_window_event/3` may keep the window open with\n`{:noreply, assigns}` or approve closure with `{:close, assigns}`.","ref":"window-chrome.html#close-policy"},{"type":"extras","title":"Deliberately deferred - Decision: declarative native window chrome","doc":"The first contract does not expose:\n\n- macOS traffic-light coordinates;\n- arbitrary platform button layouts;\n- custom window-menu policy;\n- Mica, blur, or transparent window backgrounds;\n- controlled minimized, maximized, or fullscreen state;\n- window geometry commands;\n- platform-specific title-bar APIs.\n\nThose require separate bounded contracts and platform evidence.","ref":"window-chrome.html#deliberately-deferred"}],"proglang":"elixir","content_type":"text/markdown","producer":{"name":"ex_doc","version":"0.40.3"}}