# Used by "mix format" from the umbrella root.
[
  inputs: [
    "{mix,.formatter}.exs",
    "config/**/*.{ex,exs}",
    "apps/*/{lib,test,dev,codegen}/**/*.{ex,exs}",
    "{examples,test}/**/*.{ex,exs}"
  ]
]
