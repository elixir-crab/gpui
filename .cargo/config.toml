[build]
# Windows cannot execute the repository's Bash wrapper. Maintainers and CI can
# override this with CARGO_BUILD_RUSTC_WRAPPER="" for native MSVC builds.
rustc-wrapper = ".githooks/rustc-wrapper"
incremental = false

[env]
ZCCACHE_PATH_REMAP = { value = "auto", force = false }
